format_version = 3

[job]
name = "Adaptive Debug Fixture"

[[tools]]
id = 1
name = "Fixture End Mill"
type = "end_mill"

[[models]]
id = 1
path = "square.svg"
name = "Square"
kind = "svg"
units = { kind = "millimeters" }

[[setups]]
name = "Setup 1"

# NOTE: no toolpaths defined here. This fixture is loaded as a stub
# for manual GUI debugging of adaptive operation debug_options.
# Add a toolpath via GUI after loading the project.
# The ProjectSession loader silently drops toolpath sections without
# an `[setups.toolpaths.operation]` sub-table (project_file.rs:745).
