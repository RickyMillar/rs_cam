format_version = 3

[job]
name = "Fixture 3D"

[[tools]]
id = 1
name = "Fixture Ball Nose"
type = "ball_nose"

[[models]]
id = 1
path = "flat_plate.stl"
name = "Flat Plate"
kind = "stl"
units = { kind = "millimeters" }

[[setups]]
name = "Setup 1"

# NOTE: no toolpaths defined here. This fixture exists to exercise the
# 3D model loader (see tests/fixture_projects_load_2d_and_3d_models).
# Toolpaths should be added via `add_toolpath` MCP / GUI after load.
