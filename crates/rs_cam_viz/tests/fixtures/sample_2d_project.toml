format_version = 3

[job]
name = "Fixture 2D"

[[tools]]
id = 1
name = "Fixture End Mill"
type = "end_mill"

[[models]]
id = 1
path = "square.svg"
name = "Square"
kind = "svg"
units = { kind = "millimeters" }

[[setups]]
name = "Setup 1"

# NOTE: no toolpaths defined here. This fixture exists to exercise the
# 2D model loader (see tests/fixture_projects_load_2d_and_3d_models).
# Toolpaths should be added via `add_toolpath` MCP / GUI after load.
# The ProjectSession loader silently drops toolpath sections without
# an `[setups.toolpaths.operation]` sub-table (project_file.rs:745).
