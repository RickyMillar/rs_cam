# Architecture consolidation — status tracker

**Read this before `PLAN.md`.** The audit was a read-only review at `627ef997`
on 2026-09-09. It says of itself: *"Runtime consequences were not tested."*
**98 commits landed between that commit and this tracker.**

On 2026-09-10 all twelve findings were verified against the current tree by
four read-only lanes. Their full reports are in `verify/LANE_A..D.md`, each
claim carrying a `path:line` at today's tree. **No lane ran a test.** Every
verdict below is from reading, and the reports say so; treat "STILL TRUE" as
"the code still has this shape", not "the failure was reproduced".

> **Protocol.** The orchestrator updates THIS file only. `AUDIT.md` and
> `PLAN.md` stay verbatim as produced. States: `TODO` / `IN PROGRESS (who)` /
> `PARTLY CLOSED` / `BLOCKED (on what)` / `DONE (commit)`. Never delete a row;
> mark it `DROPPED (why)`.

---

## Execution checkpoint — 2026-09-10, N1 DONE

- **N3: DONE (`069a2314`).** Already fixed by G-STEPUNITS before this
  execution. The project loader applies `EnrichedMesh::apply_uniform_scale`;
  `tests/step_project_load.rs::both_doors_apply_a_step_models_declared_units`
  pins both import doors. This phase did not reimplement it. The default
  heavy gate does not enable `step`, so its zero-test STEP binaries are not
  a rerun of that sentry.
- **N1: DONE (`d4e1154b`; sentry `2687b82b`).** Reproduced through core
  checked export before the fix: a disabled operation's label was emitted
  from its retained result. Core
  now filters `!tc.enabled` before result lookup and datum transformation.
  `tests/export_disabled_cached_n1.rs` asserts disabled motion, tool, RPM and
  pre/post snippets are absent, an enabled control remains, and re-enabling
  restores the cached operation without regeneration. Cache retention and
  enabled missing/stale-result policy are unchanged. Both CLI callers use
  this corrected core door; the cached-disabled reproduction is a core test,
  not a separate CLI-command reproduction.
- **Verification:** focused N1 1 passed; core G-code 35; datum 5; GUI export
  6; CLI 31. `cargo fmt --all -- --check` and workspace all-target Clippy
  with `rs_cam_core/heavy-tests` / `-D warnings` passed. The full core heavy
  gate completed in 2718 s: **3783 passed, 1 failed, 288 ignored**, across
  301 result targets (299 integration binaries plus library and doctests).
  This is **not a green full gate**. Its sole failure is the established
  F-036b `modulation_raises_cutting_chipload_toward_band`: **0.0214** against
  **[0.0320, 0.0550]**, exactly the baseline documented in
  `../ui_fix_2026-09-09/reports/J2.md` (lines 303–367) and its sibling
  `HANDOVER.md` (lines 74–86).
  The all-F-word median includes G-RAMPCONTAIN's extra entry segments; the
  cutting-only population is 0.0550. No assertion was weakened or ignored.
  Local logs: `/tmp/rs_cam_n1/`, `/tmp/rs_cam_n1_clippy.log`,
  `/tmp/rs_cam_n1_core_heavy.log` (matching `.status` files); these are local
  execution artifacts, not repository fixtures.
- **Next:** N2. The operator gave standing approval on 2026-09-10 ("I approve
  it all … assume approval … dont worry about me for the gates"), so the
  per-item human gate is lifted. The tests, the review step and the stop
  conditions all stand unchanged. N4 and N5 remain open; N6 stays with
  Phase 1A. No architecture phase is claimed complete. `PLAN.md` and
  `AUDIT.md` remain verbatim.

## Execution checkpoint — 2026-09-10, N5 DONE

- **N5: DONE (`1e4373aa`; sentry `3b8cd298`).** `OperationParams::set_stepover`
  and `set_depth_per_pass` are optional trait methods with empty default
  bodies. An operation with no such field inherited a setter that discarded
  the value, and the named arm of `ProjectSession::set_toolpath_param` then
  stamped `Manual` provenance on the absent field, ran
  `invalidate_result_chain` and reported `Ok(())`. Both setters now report
  whether they wrote. The arm refuses on `false`, before the stamp and
  before the invalidation, with the generic serde arm's own wording from one
  shared `unknown_param_error`. The DR-LIVE range gate moves into
  `check_param_range`, which every numeric route calls. The reject
  populations are **11** operations for `stepover` and **14** for
  `depth_per_pass`; the sentry asserts both counts and both directions, so a
  fix that refused every write would not pass. The three alias setters
  (Pencil `offset_stepover`, Waterline `z_step`, RampFinish `max_stepdown`)
  keep working. `rs_cam_cli run --set` turns a silent success on an
  unsupported field into an error, by intent.
- **Verification:** sentry RED pre-fix at `2 passed; 3 failed` (a runtime
  red), GREEN post-fix at `5 passed; 0 failed`, and GREEN again after the
  format. Core `--lib session` 157; `toolpath_rebind_g_mcprebind` 9;
  `rs_cam_cli` 31. The core dev loop (no `heavy-tests`) ran 290 result
  targets: **3741 passed, 1 failed, 271 ignored**. Its sole failure is the
  established F-036b `modulation_raises_cutting_chipload_toward_band` at
  **0.0214** against **[0.0320, 0.0550]** — the baseline in
  `../ui_fix_2026-09-09/reports/J2.md`. `cargo fmt --all -- --check` and
  workspace all-target Clippy with `rs_cam_core/heavy-tests` / `-D warnings`
  both pass. The format moved the sentry file only, so it folded into the
  sentry commit. `rs_cam_viz` tests compiled under Clippy; they were not
  executed. Local log: `/tmp/n5_core.log` — a local artifact, not a
  repository fixture.

## Execution checkpoint — 2026-09-10, N4 DONE

- **N4: DONE (`44c68add`; sentry `6d1d2026`).** `gui_and_mcp_diagnostic_ids_match`
  hand-rebuilt the session's input and compared the result with itself, so it
  could not bite. The core diagnose route built its `ResolvedHeights` from
  defaults instead of the toolpath's own `heights`, so a pinned Top Z and a
  retract pinned below the feed plane reached the GUI ribbon and never
  reached the MCP route. `ResolvedHeights::from_heights` is the one
  resolver; the diagnose route passes `&tc.heights`, and the GUI's
  `diagnostics_heights` delegates to it. The tautology test is deleted and
  the sentry replaces it: it drives the real MCP entry point and the ribbon
  on one session and asserts the two id sets agree.
- **Verification:** sentry RED pre-fix at `1 passed; 2 failed` (a runtime
  red; arm 1, the auto-heights baseline, was already green), GREEN post-fix
  at `3 passed; 0 failed`. `depth_beyond_stock_core_g_depthstockcore` 8;
  `inert_claims_dial_f4` 5; `unified_finish_dropped_band_finding_d1` 3. The
  whole `rs_cam_viz` suite: **606 passed, 0 failed**, across 36 result
  targets — the deleted test's module still compiles and carries no unused
  import. The core dev loop is unchanged from the N5 checkpoint at **3741
  passed, 1 failed, 271 ignored** across 290 result targets, its sole
  failure the established F-036b baseline. `cargo fmt --all -- --check`
  needed no change on either commit, and workspace all-target Clippy with
  `rs_cam_core/heavy-tests` / `-D warnings` passes.

## Execution checkpoint — 2026-09-10, N2 DONE

- **N2: DONE (`736a2959`; sentry `8634acf1`).** The cycle-time integrator
  published `toolpath_runtimes` for every toolpath and folded the project
  total over the engagement summary list, which carries no drill row. The
  modulation re-time then rebuilt only part of that, so the two published
  runtimes disagreed and the project total dropped the drill.
  `simulation_cut::publish_cycle_times` is now the one publisher: the
  integrator tail and the re-time both call it, so every runtime reaches
  every surface on one clock. `session::compute::reintegrate_toolpath`
  builds the map the re-time hands it.
- **Verification:** sentry RED pre-fix at `3 passed; 2 failed` (a runtime
  red). Its three fixture and non-vacuity tests were green pre-fix, so the
  fixture is not vacuous. GREEN post-fix at `5 passed; 0 failed`.
  `drill_cycle_time_integration_g_drilltime` 4;
  `air_cut_one_time_base_g_airdenom` 6; viz `cycle_time_basis_g_timeest`
  12. `adaptive_feed_modulation_pipeline_f036b` reads **9 passed, 1
  failed** — its one failure is the established red-by-design
  `modulation_raises_cutting_chipload_toward_band` at **0.0214** against
  **[0.0320, 0.0550]**, the same number as before this fix. No other
  expected number moved. The core dev loop: **3746 passed, 1 failed, 271
  ignored** across 291 result targets (the five new sentry tests are the
  whole delta from the N4 checkpoint). `rs_cam_viz` 606; `rs_cam_cli` 31.
  `cargo fmt --all -- --check` needed no change on either commit, and
  workspace all-target Clippy with `rs_cam_core/heavy-tests` /
  `-D warnings` passes.

## Execution checkpoint — 2026-09-10 late, N1–N5 DONE

- **N1** `d4e1154b` (sentry `2687b82b`), **N5** `1e4373aa` (sentry `3b8cd298`),
  **N4** `44c68add` (sentry `6d1d2026`), **N2** `736a2959` (sentry `8634acf1`).
  Each sentry ran RED alone and GREEN after its fix; the red output is in
  each fix commit's body. N3 was already closed by G-STEPUNITS.
- Full core heavy gate after all four: **3793 passed, 1 failed, 288 ignored**
  across 303 result targets. The one failure is the F-036b instrument, red by
  design. Not a green full gate. Viz suite 606/0, CLI 31/0. fmt and clippy
  (heavy-tests, `-D warnings`) clean. Log `/tmp/rs_cam_gate_n245.log`.
- **Open before the phases:** N6 (folds into Phase 1A), N7 (needs an operator
  ruling), N9 (small), N10 (needs an operator ruling), N11 (Phase 4B). N8 was
  closed inside N4.
- **The N1 checkpoint above says "N4 and N5 remain open". That was true when
  written and is superseded by this checkpoint.**
- The three landing commits carry a `docs(status): Nx DONE` row edit each.
- Process note for the next verifier: never `git reset --hard` in this tree.
  It drops the foreign `.mcp.json` edit. The N5 verifier did that once and
  restored the file byte-identical (blob `e74e2a8c`).

## Execution checkpoint — N9 DONE (`52fdd7dd`)

Standing operator approval covers commits and advancing items; tests, review,
resource limits and unresolved-policy stop conditions remain binding. N1–N5
were already landed and were not repeated. N9 now snapshots canonical session
precondition and model-reference contexts alongside the GUI's temporary entry;
the ribbon requires both contexts. The extended N4 sentry consumes that same
production snapshot, not a hand-rebuilt parallel input path. Missing Rest
predecessor and dangling-model cases reproduced at **4 passed / 2 failed**
before the fix; all **6 passed** after it, including valid controls. GUI
load-verdict, feeds, generation-stat and height inputs are preserved.

Review ACCEPT. Full viz **609/0**, MCP **29/0**, format and heavy-enabled
workspace Clippy pass. Full core heavy: **3793 passed, 1 failed, 288 ignored**,
303 result targets; only F-036b at **0.0214** vs **[0.0320, 0.0550]**, exactly
the preceding baseline. This is not a green full gate. No live GUI/MCP check
was run. Local evidence: `/tmp/rs_cam_n9_red.log`, `/tmp/rs_cam_n9_retry.log`,
`/tmp/rs_cam_n9_gates/summary.json` and sibling gate logs. An attributable type
import compilation error in the first implementation was fixed in one retry;
the final sentry also guards actual production snapshot assembly.

`.mcp.json` remains byte-identical (Git blob
`e74e2a8ce2f72ac0ed51f3f7b95638d4133de980`); raw file SHA-1 is a different hash
algorithm input and must not be compared to the Git blob hash.
Next is Phase 0's remaining executable evidence. N7 and N10 remain explicitly
decision-blocked; N6 and N11 retain their phase assignments.

## Phase 0 policy decisions — pending operator rulings (2026-09-11)

| # | Question | Facts (read, not run) | Options |
|---|---|---|---|
| P0-D1 **DONE (a) (`281c4ae7`; sentry `09263c04`)** | **Coolant: which export door is right?** | `ToolpathConfig::coolant` is a per-toolpath core field (`session/mod.rs:769`), round-trips both project loaders, and the emitter carries an A3 fix so a coolant change between same-tool phases is not suppressed. The GUI door (`viz/io/export.rs:340`) and the CLI job door (`cli/src/main.rs:547,613`) honour it. The core door hardcodes `CoolantMode::Off` (`core/gcode/mod.rs:367`), so `rs_cam_cli project --emit-gcode` and `rs_cam_cli run` drop the setting silently. **No GUI control and no MCP setter writes the field**; the export wizard's tally points at an inspector control that does not exist. | (a) core honours `tc.coolant`; the Phase 0 test inverts into an equality. (b) declare the field dead and delete it with the test. Opposite Phase 1 work; not decided here. |
| P0-D2 | **N7: retime on an unguarded kinematics fallback** | See row N7. | (a) guard with `is_some()` so `kinematics: None` keeps live-sim runtime byte-identical, per `machine.rs:141-146`. (b) accept the fallback and change the readiness label rule. |
| P0-D3 | **N10: `radial_finish` unranged divisors** | See row N10. | (a) add `ParamRange::greater_than(0.0)` rows (a threshold; operator's call). (b) leave and record. |

## Operator rulings — 2026-09-11

- **Q5 (test-fixture door) — ANSWERED 2026-09-11, "take the recommendation":** a `pub ProjectSessionBuilder` for setup-time construction plus `apply` for the live-mutation test sites (about 117 sites over three crates). WP7 is no longer blocked; it still requires WP5, WP6 and WP6b.
- **No large test gates** (2026-09-11): verifiers run sentries, targeted suites, the small crates and lint only.
- **Close-out rulings (2026-09-13, plan §27):** WP19, WP14b and WP15b run now, in that order with WP22 second; ONE capped whole-core dev loop runs after they land (the heavy gate does not); G-FEEDOPTPLUNGE becomes WP22 (cap plunges at the plunge rate in the feedopt pass); verifiers use debug builds only while the GUI runs under MCP.


- **Branch.** Work on `master` directly. `ui-fix-2026-09-09` was fast-forwarded
  into master at `8f14e02e`. No pull requests. This is a personal project.
- **Architecture.** `RULING_ONE_COMMAND_SURFACE_DRAFT.md` is ADOPTED as Phase
  0's architecture ruling: four kinds (`Command` / `Query` / `Job` /
  `UiCommand`), unconstructible resolved inputs, the six mutation hatches
  `pub(crate)`, `macro_rules!` registry with a completeness sentry, CLI surfaces
  declared per row and never generated. First step: `SetToolpathParam` alone.
- **P0-D1 coolant → (a).** The core export door honours `ToolpathConfig::coolant`.
  The Phase 0 parity test inverts to equality.
- **N7 → (a).** The modulation retime guards on `machine.kinematics.is_some()`;
  a machine with no kinematics block keeps the plain simulated runtime.
- **N10 → add rows.** `angular_step` and `point_spacing` get
  `ParamRange::greater_than(0.0)` in the `peck_depth` shape. Operator-authored
  threshold.

## Implementation plan — 2026-09-11

`IMPLEMENTATION_PLAN.md` (`fe080c6d`) sequences the adopted ruling over the nine
phases: 15 work packages, dependency order forced by types, per-package call-site
inventory, sentries, deletions, rollback, risk register, per-phase definition of
done, tracker protocol. Reviewed adversarially (round 1 REJECT, ten blockers,
all applied; round 2 ACCEPT WITH CHANGES, applied). **Nothing is implemented.**
Q5 (which test-fixture door) blocks WP7; WP1 (`SetToolpathParam` through `apply`)
can start. Next: operator reads the plan; compact; start WP1 in a gap.

## Command surface — work packages (2026-09-11)

**Programme checkpoint 2026-09-12:** all sixteen work-package rows DONE (WP1–WP13,
WP7a, WP11b+fix). Definition of done §7 as corrected by §23: the hatch greps at 0 (the `MutationKind` / `compute_stale_set` rows are NOT at 0 — kept for two callers, see REVIEW_COMPLETENESS_2026-09-12.md M1); N6/N14/N15
closed; N12 items 1–10 closed. Full heavy gate NOT run (operator ruling 2026-09-11).

Per `IMPLEMENTATION_PLAN.md` §8. One row per package. States: `TODO` /
`IN PROGRESS (who)` / `BLOCKED (on what)` / `DONE (fix hash; sentry hash)`.
Never delete a row; mark it `DROPPED (why)`.

| WP | Subject | State | Evidence |
|---|---|---|---|
| WP1 | Row 1: `SetToolpathParam` through `apply` | DONE (`90c8e90e`; sentry `255f6fe3`) | Sentries `command_registry_completeness` (8), `command_registry_surfaces` (3), `mutation_paths_invalidate_alike_p0::n15_apply_reports_the_set_the_setter_dropped` (8). All three were compile-fail red on the sentry commit. Suites: core 3777 passed / 1 red by design (`f036b::modulation_raises_cutting_chipload_toward_band`) / 271 ignored; viz 621; CLI 32; MCP 29; core `--lib session::` 154. `cargo fmt --all -- --check` and `cargo clippy --workspace --all-targets --features rs_cam_core/heavy-tests -- -D warnings` both clean. No heavy gate: WP1 is not a phase end. |
| WP2a | MCP wire compatibility pin (shape, not only name) | DONE (`ecee1ee5`; sentry `8f4faf43`) | Sentry `crates/rs_cam_viz/tests/mcp_wire_surface_pin.rs`, two arms: `wire_surface_matches_the_checked_in_snapshot` and `every_mcp_reached_command_row_is_on_the_wire`. Both were red on the sentry commit, because the snapshot did not exist. The snapshot `crates/rs_cam_viz/tests/snapshots/mcp_wire_surface.json` pins **78** tools, one row per tool: the wire name and the `inputSchema`. `cargo test -p rs_cam_viz -p rs_cam_cli --test mcp_wire_surface_pin` also passes; that build turns the `preserve_order` feature of `serde_json` on, which `cargo tree -e features` shows is absent from the `-p rs_cam_viz` build, so the key canonicalisation holds on both stores. Suites: viz 623 passed / 0 failed. `cargo clippy --workspace --all-targets --features rs_cam_core/heavy-tests -- -D warnings` and `cargo fmt --all -- --check` both clean. No heavy gate: WP2a is not a phase end. |
| WP3 | `Effects` everywhere; `AdoptResult` | DONE (`25f37b74`; sentry `0d22acaf`) | Sentry `crates/rs_cam_core/tests/adopt_result_rejects_stale_completion.rs`, five arms: `a_completion_for_a_superseded_parameter_set_is_refused`, `an_edit_to_another_toolpath_does_not_refuse_this_completion`, `an_absent_index_is_refused_as_absent`, `every_producer_reports_the_dropped_set`, `the_enable_toggle_excludes_its_own_index`. The sentry commit alone was compile-fail red, 14 errors: `AdoptResultArgs`, `Command::AdoptResult` and `SessionError::StaleCompletion` did not exist, and the producers returned `()`. Green on the fix: 5 passed / 0 failed. Two in-crate viz sentries in `crates/rs_cam_viz/src/controller/tests.rs` prove the drain hands the stamp through: `drain_refuses_a_completion_whose_revision_moved` and `drain_adopts_a_completion_whose_revision_is_current`. The two P0 contract tests `the_setter_the_inspector_door_and_the_replacement_agree` and `a_dropped_result_and_a_bumped_revision_are_the_same_event` pass with their assertions unchanged; only the arm helpers, the `.revision` read and the `AdoptResult` door moved in that file. `remove_toolpath`'s MCP arm keeps an empty stale list by ruling (`bump_all_revisions` re-keys, it does not drop). Suites: core `--test adopt_result_rejects_stale_completion` 5, `--test mutation_paths_invalidate_alike_p0` 8, `--test command_registry_completeness` 8, `--test toolpath_rebind_g_mcprebind` 9, `--test set_param_refuses_absent_field_n5` 5, `--test export_honors_coolant_p0d1` 2, `--test model_path_round_trip_g_modelrelink` 6; core `--lib session::` 154; viz 625 (was 623, plus the two new sentries); CLI 32; MCP 29 — all 0 failed. The verifier added one change beyond the writers' commits: `let _ =` at `crates/rs_cam_core/tests/command_registry_completeness.rs:395`, because `Effects` is `#[must_use]` and the writers' scan did not cover the core test targets. `cargo clippy --workspace --all-targets --features rs_cam_core/heavy-tests -- -D warnings` and `cargo fmt --all -- --check` both clean. No whole-core suite and no heavy gate: operator ruling 2026-09-11. |
| WP4 | MCP Mutations section onto `McpRequestKind::Core` | DONE (`1c399a71`; sentry `4dfde9cc`) | Sentry `crates/rs_cam_core/tests/mcp_mutation_rows_reach_core.rs`, seven arms over six rows plus the registry rule: `the_registry_declares_every_mcp_mutation` (the floor is 28 MCP mutation rows, so a shrunk registry cannot pass), the stock, machine, heights, setup-models, debug-options and toolpath-add arms. The sentry commit alone was compile-fail red, 9 errors: `E0432` on six `*Args` imports, `E0599` on `Command::SetToolpathHeights`, `SetStockConfig`, `SetMachine`, `AddToolpath`, `SetSetupModels` and `SetToolpathDebugOptions`, and `E0609` twice on `Effects::created`. Green on the fix: 7 passed / 0 failed. **Registry 6 rows to 38** (WP4 adds 32 `Command` rows; 29 `Command` rows declare `mcp: Reached` with `set_toolpath_param`), and the MCP dispatch collapses **29 Mutations arms to ONE** `McpRequestKind::Core` arm. The wrapper carries the wire request (`CoreRequest`), not a core `Command`; the conversions run on the GUI thread, because twenty of them read the session and the MCP server thread holds none. Three holdouts stay hand-written per §15 ruling 4 (`apply_feeds`, `plan_multitool_finishing`, `export_gcode`) and `load_project` keeps its own arm, so `compute_stale_set`, `MutationKind` and `StaleSet` stay until `add_toolpath_via_gui` and `apply_feeds` read `Effects`. New viz sentry `crates/rs_cam_viz/tests/mcp_core_arm_describes_every_row.rs` (4): every `Reached` `Command` row has a `CoreRequest` variant whose `id()` names it, no two variants claim one row, the describe step names every registry row, and no wildcard sits at the top of `match id`. `mcp_toasts_report_outcome_g_mcptoast` (9) is retargeted, not deleted: its literal table now reads `core_toast_for`. Suites: core `--test mcp_mutation_rows_reach_core` 7, `--test command_registry_completeness` 10, `--test mutation_paths_invalidate_alike_p0` 8, `--test adopt_result_rejects_stale_completion` 5, `--test toolpath_rebind_g_mcprebind` 9, `--test set_param_refuses_absent_field_n5` 5, `--test restore_snapshot_invalidates_like_the_setter_n14` 4, `--test replace_toolpath_config_gates_on_the_signature` 4, `--test job_three_steps_equal_generate_toolpath` 5; core `--lib session::` 156; viz 634 over 41 binaries (was 628, plus the new sentry and the retargeted arms), of which `mcp_wire_surface_pin` 2 **unchanged — the WP2a snapshot did not move**, `command_registry_surfaces` 3, `mcp_rebind_surface_g_mcprebind` 6, `mcp_escape_hatches` 15, `add_toolpath_via_gui_g_guiadd` 9, `get_notifications_g_toastread` 8, `ribbon_and_mcp_diagnostic_ids_n4` 6; CLI 32; MCP 29 — all 0 failed. The rebase onto master (WP5, WP7a, WP8, WP10) left eleven conflicts; nine were one shape (master's `ProjectSessionBuilder` fixture beside this commit's `Effects` return handling) and both sides are kept. `session/command.rs` keeps every module-doc paragraph and puts WP4's rows BEFORE `RestoreToolpathSnapshot`, `ReplaceToolpathConfig` and `GenerateToolpath`; `session/mod.rs`'s re-export list is the union. The verifier folded eight changes into the fix commit, all of them master rows the writers could not see: `Effects.created` reads at `session_builder_preserves_ids.rs:253-275` and `app/mcp.rs:5462`, `let _ =` on a `#[must_use]` `Effects` at `mutation.rs:2713,2714,2803,2805,2809` and eight sites in the WP5 and WP8 sentries, the three master rows added to both exhaustive `match CommandId` arms at `app/mcp/commands.rs:1628,1730` (all three answer "not a wire mutation"), two dead `display_name` statements deleted at `app/mcp/commands.rs:1878,1899`, `clippy::expect_used` on the test module at `ui/properties/operations/mod.rs:2294`, and the kind filter at `mcp_core_arm_describes_every_row.rs:81` — WP10's `GenerateToolpath` declares `mcp: Reached` but is a `Job` row that reaches the wire through its own three steps, never through this arm. `cargo clippy --workspace --all-targets --features rs_cam_core/heavy-tests -- -D warnings` and `cargo fmt --all -- --check` both clean. No whole-core suite and no heavy gate: operator ruling 2026-09-11. |
| WP5 | GUI inspector toolpath door | DONE (`caf8fbe6`; sentry `973834f7`) | Sentry `crates/rs_cam_core/tests/replace_toolpath_config_gates_on_the_signature.rs`, four arms: `a_replacement_outside_the_signature_lands_and_drops_nothing`, `a_replacement_that_moves_the_signature_drops_the_chain`, `the_command_writes_the_three_unsupplied_fields_verbatim`, `the_row_declares_its_surfaces`. The viz half is two in-crate tests in `crates/rs_cam_viz/src/controller/tests.rs`: `the_projection_keeps_the_three_fields_the_entry_cannot_supply_wp5` and `an_open_panel_that_edits_nothing_drops_no_result_wp5`. The sentry commit alone was compile-fail red on both targets: core read 10 errors (`E0432` on `ReplaceToolpathConfigArgs`, `E0599` on `Command::ReplaceToolpathConfig`, on `CommandId::ReplaceToolpathConfig`, on `ToolpathConfig::generation_inputs_signature` and on `ToolpathConfig::clone`), and `rs_cam_viz --lib` read one (`E0425: cannot find function project_entry_onto in module crate::ui::properties`). The signature moved to core as `ToolpathConfig::generation_inputs_signature` with nine fields in — operation, dressups, heights, boundary, rest analysis, tool id, model id, stock source, face selection — and the viz copy is deleted; the inspector applies a projection each frame through `Command::ReplaceToolpathConfig` and stamps `Effects.stale`; the stale-stamp ordering defect the writer found is fixed in the same commit (`write_entry_runtime_to_gui` runs BEFORE the config write-back, because it copies the pre-draw `entry.stale_since` and the reverse order erased the fresh stamp). The dead `ProjectSession::replace_toolpath_config` is deleted. P0 flip: arm 4 of `mutation_paths_invalidate_alike_p0.rs` moved onto the command and arm 3 stays on the raw `invalidate_toolpath_inputs` door, so `the_setter_the_inspector_door_and_the_replacement_agree` keeps its independent comparison (plan §4 WP5 non-vacuity trap). Suites: core `--test replace_toolpath_config_gates_on_the_signature` 4, `--test mutation_paths_invalidate_alike_p0` 8, `--test command_registry_completeness` 10, `--test restore_snapshot_invalidates_like_the_setter_n14` 4; core `--lib session::` 156 (was 155: one gate test replaced by two); viz 628 over 40 binaries (was 626, plus the two new in-crate tests), of which `feeds_apply_drops_result_n13` 5, `command_registry_surfaces` 3, `mcp_wire_surface_pin` 2, `boundary_controls_always_visible_g_boundaryinherit` 3, `freshness_surfaces_g_freshrender` 6, `model_refresh_invalidates_results_g_rescalestale` 4; CLI 32; MCP 29 — all 0 failed. The verifier folded two changes into the fix commit: `cargo fmt --all` rewrapped the import block in `crates/rs_cam_core/tests/mutation_paths_invalidate_alike_p0.rs:115`, and the `project_entry_onto` doc at `crates/rs_cam_viz/src/ui/properties/mod.rs:3706` named the retained boundary-inherit field by its identifier, which the G-BOUNDARYINHERIT source sentry refuses anywhere under `src/ui`; the doc names it in prose now and states the correct reason the core field survives (project-file compatibility, not emitted geometry). `cargo clippy --workspace --all-targets --features rs_cam_core/heavy-tests -- -D warnings` and `cargo fmt --all -- --check` both clean, both re-run after that edit. No whole-core suite and no heavy gate: operator ruling 2026-09-11. |
| WP6 | egui scratch-copy pattern, twelve draw sites | DONE (`9b887707`; sentry `fd883efa`; per-file `4a49b434`, `8453653d`, `a86f2df1`, `97e4eaf1`) | Sentry `crates/rs_cam_viz/tests/egui_draw_sites_write_through_commands_wp6.rs`, three source scans. On the sentry commit it carries FIVE arms: `no_ui_source_names_a_session_hatch`, `the_hatch_scan_finds_the_declarations_it_is_built_around`, `the_app_event_enum_dropped_the_five_post_write_events`, `every_adopted_row_declares_a_gui_reach`, `every_adopted_row_is_constructed_in_a_draw_site`. The last fix commit REPLACES the locator self-test with two arms over a synthetic source — `the_reader_finds_a_hatch_on_a_code_line_and_not_in_a_comment` and `the_cut_off_passes_a_cfg_test_item_and_stops_at_a_cfg_test_module` — because WP7 deletes the four hatch declarations the old arm read, so the file ends at SIX arms. The sentry commit alone COMPILES — it spells the three new rows with their wire names — and reads 1 passed / 4 failed: the hatch scan names all **12 sites** (`properties/mod.rs:137, 287, 379, 413, 423, 1283, 1366, 1427, 1443, 1471, 1607, 1738`), the five events survive in `ui/mod.rs`, and `replace_tool` resolves to no registry row. Green on the last fix commit: 6 passed / 0 failed. **Eleven rows serve the twelve sites**: the three new ones (`ReplaceTool`, `ReplaceFixture`, `ReplaceKeepOut`, added in `4a49b434` with `drop_tool_results` / setup-result rules), plus `SetStockConfig`, the four setup rows (`SetSetupFace`, `SetSetupRotation`, `SetSetupDatum`, `SetSetupModels`), `SetMachine`, `SetMachineKinematics` and `ImportMachineSettings`; each flips to `gui: Reach::Reached` in the commit that adds its caller (§19 ruling 3). The five post-write events — `StockChanged`, `StockMaterialChanged`, `HeightPlanesChanged`, `MachineChanged`, `FixtureChanged` — lose their variants, emitters and handler arms (§14 ruling 3); the view half moves to `AppState::panel_side_effects` (GPU upload, pin-drill sync), discharged once per frame. `rg "\.(stock_mut\|machine_mut\|tools_mut\|find_setup_by_id_mut)\(" crates/rs_cam_viz/src/ui/` reports **0**. Behaviour changes, named: the workholding rigidity combo now drops EVERY toolpath result (it derates the feeds of every operation); a fixture or keep-out edit now drops the setup's results (a clamp could move under a cached holder-clearance verdict); `SetSetupDatum` drops nothing, by design. Invalidation is one frame late on a drag — a `DragValue` applies on release or lost focus (§19 ruling 8) — and every mid-drag reader inside the panel reads the scratch, so the aggressiveness label follows the handle. Suites: viz `--test egui_draw_sites_write_through_commands_wp6` 6, `--test command_registry_surfaces` 3, `--test mcp_wire_surface_pin` 2 (**the WP2a snapshot did not move**: `git diff c46a5072 HEAD -- crates/rs_cam_viz/tests/snapshots/` is empty), `--test mcp_core_arm_describes_every_row` 4, `--test freshness_surfaces_g_freshrender` 6, `--test model_refresh_invalidates_results_g_rescalestale` 4; core `--test command_registry_completeness` 10, `--test mcp_mutation_rows_reach_core` 7, `--test mutation_paths_invalidate_alike_p0` 8; core `--lib session::` 156; viz 640 over 42 binaries (was 634, plus the new sentry's 6); CLI 32; MCP 29 — all 0 failed. The verifier folded two changes into the last fix commit: `cargo fmt --all` moved five WP6 files, and one test fix at `crates/rs_cam_viz/src/controller/tests.rs:4313` — the fixture's stock carries `auto_from_model` and the fixture holds a model, so `apply_stock_draft` re-sized the draft before the compare, the typed `x` arrived back at the stored record, nothing was applied and the toolpath read `Current`; the draft clears the checkbox first, as the operator does, and the production code is unchanged. The sentry's own format move was folded into the sentry commit. NOT PRESENT: plan §4 WP6 asks the sentry for a frame-time measurement on a project with cached results (`PLAN.md` rule 5); the writer's sentry is three source scans and carries none. `cargo clippy --workspace --all-targets --features rs_cam_core/heavy-tests -- -D warnings` and `cargo fmt --all -- --check` both clean, both re-run after the test fix. No whole-core suite and no heavy gate: operator ruling 2026-09-11. |
| WP6b | The 27 non-egui viz sites and the 9 CLI sites | DONE (`2cfc2b9a`; sentry `b84f2601`; per-group `a9e752b9`, `ffa633f7`, `31d8924f`) | Sentries `crates/rs_cam_viz/tests/non_egui_sites_write_through_commands_wp6b.rs` (7 arms: three source scans plus their locator arms) and `crates/rs_cam_core/tests/adopt_model_geometry_invalidates_bound_toolpaths.rs` (4 arms). **Both were red on the sentry commit, in the two different ways**: the viz sentry COMPILED and four functions failed (`no_production_site_names_a_session_hatch`, `the_core_session_declares_no_wizard_hatch`, `every_cli_adopted_row_declares_a_cli_reach`, `every_cli_reached_command_row_is_constructed_in_the_cli` — 3 passed / 4 failed); the core sentry did NOT COMPILE (`E0432` on `AdoptModelGeometryArgs`, four `E0599` on `Command::AdoptModelGeometry`, 5 errors). **34 sites** lose their hatch: `controller/io.rs` 7, `controller/events/{mod,model,undo}.rs` 7, the wizard group 13 (`app/input.rs`, `app/export.rs`, `app/mcp.rs`), the CLI 7 plus 2 comment lines. **Two new core rows**: `AdoptModelGeometry` (one door for rescale / reload / relink — it adopts the geometry AND drops the results bound to that model in one mutation, so the two halves cannot drift as they did in G-RELOADTARGETS and G-RESCALESTALE) and `SetSetupPauseMessage` (`Effects::stale` empty by design, on the `SetSetupDatum` precedent). **Seven `Surfaces` flips** with their callers (§19 ruling 3): `SetPostConfig` and `SetToolpathDebugOptions` to `gui: Reached`; `SetStockConfig`, `SetPostConfig`, `SetMachineKinematics`, `ReplaceToolpathConfig` and `SetToolpathEnabled` to `cli: Reached`. **`WizardState` and `OutputLayout` moved to viz** (`crates/rs_cam_viz/src/state/wizard.rs`, on `GuiState.wizard` — no export signature moves); `ProjectSession::wizard` / `wizard_mut` / the field and the `project_file.rs` reset are deleted, so **the hatch count is now nine**, not ten. **THREE BEHAVIOUR CHANGES, each G-FRESHSTATE-correct**: the automatic stock fit on import / rescale now drops EVERY result; the two-sided pin placement now drops every result; `SetMachineKinematics` clears `machine_ref` and `ReplaceToolpathConfig` drops the chain when `generation_inputs_signature` moved. Plus `build_session_from_legacy_job` raises the model and tool id counters above every id it loads. **DoD grep** (`stock_mut|machine_mut|tools_mut|models_mut|post_mut|setups_mut|find_setup_by_id_mut|find_toolpath_config_by_id_mut|toolpath_configs_mut` over `crates/rs_cam_viz/src` and `crates/rs_cam_cli/src`): **36 hits at the base `d5d59482`, 10 after, 0 in the CLI** (the same nine-hatch pattern on both sides; with the deleted `wizard_mut` added to the pattern the base reads 48). Nine of the ten sit in test modules (`controller/tests.rs:2475,3988,4444`; `ui/properties/operations/mod.rs:2489,2537,2566,2582,2602,2646`, all below the `#[cfg(test)]` at :2293). The tenth, `controller/events/compute.rs:1265`, is the MCP generate-all debug-capture write that §15 gave WP10 and WP11b relocated out of `app/mcp.rs`; the sentry excludes that path by design. §4's "the residue must be exactly WP6's twelve" is stale — WP6 landed (`9b887707`) and cleared its own twelve. Suites: viz **657 passed / 3 failed** over 44 binaries; the three are WP11b's known reds (`controller::tests::{a_disabled_rest_op_is_not_generated_blocked_or_failed, generate_all_drives_a_three_deep_rest_chain_to_fixpoint_in_one_call, the_fixpoint_loop_terminates_on_a_genuinely_failing_op}`, N12 item 10) and NOT WP6b's. `gen_parity_p0_tests` is measured green on its own (`--lib gen_parity`, 2 passed), which re-checks §19 ruling 7. CLI 32; MCP 29; core `--lib session::` **154** (was 156: the two `session::wizard` lib tests moved to viz with the struct). The WP2a wire snapshot did not move (`git diff d5d59482 HEAD -- crates/rs_cam_viz/tests/snapshots/` is empty). `cargo fmt --all -- --check` and `cargo clippy --workspace --all-targets --features rs_cam_core/heavy-tests -- -D warnings` both clean. No heavy gate and no whole-core suite: the operator ruling of 2026-09-11 forbids them, and WP6b is not a phase end. **Verifier changes**: the sentry did not compile as the writer left it — an unescaped `{` in the assert message at line 313 is a format placeholder — so `{{` replaces it and the sentry commit re-hashed `d25e6d10` to `b84f2601`; `cargo fmt --all` then moved one tuple in `controller/events/mod.rs:971`, folded into the last fix commit. |
| WP7 | The eleven hatches go `pub(crate)` (final count nine per plan §20; three stay `pub(crate)`, six are deleted) | DONE (`7dff635b`; sentry `72e912bf`) | Sentry `crates/rs_cam_core/tests/hatches_are_crate_private_wp7.rs`, three arms: arm 1 (each of the nine names is `pub(crate) fn` or absent under `src/session/`, and no `pub fn *_mut` stands there), arm 2 (no line outside `crates/rs_cam_core/src` names `.<hatch>(`; the walk covers `tests/`, `benches/`, viz `src`+`tests`, CLI `src`+`examples`), arm 3 (`insert_result` stays crate-private, `wizard_mut` stays deleted). Red on the sentry commit: arms 1 and 2 FAILED — 9 public declarations, 17 lines naming a hatch. **DoD greps.** The §7 WP7 grep over `crates/rs_cam_viz/src crates/rs_cam_cli/src` reads **0**. The widened grep over `crates/` minus `rs_cam_core/src` reads 36 raw, all of them scanner DATA in `egui_draw_sites_write_through_commands_wp6.rs` / `non_egui_sites_write_through_commands_wp6b.rs` plus one `//!` line in `feeds_apply_drops_result_n13.rs`; with those three files and comment lines excluded it reads **0**. `rg "^\s*pub fn (<the eleven>)" crates/rs_cam_core/src` reads **0**, and `rg "^\s*pub fn [a-z_0-9]*_mut" crates/rs_cam_core/src/session/mod.rs` reads **0**. **Containment proof** (§7, the only compile-time check): `cargo build -p rs_cam_viz` → `Finished `dev` profile [unoptimized + debuginfo] target(s) in 24.50s`, after compiling `rs_cam_core`, `rs_cam_mcp` and `rs_cam_viz`. **Hatch fates.** DELETED, no caller anywhere: `stock_mut`, `machine_mut`, `tools_mut`, `models_mut`, `post_mut`, `find_setup_by_id_mut`. Kept `pub(crate)`: `setups_mut` (only caller is `session/save.rs`'s `#[cfg(test)]` module, so `#[cfg_attr(not(test), allow(dead_code))]`), `find_toolpath_config_by_id_mut` (`session/mutation.rs:702`, production), `toolpath_configs_mut` (`compute/validate.rs:153`, production). No `#[allow(dead_code)]` remains on a hatch. **Migrations.** One production site, `mcp_start_generate_all` → `Command::SetToolpathDebugOptions` per index; six `ui/properties/operations/mod.rs` fixtures → `ProjectSessionBuilder`; `controller/tests.rs` two sites → `Command::ReplaceToolpathConfig` and one reproduction deleted (§20 ruling 3); `wizard_e2e.rs` → `Command::SetSetupPauseMessage`; `apply_suggest_save.rs` → `Command::ReplaceToolpathConfig`; four core tests → `ReplaceToolpathConfig` / `AdoptModelGeometry`; the P0 arm 3 reproduction moved into `session/mutation.rs`'s `#[cfg(test)]` module. **Suites.** `hatches_are_crate_private_wp7` 3; `mutation_paths_invalidate_alike_p0` 8; `arc_fit_disposition_a5` 3 (23.76 s); `arcfit_gate_population_d4` 4 (+1 ignored); `drill_picks_resolve_to_targets_g_drillpickstale` 6; `session_builder_preserves_ids` 7; `command_registry_completeness` 10; core `--lib session::` 156 (154 + the moved arm's 2); viz 660 = 661 − 1 deleted reproduction, with `wizard_e2e` 12, `egui_draw_sites_write_through_commands_wp6` 6, `non_egui_sites_write_through_commands_wp6b` 7 and `command_surface_completeness` 14 all green and `crates/rs_cam_viz/tests/snapshots/` byte-unchanged against `20d615b7`; CLI 32; MCP 29. `cargo fmt --all -- --check` and `cargo clippy --workspace --all-targets --features rs_cam_core/heavy-tests -- -D warnings` both clean. No heavy gate, no whole-core dev loop (operator ruling 2026-09-11). |
| WP7a | `ProjectSessionBuilder` + the 63 SETUP test sites (plan §20) | DONE (`084b9b50`; sentry `76dbfb41`) | Sentry `crates/rs_cam_core/tests/session_builder_preserves_ids.rs`, seven arms: `builder_keeps_tool_ids_and_order`, `builder_keeps_model_ids_and_order`, `builder_keeps_toolpath_order_and_bindings`, `builder_replaces_the_seeded_setup_then_appends`, `builder_leaves_the_stock_alone`, `builder_stores_the_result_and_no_revision`, `builder_raises_the_id_counters_above_every_supplied_id`. The sentry commit alone was compile-fail red, one error: `E0432: no ProjectSessionBuilder in session`. Green on the fix: 7 passed / 0 failed. 51 sites migrated of the 63 the scout classified SETUP — 27 in `crates/rs_cam_viz/tests` (16 files), 18 in the viz in-crate test modules (7 files), 6 in `crates/rs_cam_core/tests` (5 files); the verifier counted the hatch half of that as 38 `*_mut()` call lines removed (22 viz tests / 13 viz in-crate / 3 core tests). Twelve SETUP sites stay for WP7: eleven `wizard_mut` arranges in `wizard_e2e.rs` and one boundary arrange in viz `controller/tests.rs`. Six `models_mut().push` sites in `ui/properties/operations/mod.rs` `#[cfg(test)]` are SETUP-shaped and remain for WP7. Suites: core `--test session_builder_preserves_ids` 7; the five migrated core binaries in one run — `claims_reference_cascade_am6` 8, `export_datum_setup_frame` 5, `lateral_setup_end_to_end` 4, `setup_datum_round_trip_p2` 5, `simulation_issue_channel_m1` 2 passed / 1 ignored (`rivers_b4_probe_project_curve_on_remaining_stock`, expensive); viz 625 (40 binaries); CLI 32; MCP 29 — all 0 failed. The verifier added `let _ =` at eight sites in three test files, because the session setters return `#[must_use]` `Effects` and the raw field writes they replaced returned nothing: viz `controller/tests.rs:2581,2642,2757,2927,3118`, core `tests/simulation_issue_channel_m1.rs:450`, core `tests/claims_reference_cascade_am6.rs:463,647`. `cargo clippy --workspace --all-targets --features rs_cam_core/heavy-tests -- -D warnings` and `cargo fmt --all -- --check` both clean. No whole-core suite and no heavy gate: operator ruling 2026-09-11. |
| WP8 | N14 and N6: undo, optimizer, drill picks | DONE (`e079e192`; sentry `5c32ced7`) | Sentry `crates/rs_cam_core/tests/restore_snapshot_invalidates_like_the_setter_n14.rs`, four arms: `the_restore_drops_the_set_the_setter_drops_n14`, `a_byte_identical_restore_still_drops_the_chain_f2_5`, `the_payload_writes_the_feeds_provenance`, `a_drill_pick_drops_the_chain_n6`. The viz half is one in-crate test, `an_undo_stales_the_downstream_row_and_restores_the_provenance_n14` in `crates/rs_cam_viz/src/controller/tests.rs`. The sentry commit alone was compile-fail red on three targets, two errors each: `--test restore_snapshot_invalidates_like_the_setter_n14` and `--test mutation_paths_invalidate_alike_p0` read `E0432` on `RestoreToolpathSnapshotArgs` and `E0599` on `Command::RestoreToolpathSnapshot`; `rs_cam_viz --lib` read `E0559` twice on `UndoAction::ToolpathParamChange`'s absent `old_feeds_provenance` and `new_feeds_provenance`. P0 flip: arm 2 of `mutation_paths_invalidate_alike_p0.rs` moved onto the new command (the old door is `pub(crate)` now, so the arm could not flip in place), `n14_undo_and_the_setter_invalidate_alike` and `n6_a_drill_pick_invalidates_the_chain` both read `{0, 1}`, and the pin-hole sibling stays green unchanged. The optimizer keeps `pub(crate) apply_toolpath_param_snapshot_narrow` on its two call sites; `the_narrow_path_leaves_the_neighbour_result_cached` in `session/mutation.rs` pins that the neighbour's cached result survives it. N14 and N6 closed; `set_feeds_provenance` kept pub with zero production callers. Suites: core `--test restore_snapshot_invalidates_like_the_setter_n14` 4, `--test mutation_paths_invalidate_alike_p0` 8, `--test command_registry_completeness` 10, `--test adopt_result_rejects_stale_completion` 5, `--test drill_picks_resolve_to_targets_g_drillpickstale` 6, `--test toolpath_rebind_g_mcprebind` 9; core `--lib session::` 155; core `--lib tool_load::optimize::` 223; viz 626 (40 binaries, was 625); CLI 32; MCP 29 — all 0 failed. Both cherry-picks auto-merged; WP7a did not touch `mutation_paths_invalidate_alike_p0.rs`. The verifier changed nothing beyond the writer's two commits. `cargo clippy --workspace --all-targets --features rs_cam_core/heavy-tests -- -D warnings` and `cargo fmt --all -- --check` both clean. No whole-core suite and no heavy gate: operator ruling 2026-09-11. |
| WP9 | `Query` first row: cycle time | DONE (`5173c923`; sentry `720626b2`) | Sentry `crates/rs_cam_core/tests/query_cycle_time_one_answer.rs`, six arms: `machine_model_basis_matches_the_oracle`, `simulated_no_accel_basis_matches_the_oracle`, `cutting_only_basis_matches_the_oracle`, `no_evidence_matches_the_oracle_none`, `a_query_for_a_missing_toolpath_refuses`, `the_registry_carries_the_cycle_time_row_as_a_query`. The file copies the pre-fix `readiness::toolpath_cycle_time` in as a frozen oracle and asserts the `Query` answers the same `CycleTime`. `command_registry_completeness` gains two arms, `a_constructed_query_answers_its_identifier` and `query_answer_has_one_variant_per_query_row`. Both binaries were compile-fail red on the sentry commit alone, 5 errors each: `Query`, `QueryAnswer`, `ToolpathCycleTimeArgs`, `ToolpathCycleTimeAnswer`, core `CycleTime` / `CycleTimeBasis` and `ProjectSession::query` did not exist. The payload carries the GUI's cut trace (the GUI does not use the session simulation slot), so the `MachineModel` and `SimulatedNoAccel` bases survive on a real project. The `Query` row declares `mcp: Skip` and the wire snapshot did not move. Suites: core `--test query_cycle_time_one_answer` 6, `--test command_registry_completeness` 10 (was 8), `--test mutation_paths_invalidate_alike_p0` 8, `--test adopt_result_rejects_stale_completion` 5, `--test drill_runtime_survives_retime_n2` 5 (the plan's P0 flip, green byte-for-byte); core `--lib session::` 154; viz 625, of which `cycle_time_basis_g_timeest` 12, `command_registry_surfaces` 3 and `mcp_wire_surface_pin` 2; CLI 32; MCP 29 — all 0 failed. The verifier changed nothing beyond the writer's two commits. `cargo clippy --workspace --all-targets --features rs_cam_core/heavy-tests -- -D warnings` and `cargo fmt --all -- --check` both clean. No whole-core suite and no heavy gate: operator ruling 2026-09-11. |
| WP10 | `Job` first row: generate one toolpath | DONE (`3ff3cffa`; sentry `b952e6ee`) | Sentry `crates/rs_cam_core/tests/job_three_steps_equal_generate_toolpath.rs`, four arms plus a control: (a) `the_three_job_steps_generate_what_generate_toolpath_generates` — the three steps cache the move count, the FNV-1a fingerprint over `Debug` of the moves, the span count and `spans_valid` that `generate_toolpath` caches, with a non-vacuity guard on an empty move list; (b) `an_edit_after_start_refuses_the_handles_result` — an edit between step (i) and step (iii) moves the revision, so the adopt refuses with `SessionError::StaleCompletion` and inserts nothing; (c) `execute_job_holds_no_session` — `execute_job` coerces to a plain function pointer, and the arm drops the session and generates from the handle alone; (d) `the_generate_toolpath_row_is_a_job` — the row declares `CommandKind::Job` and the wire name the MCP tool carries; control `generate_toolpath_repeats_its_own_answer`. The sentry commit alone was compile-fail red, 7 errors: `E0432` on `GenerateToolpathArgs`, `GenerateToolpathHandle`, `Job`, `JobHandle` and `execute_job`, `E0599` three times on `ProjectSession::start` and three times on `CommandId::GenerateToolpath`. Green on the fix: 5 passed / 0 failed. **Core-only, per plan §16 addendum**: `start` / `execute_job` / `AdoptResult` land, `generate_toolpath` runs the three steps inline, and the viz worker and drain switch is WP11b, which also closes N12. Two deliberate deviations from §16's ruling 2 stand: `start` takes the cancel flag (the resolver walks a full-grid tier map for a `PlannedTierRegions` boundary, so a flag made inside `start` would make Cancel a lie), and `start` does NOT bump the revision — it calls `self.results.remove`, the call the old head made, and `drop_result` stays the door that bumps. The CLI needed no change: all six sites call `generate_toolpath`, whose signature did not move. Suites: core `--test job_three_steps_equal_generate_toolpath` 5, `--test command_registry_completeness` 10 (`DECLARED_ROWS` derives from the registry list, so no count needed a bump), `--test adopt_result_rejects_stale_completion` 5, `--test mutation_paths_invalidate_alike_p0` 8, `--test set_param_refuses_absent_field_n5` 5; the generation-heavy trio `--test disconnected_finish_retract_structure_p0` 1, `--test drill_runtime_survives_retime_n2` 5, `--test export_honors_coolant_p0d1` 2; core `--lib session::` 156; viz 628 over 40 binaries; CLI 32; MCP 29 — all 0 failed. Both cherry-picks needed a resolution, because the writer based on `a012d10f` and master since gained WP5. `session/command.rs`: the module doc keeps both paragraphs, the `use super::` list is the union (`GenerateToolpathHandle` and `ToolpathConfig` both), the row list keeps WP5's `ReplaceToolpathConfig` row and puts the `Job` row last, and both payload structs survive (`ReplaceToolpathConfigArgs` and `GenerateToolpathArgs`). `session/mod.rs`: both re-export lists are the union; WP10's `pub use compute::{..}` block is a superset of master's, so nothing was dropped. The verifier changed nothing beyond the writer's two commits: `cargo fmt --all -- --check` was clean on both, so neither of the writer's two flagged moves was needed, and clippy reported zero warnings on the first run. `cargo clippy --workspace --all-targets --features rs_cam_core/heavy-tests -- -D warnings` and `cargo fmt --all -- --check` both clean. No whole-core suite and no heavy gate: operator ruling 2026-09-11. |
| WP11a | Publish `ResolvedGenInputs` with private fields | DONE (`a9ef73d6`; sentry `181f5c13`) | Sentry `crates/rs_cam_core/tests/resolved_gen_inputs_has_one_producer.rs`, three arms: `the_bundle_is_public_and_its_fields_are_private`, `one_public_function_produces_the_bundle`, `the_bundle_is_nameable_from_another_crate`. The sentry commit alone was compile-fail red (`E0412: cannot find type ResolvedGenInputs in module rs_cam_core::session`, two sites); the two scan arms are red by inspection, because the build stops before a scan runs. Green on the fix: 3 passed / 0 failed. Suites: core `--test command_registry_completeness` 8, core `--test mutation_paths_invalidate_alike_p0` 8, viz 623, CLI 32, MCP 29 — all 0 failed. `cargo clippy --workspace --all-targets --features rs_cam_core/heavy-tests -- -D warnings` and `cargo fmt --all -- --check` both clean. No whole-core suite and no heavy gate: operator ruling 2026-09-11. |
| WP11b | Narrow the executor; close N12 | DONE (WP11b fix `4b53576b` + fix `276e5c13`; sentries `df963567`, `bab065ce`) | Sentry `crates/rs_cam_core/tests/gen_inputs_one_assembly_n12.rs`, five arms: `the_job_route_modulates_feeds_from_the_feed_optimisation_stock` (the N12 item-3 behavioural arm), `both_core_routes_report_one_feed_optimisation_answer`, `no_viz_source_names_the_loose_executor`, `one_core_function_returns_the_resolved_bundle`, `the_narrowed_executor_reads_the_resolved_bundle`. The `gen_parity_p0_tests` item-3 arm is flipped to an identity claim. Both kinds of red stood at the sentry commit: the core sentry and the WP10 sentry FAILED TO COMPILE (`E0432` on `GenObserver` and `execute_generation`, `E0061` on `execute_job`), and the flipped viz arm failed as an assertion (28 feed mismatches, `feed_rate_count` gui 5 vs session 2). Shapes as landed: `execute_generation(inputs, ctx, observer, cancel)` beside `execute_job`; `GenObserver` a core struct of optional sinks, with the debug-options gate READ off the handle and the artifact path left with the caller; `ResolvedGenInputs::spatial_index` an `Arc<LazyIndex>` forced on the worker thread; a FRESH `Arc<AtomicBool>` per submit carried in `VizExtras`; `ComputeRequest { handle, viz }`; `ComputeResult::revision` so the drain adopts at the revision the submit read. GREEN: core `--test gen_inputs_one_assembly_n12` 5, `--test job_three_steps_equal_generate_toolpath` 5, `--test resolved_gen_inputs_has_one_producer` 3, `--test command_registry_completeness` 10, `--test adopt_result_rejects_stale_completion` 5, `--test boundary_clip_invalidates_spans` 2, `--test skipped_boundary_offset_f8` 4; core `--lib session::` 156; CLI 32; MCP 29; the viz wire snapshot is unchanged. `cargo clippy --workspace --all-targets --features rs_cam_core/heavy-tests -- -D warnings` and `cargo fmt --all -- --check` both clean. **N12 items 1-10 are closed**, item 10 through the `AdoptSimulation` row (the §22 addendum ruling, 2026-09-11 night): the GUI drain adopts its simulation into the session after the modulation post-pass, so `ProjectSession::start` reads the prior stock a `FromRemainingStock` operation needs. Fix `276e5c13`, sentries `crates/rs_cam_core/tests/adopt_simulation_stores_prior_stocks.rs` (2 arms: the row stores the simulation and `start` then succeeds; a control arm asserts the refusal names the missing snapshot) and the in-crate `controller::tests::a_gui_simulation_reaches_the_session_so_start_sees_the_prior_stock_n12_item10`. **Both kinds of red stood at the fix sentry commit `bab065ce`**: the core sentry did NOT COMPILE (`E0432` on `AdoptSimulationArgs`, four `E0599` on `Command::AdoptSimulation` / `CommandId::AdoptSimulation`, 5 errors) and the viz arm failed on an ASSERTION ("the session and the viewport must hold ONE snapshot; the session holds none", `controller/tests.rs:3103`). **The three tests WP11b left red are GREEN**: `controller::tests::{a_disabled_rest_op_is_not_generated_blocked_or_failed, generate_all_drives_a_three_deep_rest_chain_to_fixpoint_in_one_call, the_fixpoint_loop_terminates_on_a_genuinely_failing_op}`. Suites on the fix: core `--test adopt_simulation_stores_prior_stocks` 2, `--test command_registry_completeness` 10, `--test gen_inputs_one_assembly_n12` 5, `--test job_three_steps_equal_generate_toolpath` 5; core `--lib session::` **154**; viz `--lib` **372**; viz **661 passed / 0 failed** over 44 binaries (was 657 passed / 3 failed: the three go green and the new arm arrives); CLI 32; MCP 29 — all 0 failed. The WP2a wire snapshot did not move (`git diff 06a7332d HEAD -- crates/rs_cam_viz/tests/snapshots/` is empty), which the row's `mcp: Reach::Skip` predicts. `cargo clippy --workspace --all-targets --features rs_cam_core/heavy-tests -- -D warnings` and `cargo fmt --all -- --check` both clean. **Verifier changes on the fix**: the rebase onto master left one conflict, `crates/rs_cam_core/src/session/mod.rs`, and the re-export list is the union of WP6b's `AdoptModelGeometryArgs` / `SetSetupPauseMessageArgs` and the fix's `AdoptSimulationArgs`; `cargo fmt --all` then rewrapped that list and rewrapped the sentry file, folded into the fix and the sentry commit. Clippy needed no change. The row list in `session/command.rs`, the two exhaustive `CommandId` arms in `app/mcp/commands.rs` and the `app/mcp.rs` comments auto-merged and were read line by line; `AdoptSimulation` is the last row. The historical reading follows. **WAS NOT GREEN**: viz 648 passed / 3 failed — `controller::tests::{a_disabled_rest_op_is_not_generated_blocked_or_failed, generate_all_drives_a_three_deep_rest_chain_to_fixpoint_in_one_call, the_fixpoint_loop_terminates_on_a_genuinely_failing_op}`. One cause, and it is a regression, not a stale test: `start` reads the rest snapshot from `self.simulation.prior_stocks` (`session/compute.rs:2520,2572`), the GUI adopts its simulation into the VIZ state (`controller/events/compute.rs:665,703`) and never writes `session.simulation` (`controller/tests.rs:4330` asserts that), so the viz existence check at `controller/events/compute.rs:274` passes and `start` then refuses every `FromRemainingStock` operation in the GUI process. This is a TENTH N12-class divergence the §22 scout did not list: two simulation states. The item-3 sentries miss it because neither fixture carries a rest operation. The repair is a ruling on where the GUI simulation state lives (a `ProjectSession` setter, `start` taking a prior-stock source, or the GUI simulation moving into core), so the verifier did not make it. No whole-core suite and no heavy gate: operator ruling 2026-09-11. |
| WP12 | Delete `ComputeRequest`'s mirrored fields (rescoped by §23: the loose executor goes crate-private) | DONE (`77627c33`; sentry `89f81cb4`) | Sentry `crates/rs_cam_core/tests/loose_executor_is_crate_private_wp12.rs`, three arms. Arms (a) and (b) were assertion-red on the sentry commit (the file compiles); arm (c) was already green, because WP11b closed the mirror. The three generation entries in `compute/execute.rs` now read `pub(crate) fn`: `execute_operation` (`:3358`), `execute_operation_annotated` (`:3406`), `execute_operation_annotated_with_regions` (`:3498`). **20 tests moved in-crate** from `crates/rs_cam_core/tests/` to `crates/rs_cam_core/src/compute/execute/`, as four `#[cfg(test)] mod` declarations, every assertion byte-identical: `pinned_bottom_z_reaches_motion_g_bottompin` (4), `project_curve_chaining` (9), `unified_finish_ring_collapse_g_unifiedcrash` (3), `unified_finish_semantic_regions` (4). The four old integration files are gone and `crates/rs_cam_core/Cargo.toml` names none of them in a `[[test]]` entry. The mirror comment in `crates/rs_cam_viz/src/compute/worker/gen_parity_p0_tests.rs` is rewritten. **§7 greps as §23 ruling 4 corrects them:** `rg "pub struct ResolvedHeights" crates/rs_cam_core/src` → 2 (`compute/config.rs:1656`, `diagnostics/adapters/from_static_checks.rs:58`); `rg -- "-> Result<ResolvedGenInputs" crates/rs_cam_core/src` → 1 (`session/compute.rs:1993`); `rg "session/compute.rs" crates/rs_cam_viz/src/compute` with comment lines excluded → 0 (0 occurrences of any kind); no `#[allow]` on a `pub` generation entry → 0 `pub fn execute_operation` in `execute.rs` (`too_many_arguments` there reads 5, not §23's stated floor of 3: the three `pub(crate)` entries plus `attach_generic_rest_analysis` and the `pub` `apply_dressups`, neither a generation entry). **Suites:** the four named `--test` binaries 16 passed / 0 failed (`loose_executor_is_crate_private_wp12` 3, `gen_inputs_one_assembly_n12` 5, `job_three_steps_equal_generate_toolpath` 5, `resolved_gen_inputs_has_one_producer` 3); core `--lib compute::execute::` 41 passed / 0 failed (the 20 moved plus 21 pre-existing, all 20 names present); core `--lib session::` 154 passed / 0 failed; viz 661; CLI 32; MCP 29. `cargo fmt --all -- --check` and `cargo clippy --workspace --all-targets --features rs_cam_core/heavy-tests -- -D warnings` both clean. No heavy gate and no whole-core dev loop, per the operator's 2026-09-11 ruling. **Verifier change beyond the writer's commits:** the demotion showed that the 14-argument `execute_operation` has NO production caller in the workspace — six callers in `execute.rs`'s own `mod tests`, one in the moved `pinned_bottom_z_reaches_motion_g_bottompin` module — so clippy failed with `function `execute_operation` is never used`. The entry carries `#[cfg_attr(not(test), allow(dead_code))]` and says why. A residual for the §5 list, beside the strategy advisor's in-crate loose call. |
| WP13 | `UiCommand` split and the cross-surface sentry | DONE (`35ae77ad`; sentry `50f975e2`; classification draft `WP13_CLASSIFICATION_DRAFT.md`) | Sentry `crates/rs_cam_viz/tests/command_surface_completeness.rs`, **14 arms** over the five properties of §21 ruling 9. P1 — `every_gui_reached_core_row_is_constructed_in_the_view`, `no_gui_skipped_core_row_is_constructed_in_the_view`, `the_p1_locator_finds_a_known_construction`; P2 — `every_view_skip_names_its_reason`, `no_view_row_reaches_the_batch_command_line`, `every_view_row_is_reached_by_some_surface`, `the_view_registry_declares_only_view_kinds`; P3 — `no_view_command_handler_writes_the_session`, `every_view_command_row_has_a_handler_or_a_tool`, `the_view_read_door_holds_the_session_by_shared_reference`, `every_view_read_row_has_an_arm_in_the_read_door`; P4 — `every_mcp_reached_view_row_declares_a_tool`, `no_mcp_skipped_view_row_claims_a_wire_name`; P5 — `no_wire_name_appears_in_both_registries`. Two NAMED exemptions, each with the WP that removes it: `P1_EXEMPT = [GenerateToolpath]` (WP11b switches the worker and the drain, so the GUI cannot construct the `Job` row yet) (`P1_EXEMPT` removed by WP20), and `P3_NAMED_EXEMPTIONS = [CloseOptimizeModal, CloseOptimizeProject]` (WP10's Job shape; MEASURED to pass unaided — the `std::mem::replace` sits in `open_optimize_modal` and the restore in the compute drain, so the names stay as a record, not as a suppression). The sentry commit alone was compile-fail red, **13 errors**: `E0432` on the unresolved import `rs_cam_viz::ui_command`, `E0599` on the absent `CommandKind::UiQuery`, and repeated `E0282` where the absent `UiCommandId` left `id` uninferable. Green on the fix: 14 passed / 0 failed. **The view registry carries 70 rows — 62 `UiCommand` and 8 `UiQuery`** (`crates/rs_cam_viz/src/ui_command.rs`, `for_each_ui_command!`, core's `CommandKind` / `Reach` / `Surfaces` reused, union type `SurfaceId { Core(CommandId), Ui(UiCommandId) }`). **56 `AppEvent` variants deleted** of 133 (55 became view rows, plus `RemoveSetup`), replaced by one `AppEvent::Ui(UiCommand)`; **18 `McpRequestKind` variants deleted** of 76, replaced by `McpRequestKind::Ui(UiCommand)` and `McpRequestKind::UiQuery(UiQuery)`. Three rows carry both a GUI control and an MCP tool (`SimJumpToMove`, `SimJumpToStart`, `SimJumpToEnd`), so 73 deleted variants make 70 rows. **`RemoveSetup` is deleted** (§21 ruling 8: zero emitters, no control, no tool, no row); two of its four test sites measured only the handler and go with it, the third calls `session.remove_setup` directly. **`GetOperationSchema` is a core `Query` row** (§21 ruling 3) and `CommandKind` gains its fifth variant `UiQuery` (ruling 2). Two ruling deviations, each measured and stated in the commit: the read door hangs on `RsCamApp::ui_query(&self, ..)` and not `AppState` (two of the eight reads need the toast stack and the triage builder), and the sentry asserts `cli: Skip` + reached-by-some-surface instead of ruling 6's `gui: Reached`, because seven view commands are MCP-only. Suites: viz `--test command_surface_completeness` 14, `--test command_registry_surfaces` 3, `--test mcp_wire_surface_pin` 2 (**the WP2a snapshot did not move**: `git diff 8eee4c3f HEAD -- crates/rs_cam_viz/tests/snapshots/` is empty), `--test egui_draw_sites_write_through_commands_wp6` 6, `--test mcp_core_arm_describes_every_row` 4, `--test mcp_escape_hatches` 15, `--test mcp_rebind_surface_g_mcprebind` 6, `--test overlays_registry` 27; core `--test command_registry_completeness` 10, `--test query_cycle_time_one_answer` 6, `--test mcp_mutation_rows_reach_core` 7; core `--lib session::` 156; viz 652 over 43 binaries (was 640: the two `RemoveSetup` tests went, the sentry's 14 arrived); CLI 32; MCP 29 — all 0 failed. The rebase onto master (WP6) left three conflicts, and both sides are kept in each: `session/command.rs` keeps WP6's `ReplaceTool` / `ReplaceFixture` / `ReplaceKeepOut` rows AND the `GetOperationSchema` Query row (placed after them, which is after the Query row `ToolpathCycleTime`; the wire snapshot sorts by name, so row order cannot move it); `session/mod.rs`'s re-export list is the union; `ui/properties/setup.rs` keeps WP6's `edit.commit()` and WP13's `AppEvent::Ui(UiCommand::PreviewOrientation(face))`. The four `ui/properties/` emitter rewrites and the two `use` lines auto-merged and were checked line by line. The verifier folded five changes into the fix commit and one into the sentry commit. Into the fix: `app/mcp/commands.rs:1715,2504` — both exhaustive `match id` arms gain `CommandId::GetOperationSchema` beside the sibling Query row `ToolpathCycleTime`, because a Query row is not a wire mutation (`E0004`, twice); `app/mcp.rs:4307,4365` — `move_index: move_index` shortened (`clippy::redundant_field_names`); `ui/overlays/panel.rs:275` — a terminating semicolon (`clippy::semicolon_if_nothing_returned`); `ui_command.rs:781` — the continuation line of a backslash-continued literal moved to column 0, because `cargo fmt` removed six spaces from it on every pass and never converged; and `cargo fmt --all` over 20 WP13 files. Into the sentry: `command_surface_completeness.rs:250` — the P1 locator's negative pin moved from `SetStockConfig` to `SetStockSource`, because WP6 landed a GUI caller for `SetStockConfig` (`ui/properties/mod.rs:201`) and flipped its `gui` column to `Reached`, so the old pin asserted a falsehood; `SetStockSource` is the adjacent row, still `gui: Skip`, and the MCP wire is still its only constructor. `cargo clippy --workspace --all-targets --features rs_cam_core/heavy-tests -- -D warnings` and `cargo fmt --all -- --check` both clean. No whole-core suite and no heavy gate: operator ruling 2026-09-11. |
| WP14 | The three ruled `Job` rows: `OptimizeToolpath`, `RecommendClearingStrategy`, `PreviewTierMap` (§14 ruling 1; review M3) | DONE (WP14a 6e9a1326; WP14b 2abf78aa; sentries 5a43a365, 5f894b58) | Sentry `crates/rs_cam_core/tests/job_rows_recommend_and_preview_wp14a.rs`, five arms. Red on the sentry commit was a compile failure of the sentry binary: the registry declared neither row and neither `execute_*` existed (14 errors). `RecommendClearingStrategy` and `PreviewTierMap` are `CommandKind::Job` rows now. Each captures a handle — `RecommendClearingStrategyHandle` (the one `ResolvedGenInputs` plus an `AdvisorContext`) and `PreviewTierMapHandle` (the setup-frame mesh and its index as `Arc`s, the resolved ladder, the spec) — and `execute_recommend_clearing_strategy` / `execute_preview_tier_map` coerce to plain `fn` pointers, so step (ii) holds no session. There is no adopt step: both rows are reads. The GUI gains a fifth lane, `ComputeLane::Job`, with `JobRequest` and `ComputeMessage::Job`; `ComputeBackend::submit_job` is defaulted. No `timeout_s` on either MCP tool: §24 ruling 1 adds it only if the tools' param structs already carry it, and `IndexParam` and `PreviewTierMapParam` do not, so the poll shape stays WP14c. `git diff 520f16ab HEAD -- crates/rs_cam_viz/tests/snapshots/` is empty — the MCP wire snapshot does not move. Suites: `job_rows_recommend_and_preview_wp14a` 5, `job_three_steps_equal_generate_toolpath` 5, `gen_inputs_one_assembly_n12` 5, `command_registry_completeness` 10, `query_cycle_time_one_answer` 6; core `--lib session::` 156; core `--lib multitool` 9; core `--lib tool_load::` 352 passed / 2 failed — both `retarget_reconciliation_a8` arms, PRE-EXISTING, reproduced identically at `520f16ab` (`feedopt::optimize_feed_rates_inner` panics on `min > max, min = 6000.0, max = 3000.0`); viz 662 over 44 binaries; CLI 32; MCP 29. `cargo fmt --all -- --check` and `cargo clippy --workspace --all-targets --features rs_cam_core/heavy-tests -- -D warnings` both clean. No heavy gate: WP14a is not a phase end. **WP14b.** Sentries `crates/rs_cam_core/tests/optimize_toolpath_is_a_job_wp14b.rs` (five arms) and `crates/rs_cam_viz/tests/optimize_runs_on_the_job_lane_wp14b.rs` (two arms). Red on the sentry commit was a compile failure of the core sentry binary, six errors, one per missing name: `CommandId::OptimizeToolpath`, `Job::OptimizeToolpath`, `OptimizeToolpathArgs`, `OptimizeToolpathHandle`, `JobHandle::OptimizeToolpath`, `execute_optimize_toolpath` and `ProjectSession: Clone`. The viz sentry was assertion-red: its scan named `controller/events/mod.rs:659`, `controller/events/mod.rs:1365` and `controller/events/planner.rs:176`, and `OptimizeResult` still declared `pub session`. Arm (d) measures the residue on a generated and simulated pocket: the inline call drops the cached result, bumps the revision and clears the simulation; the job leaves all three. The whole core sentry binary runs in 2.63 s and arm (d) is 2.44 s of it, so no arm is `#[ignore]`. The session clone costs 0.071 ms on the P0 fixture (arm (e)) — four orders under §24 ruling 2's one-second point, so the handle keeps the simulation by value and copies the display mesh. Recorded: an `optimize_toolpath` issued after an edit that dropped the session's simulation now REFUSES at submit with `SessionError::SimulationRequired`, where it used to score against the view slot's older trace; and the full-screen Optimize placeholder is now a POLICY — one Optimize run at a time — rather than a necessity, because nothing lends the session out. The verifier folded two edits into the fix commit: `OptimizeToolpathArgs` was missing from `session/mod.rs`'s `pub use command::{ ... }` list, so neither the sentry nor `rs_cam_viz` compiled, and `cargo fmt --all` moved `session/mod.rs`, `controller/events/planner.rs` and the core sentry file. `git diff 68c6cbc9 HEAD -- crates/rs_cam_viz/tests/snapshots/` is empty — the MCP wire snapshot does not move. Suites, all green: core `optimize_toolpath_is_a_job_wp14b` 5, `job_rows_recommend_and_preview_wp14a` 5, `job_three_steps_equal_generate_toolpath` 5, `gen_inputs_one_assembly_n12` 5, `command_registry_completeness` 10, `adopt_simulation_stores_prior_stocks` 2, `query_cycle_time_one_answer` 6, `--lib tool_load::optimize::` 223, `--lib session::` 156, `--lib multitool` 9; viz `optimize_runs_on_the_job_lane_wp14b` 2, `command_surface_completeness` 14 (the `PreviewTierMap` row's `gui` column flips to `Reached`), `command_registry_surfaces` 3, `mcp_wire_surface_pin` 2, `mcp_core_arm_describes_every_row` 4, `mcp_escape_hatches` 15, `effects_are_stamped_wp19` 4, `--lib controller::tests` 101, `--lib planner` 16; whole viz crate 678 over 47 binaries; CLI 32; MCP 29. The two `retarget_reconciliation_a8` failures WP14a recorded are gone: `--lib tool_load::optimize::` reads 223 passed / 0 failed both at `68c6cbc9` and here. `cargo fmt --all -- --check` and `cargo clippy --workspace --all-targets --features rs_cam_core/heavy-tests -- -D warnings` both clean. No whole-core suite and no heavy gate: operator ruling 2026-09-11; debug builds only: §27 ruling 4. |
| WP15 | Public setters bypass `apply`: ~33 direct setter sites in viz and CLI; setters go `pub(crate)`; `set_face_selection` and `replace_setups_and_toolpaths` get rows (review M4) | DONE (WP15a `ad06c690`; WP15b `30dab0d0` over `63122bd7`, `29f17edc`, `f65ef131`, `a124d8a8`, `1501a69e`; sentries `a4c0abd7`, `e8450e09`) | Plan §25 ruling 1. The registry holds 71 rows, up from 47: 24 new rows, one per row-less public setter, each `apply` arm delegating to the setter so the setter keeps the invalidation rule. 48 external production setter sites now take `apply` (viz 39 in seven files, CLI 9 in three); 26 `Surfaces` flips to `Reached` (22 `gui`, 4 `cli`) in the same commit as the caller, and three `Skip` reasons restated as present facts. Sentries: `crates/rs_cam_core/tests/setters_have_rows_wp15a.rs` (2) — RED at the sentry commit on a runtime assertion naming the 24 row-less setters; and `crates/rs_cam_viz/tests/production_writes_go_through_apply_wp15a.rs` (6), which ships in the fix commit. I proved its red by injection: one direct `self.state.session.set_name(..)` call restored in `controller/events/model.rs` made it FAIL naming that line; the revert made it pass. Suites: core `command_registry_completeness` 10, `mcp_mutation_rows_reach_core` 7, `hatches_are_crate_private_wp7` 2, `--lib session::` 156; viz `command_surface_completeness` 14, `command_registry_surfaces` 3, `mcp_wire_surface_pin` 2, `mcp_core_arm_describes_every_row` 4, `non_egui_sites_write_through_commands_wp6b` 7, whole crate 668 / 0, `--lib undo` 11; CLI 32; MCP 29. The MCP wire snapshot does not move (all 24 rows are `mcp: Skip`). `cargo fmt --all -- --check` and `cargo clippy --workspace --all-targets --features rs_cam_core/heavy-tests -- -D warnings` both clean. NOT RUN: the full heavy gate and the whole-core dev loop (operator ruling 2026-09-11). **WP15b (2026-09-13).** The 59 `pub fn` setters on `ProjectSession` go `pub(crate) fn`; `rename_setup` was already crate-private, so the checked population is 60. Sentry `crates/rs_cam_core/tests/setters_are_crate_private_wp15b.rs`, three arms. **Both scanning arms were assertion-red at the sentry commit, never compile-red** — the setters are still `pub` there, so every crate builds. Arm 1 `every_session_setter_is_declared_crate_private` read `I read 59 public declaration(s)` and named each with its `path:line`. Arm 2 `no_crate_outside_core_names_a_session_setter` read `I read 610 call site(s) in 534 file(s)`, the 534 being the files walked. Arm 3 `neither_scan_is_vacuous` passed on the sentry commit, so neither scan reads an empty tree. Arm 2 then fell 610 → 123 (fix A-core) → 122 (the WP14b gap site, migrated by the verifier) → 0 (fix A-viz), and arm 1 went green on fix B. **Sites, receiver-aware, before → after**: `crates/rs_cam_core/tests` + `benches/hot_paths.rs` 487 → 0; `crates/rs_cam_viz/src` test modules + `crates/rs_cam_viz/tests` 122 → 0; `crates/rs_cam_cli` 0; `crates/rs_cam_mcp` 0. A RAW `rg -o` count is NOT a residual: it reads 291 in core and 62 in viz after the migration, because `ProjectSessionBuilder` publishes `add_tool`, `add_model`, `add_setup` and `add_toolpath` under the setter names on purpose. The viz 62 are 52 builder calls, 4 `Controller` name collisions and 6 setter names as data in the WP15a sentry. **The builder extension**: `ProjectSessionBuilder` gains the id-ALLOCATING `add_tool` / `add_model` / `add_setup` / `add_toolpath`, which delegate to the four `*_impl` halves and return what `Effects::created` returns (index, id, index, index); the chaining `name`; and the read accessors `tools` / `models` / `setups`. The `add_model` route keeps `StockConfig::update_from_bbox`, so a migrated fixture on a default stock behaves as it did — no core site takes the chaining `.model()` (`git diff 2bddd689 HEAD -- crates/rs_cam_core/tests` adds none). **THE COMPILE IS THE COMPLETENESS PROOF, and it ran**: `cargo test --no-run` reports 0 errors on `rs_cam_core` with `--features heavy-tests` (so the 12 heavy binaries and `benches/hot_paths.rs` compile too), `rs_cam_viz`, `rs_cam_cli` and `rs_cam_mcp`. A source scan cannot give that; a missed site is a hard error in four compilation units. **Three readers broke on the flip, not two.** The brief named `setters_have_rows_wp15a.rs` and `production_writes_go_through_apply_wp15a.rs`; both now accept `pub(crate) fn ` beside `pub fn `, and the first gains a fourth allowlist `CRATE_PRIVATE_HELPERS` for the 19 crate-private `&mut self` helpers the wider reader newly sees (67 declarations, 59 checked, 0 missing). The third is `crates/rs_cam_viz/tests/effects_are_stamped_wp19.rs:416`, which WP19 landed after the brief's detection command ran: `no_production_site_discards_an_effects_answer` failed its own non-vacuity guard with `I read 1` producer name. **Suites, all 0 failed unless named**: core `setters_are_crate_private_wp15b` 3, `hatches_are_crate_private_wp7` 3 (it reads 3 at `2bddd689` too — the WP15a text's "2" is stale, not a move), `setters_have_rows_wp15a` 2, `command_registry_completeness` 10, `mutation_paths_invalidate_alike_p0` 8, `restore_snapshot_invalidates_like_the_setter_n14` 4, `mcp_mutation_rows_reach_core` 7, `session_builder_preserves_ids` 7, `--lib session::` 156; the ten most-changed core binaries `op_precondition_static_validation_f015` 9, `toolpath_rebind_g_mcprebind` 9, `export_datum_setup_frame` 5, `generated_empty_refusal_g_entryempty` 6, `retract_intent_move_type_census_w6` 2, `adversarial_2d_campaign_r2` 6 passed / 1 ignored, `export_disabled_cached_n1` 1, `op_model_ref_static_validation_f023` 3, `unified_finish_dropped_band_finding_d1` 3, `set_param_refuses_absent_field_n5` 5, plus `air_cut_family_calibration_w5bf4` 2 under `--features heavy-tests`; viz `production_writes_go_through_apply_wp15a` 6, `command_surface_completeness` 14, `effects_are_stamped_wp19` 4, `mcp_wire_surface_pin` 2, `export_parity_core_vs_gui_p0` 2, `--lib gen_parity_p0_tests` 2, whole crate 678 over 47 binaries; CLI 32; MCP 29. `git diff 2bddd689 HEAD -- crates/rs_cam_viz/tests/snapshots/` is empty — the MCP wire snapshot does not move. **One PRE-EXISTING red, not a WP15b regression**: `perf_golden_sim_metrics` reads 4 passed / 1 failed, and `sim_metrics_match_golden` reports **35** fields — the same count the WP22 row ledgers as G-PERFGOLDEN2D. **Verifier-migrated sites**: three packages landed after the writers' base `61c16b75`, so their sites were in no writer's migration. The fix-B commit body lists all six folds by file, with the reason for each. `cargo fmt --all -- --check` and `cargo clippy --workspace --all-targets --features rs_cam_core/heavy-tests -- -D warnings` both clean. Debug builds only: §27 ruling 4. NOT RUN: the full heavy gate. **One open item**: `set_toolpath_param` is now a `pub(crate)` method with no production caller, kept alive by `#[allow(dead_code)]` because 33 in-crate test sites call it and `setters_have_rows_wp15a::every_wrapper_exemption_calls_the_door` panics without the declaration. Whether to delete the method and its exemption is an orchestrator call. |
| WP16 | `SetSetupName` row unreached: route `rename_setup` at `controller/events/model.rs` through the row (review M2) | DONE (`183900e2`; sentry `ff81b712`) | Sentry `crates/rs_cam_viz/tests/command_surface_completeness.rs`, the P1 arm `every_gui_reached_core_row_is_constructed_in_the_view`. The sentry commit flips the row's `gui` column to `Reach::Reached` alone and adds no caller, so the arm is assertion-red: `these rows claim the GUI reaches them, and no view source constructs one: ["set_setup_name"]` (the assert prints the wire name), `test result: FAILED. 13 passed; 1 failed`. **One caller migrated:** `AppController::handle_rename_setup` (`crates/rs_cam_viz/src/controller/events/model.rs:445`) builds `Command::SetSetupName(SetSetupNameArgs { setup_index, name })` and applies it through `apply_controller_command`, the door the other setup edits in that file take; `mark_edited` now runs on the applied path only. **`ProjectSession::rename_setup` is `pub(crate)`** (`crates/rs_cam_core/src/session/mutation.rs:1180`); the workspace grep leaves two callers, the `Command::SetSetupName` arm at `session/command.rs:1650` and the two unit tests in `session/mutation.rs`. Green: viz `--test command_surface_completeness` 14 passed / 0 failed, `--test command_registry_surfaces` 3; viz `--lib rename_setup` 1 (`controller::tests::rename_setup_updates_name`, unchanged); core `--test command_registry_completeness` 10; core `--lib session::` 156; viz 660; CLI 32; MCP 29 — all 0 failed, every count at its baseline. `cargo fmt --all -- --check` and `cargo clippy --workspace --all-targets --features rs_cam_core/heavy-tests -- -D warnings` both clean. No verifier change beyond the writer's two commits. No heavy gate and no whole-core dev loop, per the operator's 2026-09-11 ruling. `cargo doc` is not a gate here and was not run: the doc link at `session/mod.rs:1796` now names a crate-private function. |
| WP17 | Save clears the session simulation (`set_post_config` before save) so rest ops are refused after a save (tech-debt H1) | DONE (`525fe504`; sentry `91bc6c39`) | Sentries: core `crates/rs_cam_core/tests/save_keeps_the_simulation_wp17.rs` (`a_post_config_edit_keeps_the_simulation_wp17`, plus the companion `a_save_keeps_the_simulation_wp17`, which passes pre-fix) and viz `controller::tests::{a_save_keeps_the_simulation_so_a_rest_op_still_starts_wp17, the_mcp_save_conversion_writes_no_session_state_wp17}`. All three assertion-red on the sentry commit alone: `a post-config write that changes nothing clears nothing` (core `:237`), `a save keeps the simulation; the rest operations depend on it` (viz `:3204`), and `the conversion step reads; it must not write` (viz `:3254`, which prints the old arm). No compile-red arm — neither viz test names a fix symbol. **Per-field rule:** `ProjectSession::set_post_config` asks the new `post_change_reaches_motion` (`session/mutation.rs`) which field moved. Two fields clear NOTHING, because a live read can hold no stale copy: `format`, which `export_gcode_checked` resolves at emit time, and `spindle_strategy`, which the feeds suggest path reads at suggest time. **Four fields still clear the simulation**: `spindle_speed` (the emitted S word and `SimulationRequest::spindle_rpm`, so the cut trace's gate readings carry the old value), `high_feedrate_mode` and `high_feedrate` (`SimulationRequest::rapid_feed_mm_min`, so the runtime and air-cut readings carry the old rate), and `safe_z` (`SetupEvalContext` resolves it at generation time and the generators emit rapids at that height). The helper follows `fixture_collision_inputs_moved`: it equalises the two exempt fields and compares the WHOLE record, so a field added to `ProjectPostConfig` later counts as a motion input by default. **Both save sites take the row.** `AppController::save_job_to_path` (`controller/io.rs`) and the MCP `save_project` arm compare the two blocks first and build `Command::SetPostConfig` only on a difference; each binds the `Effects` and mirrors them through the new `AppController::adopt_post_effects`, which stamps stale and calls `invalidate_simulation` on `simulation_cleared`. **The MCP conversion no longer mutates**: `core_command_for`'s save arm returns the new `CorePlan::ApplyPair(preparation, command, before)`, and `RsCamApp::run_core_preparation` (`app/mcp.rs`) applies the preparation before the row's own command; the dispatch match names `ApplyPair` rather than a wildcard, so a fourth plan must still compile there. `large_enum_variant` does not fire on the new variant. **Residual, ledgered to WP15a:** `rg -n "set_post_config\(" crates/rs_cam_viz/src crates/rs_cam_cli/src` leaves three direct callers, all pre-existing and untouched — `controller/events/undo.rs:21` and `:75` (the two `PostChange` arms) and `ui/properties/mod.rs:646` (the post panel, which discards the `Effects` with `let _ =`, so a `spindle_speed` drag clears the session simulation while the viewport keeps showing one). CLI has none. One gap the fix names at the arm: a `safe_z` change makes the stored RESULTS stale and this door does not drop them, because the post panel writes the block on every frame its widget differs and a results drop would destroy generated work during a drag. Suites, all 0 failed: core `save_keeps_the_simulation_wp17` 2, `adopt_simulation_stores_prior_stocks` 10, `command_registry_completeness` 7, `mcp_mutation_rows_reach_core` 2, `--lib session::` 156; viz `--lib wp17` 2, `mcp_core_arm_describes_every_row` 4, `mcp_toasts_report_outcome_g_mcptoast` 9, `mcp_wire_surface_pin` 2 (the WP2a wire snapshot did not move), whole crate 662 (660 + the 2 new arms); CLI 32; MCP 29. `cargo fmt --all -- --check` and `cargo clippy --workspace --all-targets --features rs_cam_core/heavy-tests -- -D warnings` both clean. No verifier change beyond the writer's two commits. No heavy gate and no whole-core dev loop, per the operator's 2026-09-11 ruling. |
| WP18 | Vacuous WP12 sentry arm (H4) and lost feed-optimisation refusal coverage (H5) | DONE (`3654826a`; coverage `514fd0fc`) | H5: new `crates/rs_cam_core/tests/feed_optimization_refusals_wp18.rs`. Four arms green: `the_predicate_names_the_three_refusals_and_clears_a_fresh_pocket`, `the_captured_inputs_carry_the_real_stock_bounds`, `the_feed_optimisation_dial_moves_the_off_commanded_feed_count`, `a_remaining_stock_operation_generates_with_the_dial_inert`. Coverage grep: `rg -c feed_optimization_unavailable_reason crates/rs_cam_core/tests crates/rs_cam_viz/tests` matched 0 files at `48869f11` and matches 1 file (8 hits) now. The verifier corrected arm (c) from `>` to `!=`. The arm measured ON 72 and OFF 114. `feedopt::optimize_feed_rates_inner` writes `nominal * factor` onto every cutting move and reads no move's own feed (`feedopt.rs:164`), so a plunge descent at full engagement returns to the commanded feed. The arm asserts that the dial MOVES the count, which is what arm (d) needs. H4: `loose_executor_is_crate_private_wp12::viz_names_the_core_generation_module_only_where_the_allowlist_says` reads every line of `crates/rs_cam_viz/src` and allows two references. Injection experiment: the verifier appended `// mirrors session/compute.rs` to `crates/rs_cam_viz/src/compute/mod.rs`; the arm FAILED and named `rs_cam_viz/src/compute/mod.rs:336`; the revert returned 3 passed. The green run clears the staleness assertion, so both allowlist entries still match. Suites: core `feed_optimization_refusals_wp18` 4, `loose_executor_is_crate_private_wp12` 3, `gen_inputs_one_assembly_n12` 5, `job_three_steps_equal_generate_toolpath` 5; viz 660; CLI 32; MCP 29. `cargo fmt --all -- --check` and `cargo clippy --workspace --all-targets --features rs_cam_core/heavy-tests -- -D warnings` both clean. No heavy gate (operator ruling 2026-09-11). |
| WP19 | Stale stamping: 19 `let _ =` Effects sites in viz; merge `mcp_stamp_stale` into `stamp_stale` (H2, H3) | DONE (`0395aba0` + `09eb9768`; sentry `64922519`) | Re-measured at `10263b4d`: the nineteen `stale` sites were already closed by WP15a and WP17. The live residual was `Effects::simulation_cleared` — 23 core mutation sites drop `session.simulation`, and ONE viz production site read the flag. Sentry `crates/rs_cam_viz/tests/effects_are_stamped_wp19.rs` (4 arms) plus 4 arms in `crates/rs_cam_viz/src/controller/tests.rs`. **The sentry commit alone was assertion-red, never compile-red.** Arm (a) `no_production_site_discards_an_effects_answer` printed `I read 1 discarded answer(s): crates/rs_cam_viz/src/app/input.rs:~116: apply` — the `WizardSetPost` arm bound the error alone and dropped BOTH halves of the answer; it now takes `adopt_post_effects`. Arm (c2) `one_helper_stamps_stale_on_both_routes_h3` printed ``H3: `app/mcp.rs` still declares or calls `mcp_stamp_stale`. One helper stamps `stale_since`, and it is `crate::state::stale::stamp_stale`.`` — pins plan §28 ruling 2. Arm (b2) `a_panel_stock_edit_clears_the_view_simulation_wp19` printed `the session dropped its simulation and the viewport still draws one.` — pins §28 ruling 3, the `PanelSideEffects.invalidate_simulation` flag discharged once per frame. Arm (c1) `a_stamped_row_reads_the_catalog_auto_regen_h3` printed ``assertion `left == right` failed: a created row must read the operation's own `default_auto_regen`  left: true  right: false`` — `toolpath_rt_or_default` hardcoded `true` and the fixture's `Scallop` declares `false`; twelve of the twenty-four operations do. Arm (b3) `an_undone_stock_change_stales_every_toolpath_wp19` was GREEN on both sides by design and stays as a regression pin (WP15a routed the undo door through `apply_quietly`). Arm (b1) `a_toolpath_edit_clears_the_view_simulation_wp19` ships WITH its fix, so its red was read against the fix-1 commit with the fix-2 `controller/tests.rs` checked out over it: the control passed (the toggle drops the SESSION's simulation) and the arm stopped at `and the viewport must not go on drawing the run of a job that no longer exists`, `tests.rs:5854`. It pins §28 ruling 1 — a toolpath edit CLEARS the view's simulation like every other edit, and the F2.5 banner treatment for toolpath edits ends. **No test expectation moved for that ruling**: nothing in the crate pinned the old stale-not-clear reading, so the whole crate is green unchanged. Suites, all 0 failed: viz `--test effects_are_stamped_wp19` 4, `--lib controller::tests` 101, `--test production_writes_go_through_apply_wp15a` 6, `--test command_surface_completeness` 14, `--test command_registry_surfaces` 3, `--test mcp_wire_surface_pin` 2, `--test mcp_core_arm_describes_every_row` 4, `--test mcp_rebind_surface_g_mcprebind` 6, `--test feeds_apply_drops_result_n13` 5, `--test model_refresh_invalidates_results_g_rescalestale` 4; `every_undo_arm_marks_the_project_edited_g_undofresh` is an in-crate arm, covered by `--lib controller::tests`. Whole crates: viz 676, CLI 32, MCP 29; core `--test command_registry_completeness` 10. **The MCP wire snapshot did not move**: `git diff 1c656927 HEAD -- crates/rs_cam_viz/tests/snapshots/` is empty and the directory carries no untracked file. `cargo fmt --all -- --check` clean after one fold: the sentry file's own `let answers_effects` binding re-wrapped, so the format folded into the SENTRY commit, and no fix file moved. `cargo clippy --workspace --all-targets --features rs_cam_core/heavy-tests -- -D warnings` exit 0 (the one `warning:` line is the pre-existing `nom` / `quick-xml` future-incompat note). No whole-core suite and no heavy gate: operator ruling 2026-09-11; debug builds only: §27 ruling 4. |
| WP20 | Prose pass: row count doc (H6), twenty stale `Skip` reasons (H7), plan §5 residual list (H9), doubled "Save failed" (H10), dead `P1_EXEMPT` (M13) | DONE (`9b3f75de`) | No sentry: P1 of `crates/rs_cam_viz/tests/command_surface_completeness.rs` covers the exemption deletion, and it now scans `GenerateToolpath` like every other row. **Four greps at 0**: `rg "WP6 adopts|WP5 gives|WP4 revisits|WP6b adopts" crates/rs_cam_core/src/session/command.rs`; `rg "Save failed: Save failed" crates/`; `rg "P1_EXEMPT" crates/`; and the module doc states no row count (`CommandId::ALL.len()` is the count, per H6). Nineteen `Reach::Skip` reasons restated; the twentieth, `SetSetupName`, belongs to WP16 and keeps its `gui: Reach::Reached` — the cherry-pick auto-merged and that row was read back. `IMPLEMENTATION_PLAN.md` §5 gains the residual addendum, nine rows; every line citation in it was read back on master, and the verifier folded one correction into the fix commit (`session/mutation.rs:1223` → `:1230`, moved by WP16). Suites, all 0 failed: viz `--test command_surface_completeness` 14, `--test command_registry_surfaces` 3, `--test mcp_wire_surface_pin` 2, `--test mcp_core_arm_describes_every_row` 4; core `--test command_registry_completeness` 10; viz 660 over 44 binaries; CLI 32; MCP 29. The WP2a wire snapshot did not move. `cargo fmt --all -- --check` and `cargo clippy --workspace --all-targets --features rs_cam_core/heavy-tests -- -D warnings` both clean. No whole-core suite and no heavy gate: operator ruling 2026-09-11. |
| WP21 | `feedopt::optimize_feed_rates_inner` clamp panics `min > max` when half the nominal feed exceeds the machine max (two pre-existing red tests in `retarget_reconciliation_a8`, reproduced at `520f16ab` by the WP14a verifier; G-FEEDOPTPLUNGE family) | DONE (`4aeaeda3`; sentry `10c2ad37`) | Sentry `crates/rs_cam_core/tests/feedopt_clamp_never_panics_wp21.rs`, two arms: `a_pocket_above_twice_the_ceiling_generates_without_a_panic` (the production door, through `Job::GenerateToolpath` + `execute_job`) and `the_pass_caps_the_floor_at_the_ceiling_instead_of_panicking` (the pass itself). The sentry commit alone was **panic-red**, not compile-red — both arms printed `min > max, or either was NaN. min = 6000.0, max = 3000.0` at `library/core/src/num/f64.rs:1415`. The two pre-existing a8 arms printed the same line. **Construction site** `compute::execute::apply_dressups` (`execute.rs:4381`) caps the derived floor at the dial: `min_feed_rate: (nominal * 0.5).min(max_rate)`. **Clamp site** `feedopt::optimize_feed_rates_inner` (`feedopt.rs:~185`) composes `.max(floor).min(ceiling)`, so the ceiling wins on an inverted pair from any later caller; `rg "\.clamp\(" crates/rs_cam_core/src/feedopt.rs` now matches 0. The 0.5 factor does not move — `git diff d83c6b55 HEAD -- crates/rs_cam_core/src | rg "^[-+].*(0\.5|feed_max_rate)"` shows only that one line pair — and no threshold, gate bar or feeds constant changes. **The clamp is old; the red is one day old.** The dressup default flipped ON at `0be9ccc4` (2026-05-11); `feed_optimization` first reaches `session/compute.rs` at `4b53576b` (WP11b, 2026-09-11), which is why the a8 arms are green in the 2026-08-08 artifacts and panic on master. **The a8 tests are green WITH a fixture change** (verifier, folded into the fix commit): `retarget_reconciliation_a8::fixture_session` sets `dressups.feed_optimization = false`. With the dial ON and the panic gone, both arms failed at `retarget_reconciliation_a8.rs:296` with `fixture produced no chipload measurement at all: SteadyStateSamplesNotPresent` — the gate abstained on an empty population; that abstention's cause is NOT measured. The fixture measures retarget reconciliation against the chipload band and lifts its MACHINE ceiling to 24 000 mm/min for exactly that reason, so the dial is off. No constant, threshold or assertion in that file moves; `max_feed_mm_min` is untouched. The verifier also removed one stray blank line the writer's `CLAUDE.md` bullet left. Suites, all 0 failed: core `--test feedopt_clamp_never_panics_wp21` 2, `--test feed_optimization_refusals_wp18` 4, `--test gen_inputs_one_assembly_n12` 5, `--lib retarget_reconciliation_a8` 2, `--lib tool_load::` 354, `--lib feedopt` 10; viz 662; CLI 32; MCP 29. `cargo fmt --all -- --check` and `cargo clippy --workspace --all-targets --features rs_cam_core/heavy-tests -- -D warnings` both clean (0 lint lines; the one `warning:` is the pre-existing `nom`/`quick-xml` future-incompat note). No whole-core suite and no heavy gate: operator ruling 2026-09-11. |
| WP22 | G-FEEDOPTPLUNGE: the feedopt pass caps a geometric plunge at the operation's `plunge_rate` (plan §27 ruling 3) | DONE (`d880ca1a`; sentry `e9289426`) | Sentry `crates/rs_cam_core/tests/feedopt_caps_plunges_g_feedoptplunge.rs`, four arms. On the sentry commit alone the file carried arms (a) and (c) and was **assertion-red**, not compile-red: `a_generated_pocket_holds_no_plunge_above_the_plunge_rate` printed `G-FEEDOPTPLUNGE: 18 of 18 plunges exceed the plunge rate; worst ratio 2.00x. Move 2 carries 1000 mm/min against a plunge rate of 500 mm/min`, and the control `the_generator_alone_emits_no_plunge_above_the_plunge_rate` passed on both sides. Green on the fix: 4 passed / 0 failed, the fix arms being `the_pass_caps_a_vertical_descent_and_leaves_a_lateral_move` and `the_pass_caps_a_descent_that_reads_air`. **The cap reaches the production door, measured independently**: on the `perf_golden_sim_metrics` 2D fixture, whose operations carry `plunge_rate: 400.0` against a `feed_rate` of 1000.0, `per_kinematics[Plunge].average_leading_edge_speed_mm_min` reads 1000.0 at base `61c16b75` and exactly 400.0 with WP22, on all three operations. **One PRE-EXISTING red, not a WP22 regression**: `perf_golden_sim_metrics::sim_metrics_match_golden` fails at base `61c16b75` with 36 fields and with WP22 with 35. The verifier ran the binary at base in a detached checkout and diffed the field sets: they are identical except `triage_action_count`, which reads 4 at base against a golden 1 and which WP22 RESTORES to 1. Every plunge field moves toward the golden. The golden file was last written at `d0aeee02`, a G-RAMPCONTAIN fix; the last explicit re-bless is `691304b4`. This base red is ledgered nowhere else in `planning/`. it is NOT re-baselined here, because a golden move is not the verifier's to make. Suites, all 0 failed unless named: core `--test feedopt_caps_plunges_g_feedoptplunge` 4, `--test feedopt_clamp_never_panics_wp21` 2, `--test feed_optimization_refusals_wp18` 4, `--test gen_inputs_one_assembly_n12` 5, `--test job_three_steps_equal_generate_toolpath` 5, `--test lead_in_out_feed_rates_f040` 5, `--test drill_metrics_pr2` 2, `--test drill_runtime_survives_retime_n2` 5, `--test drill_material_plumbing_f016` 3, `--test pindrill_emission_frame_g_pindrill` 2, `--test drill_fed_descents_motion` 3, `--test predicted_feed_gates_f035` 4, `--test adaptive3d_commanded_ladder` 1, `--test post_format_round_trip_p1` 4, `--test perf_golden_sim_metrics` 4 passed / 1 failed (the pre-existing red above), `--lib feedopt` 10, `--lib tool_load::` 354; extra, per the scout brief section 7: `--test plunge_guard_p3` 7, `--test dressup_span_invariants` 4. `--test wanaka_e2e_chipload_gate` ran and reports 0 passed / 1 ignored: it is an instrument, not coverage. viz 668; CLI 32; MCP 29. WP18 arm (c) re-measured through a temporary inverted assertion that the verifier reverted in the same command: the pair is UNCHANGED from the WP18 row's own numbers, so the cap moved no plunge that the pass had lifted to the commanded feed on that fixture. NOT MEASURED: whether that fixture holds any geometric plunge at all; its pocket names no `entry_style` and so takes the shipped `Ramp` default, which `classify_move` calls `Ramp`. `cargo fmt --all -- --check` clean, no file moved. `cargo clippy --workspace --all-targets --features rs_cam_core/heavy-tests -- -D warnings` exit 0 (the one `warning:` line is the pre-existing `nom` / `quick-xml` future-incompat note). No whole-core suite and no heavy gate: operator ruling 2026-09-11; debug builds only: section 27 ruling 4. |
| WP23 | Review B1: two `UiCommand` rows declare `gui: Reached` with no constructor (`SimJumpToOpEnd`, `CancelToolpathGeneration`); delete both; view sentry gains a constructor census (plan §29 ruling 1) | DONE (`325f2249` + `07eddc1e`; sentry `151f1c21`) | Sentry `crates/rs_cam_viz/tests/command_surface_completeness.rs`, a new P2 census arm `every_gui_reached_view_row_is_constructed_in_the_view` plus the non-vacuity guard `the_p2_view_locator_finds_a_known_construction`. **The sentry commit alone was ASSERTION-red, never compile-red** — the sentry names no symbol the fix introduces, so the binary builds. The arm printed `these view rows claim the GUI reaches them, and no production view file constructs one: ["sim_jump_to_op_end", "cancel_toolpath_generation"]` at `command_surface_completeness.rs:326`, and `test result: FAILED. 15 passed; 1 failed`. The guard passed in the SAME run, so the census is not vacuous: it pins two known constructions (`SimJumpToOpStart` in `ui/sim_op_list.rs`, `CancelCompute` in `ui/viewport_overlay.rs`) and one known ABSENCE, `SetUiView`, which owns a dispatch arm and no GUI caller — a hit there would prove the needle matched a match arm. The needle keeps the `Ui(` of `AppEvent::Ui(...)` for that reason. **Deleted** (`325f2249`, 4 files, 19 insertions / 88 deletions): the two `for_each_ui_command!` registry rows; the `SimJumpToOpEndArgs` payload; both handler arms in `controller/events/mod.rs` and the `SimJumpToOpEndArgs` import they needed; `AppController::handle_sim_jump_to_op_end` in `controller/events/simulation.rs`, whose only caller was the deleted arm; and the orphan test `cancel_toolpath_generation_event_only_cancels_toolpath_lane` in `controller/tests.rs`, which pinned the deleted row alone — its shared section banner now describes the surviving test and records why the first went. `SimJumpToToolpathEnd` cited the deleted wire name as its GUI skip reason; the reason now states the fact. A flip to `Skip` was the wrong repair, per §29 ruling 1: a `Skip` keeps the handler arm, and the arm is what hid the row from the handler census. The `controller/tests.rs` imports needed no fold — `LaneState` and `NoArgs` keep 9 and 11 other uses. **The MCP wire did not move**: `git diff bf77aac4 HEAD -- crates/rs_cam_viz/tests/snapshots/` is empty and the directory carries no untracked file; both rows declared `mcp: Skip`, and `RsCamApp::describe_core` matches `CommandId`, not `UiCommandId`, so the MCP dispatch held no arm for either. MCP `cancel_generation` still cancels through `GenerationControl` on the server thread (`mcp_escape_hatches` 15 passed). **Non-mcp compile** (`07eddc1e`, no sentry — a feature configuration is a build property and a unit test in one configuration cannot measure another). `cargo check -p rs_cam_viz --no-default-features` at master `bf77aac4`: **exit 101**, `error[E0433]: failed to resolve: use of unresolved module or unlinked crate rs_cam_mcp` at `ui_command.rs:312:15`, plus `warning: unused variable: progress` and `warning: unused variable: total` at `compute.rs:1684`. At `07eddc1e`: **exit 0, and the crate emits NO warning**. The repair gates `GetCutTraceArgs::caps` (a wire-only row whose builder and reader both already carry the gate) and adds `#[cfg_attr(not(feature = "mcp"), allow(unused_variables))]` to `generate_all_progress_at`; `Cargo.toml` is untouched and the MCP behaviour with the feature ON is unchanged. **Verifier fold, a THIRD defect of the same class**: `RsCamApp::egui_ctx` (`app.rs:56`) is read inside `pump_dispatch`'s `#[cfg(feature = "mcp")]` arm alone (`app.rs:589`), so the non-mcp build called the field dead. The E0433 MASKED it — rustc runs no dead-code analysis on a crate that fails name resolution — so it was unreachable until the writer's own fix landed, and the writer's session limit left their claimed clippy command unrun. The field now carries `#[cfg_attr(not(feature = "mcp"), allow(dead_code))]`, the writer's own idiom; a `#[cfg]` gate on the field would force one on the constructor at `app.rs:244` too. `cargo clippy -p rs_cam_viz --no-default-features --lib -- -D warnings` exit 0. **The `--all-targets` form exits 101 on a PRE-EXISTING breakage WP23 does not reach**: `crates/rs_cam_viz/src/controller/results_parity_tests.rs`, an in-crate `#[cfg(test)]` module created `4bfed4c4` (2026-08-13), calls the mcp-gated `build_mcp_diagnostics` and `crate::app::mcp::viz_project_evidence` with no feature gate — eight errors at lines 265, 268, 327, 339, 342, 408, 418 and 426, byte-identical at `bf77aac4` and here. Gating that module is not WP23's, so the commit body names `--lib`. Suites, all 0 failed: viz `--test command_surface_completeness` **16** (14 + the two new arms), `--test command_registry_surfaces` 3, `--test mcp_wire_surface_pin` 2, `--test mcp_core_arm_describes_every_row` 4, `--test mcp_escape_hatches` 15, `--test effects_are_stamped_wp19` 4, `--test production_writes_go_through_apply_wp15a` 6, `--lib controller::tests` **100** (was 101 — the deleted orphan test); whole viz crate **679 over 47 binaries** (678 + the two new census arms − the deleted orphan), measured both before and after the fold; CLI 32; MCP 29. `cargo fmt --all -- --check` clean at every commit — no fold was needed, and only `app.rs` moved for the third defect. `cargo clippy --workspace --all-targets --features rs_cam_core/heavy-tests -- -D warnings` exit 0 (the one `warning:` line is the pre-existing `nom` / `quick-xml` future-incompat note). No whole-core suite and no heavy gate: operator ruling 2026-09-11; debug builds only: §27 ruling 4. |
| WP24 | Lift the full-screen Optimize placeholder: non-modal progress row with cancel; GUI usable during a run (plan §30 ruling 3) | DONE (`e8c0076b`; sentry `28f5675b`) | Sentry `crates/rs_cam_viz/tests/optimize_run_is_non_modal_wp24.rs` (five arms, every needle a string so the binary compiles against the pre-fix library) plus three in-crate arms in `crates/rs_cam_viz/src/controller/tests.rs` under the heading "WP24 — the Optimize run is state, not a lockout". **Two red kinds at the sentry commit alone.** ASSERTION-red, `cargo test -p rs_cam_viz --test optimize_run_is_non_modal_wp24`, **1 passed; 4 failed**: `app_draws_the_workspace_layout_during_an_optimize_run` read "app.rs still gates drawing on the Optimize flag"; `no_full_screen_optimize_heading_survives` read "app.rs still carries the full-screen placeholder heading 'Optimize is running'"; `the_workspace_bar_draws_the_optimize_progress_row` read "ui/workspace_bar.rs draws no Optimize progress row"; `the_registry_declares_the_cancel_optimize_run_row` read "ui_command.rs declares no CancelOptimizeRun row"; the non-vacuity arm `the_source_scans_find_a_known_control` passed, which is what makes the four absence arms trustworthy. COMPILE-red, `cargo test -p rs_cam_viz --lib optimize`: `error[E0609]: no field optimize_run on type state::AppState` at `controller/tests.rs:5970:26` and `:5985:26`, `error[E0599]: no variant or associated item named CancelOptimizeRun found for enum ui_command::UiCommand` at `:6024:62`, `error: could not compile rs_cam_viz (lib test) due to 5 previous errors`. The third in-crate arm's assertion-red was MASKED by that compile failure, as the sentry body said. **GREEN after the fix**: the sentry binary **5 passed; 0 failed**; `--lib` **379 passed; 0 failed**. **Verifier correction, one probe and nothing else.** Arm `a_view_command_still_applies_while_an_optimize_run_is_in_flight` dispatched `UiCommand::SwitchWorkspace(Workspace::Setup)` and read `Toolpaths` against `Setup`: the controller does not apply that command (`UiCommand::SwitchWorkspace(_) => {}` at `controller/events/mod.rs:586`) and `RsCamApp` applies it in `app/input.rs`, because the switch also moves the camera and the overlay set. The probe is now `UiCommand::ToggleSimPlayback`, read before and after the dispatch; the arm's subject and its COMPILE-red kind do not move. The brief's §4 arm 3 proposed `SwitchWorkspace`, so the arm's doc records why a controller test cannot use it. **Suites, all 0 failed**: whole viz crate `cargo test -p rs_cam_viz -q` **687 passed over 48 binaries** (679 over 47 at WP25, plus the five integration and three in-crate arms); `--test command_registry_surfaces` 3; `--test command_surface_completeness` 16 (the WP23 view-constructor census, which the new `gui: Reached` row has to satisfy); `--test optimize_run_is_non_modal_wp24` 5; `--test optimize_runs_on_the_job_lane_wp14b` 2; `--test mcp_wire_surface_pin` 2; `--test egui_draw_sites_write_through_commands_wp6` 6; `--test ui_string_hygiene` 1; `--test workspace_menu_complete_g_wsmenu` 10; `--test mcp_toasts_report_outcome_g_mcptoast` 9; `cargo test -p rs_cam_core --test command_registry_completeness` 10; `cargo test -p rs_cam_core --test optimize_toolpath_is_a_job_wp14b` 5; `cargo test -p rs_cam_mcp -q` 29; `cargo test -p rs_cam_cli -q` 23 + 9. **Gates**: `cargo fmt --all -- --check` exit 0 (one `cargo fmt --all` fold into the fix commit — rustfmt expands `Toolpath { toolpath_id: rs_cam_core::ToolpathId }` onto three lines in `state/mod.rs`, and `git diff --stat` confirmed that file alone moved); `cargo clippy --workspace --all-targets --features rs_cam_core/heavy-tests -- -D warnings` **exit 0**; `cargo clippy -p rs_cam_viz --no-default-features --all-targets -- -D warnings` (the WP25 addition, required because this item adds viz code) **exit 0**. Both clippy runs print only the pre-existing `nom` / `quick-xml` future-incompat note. Memory low-water **10 GiB available**, above the 9 GiB close-out floor. Debug builds only and `target/release` untouched (§27 ruling 4); no whole-core suite and no heavy gate (operator ruling 2026-09-11). **Three things the fix records rather than decides.** (1) `AppState::close_modals_for_exclusivity` still SPARES a running Optimize, so with the placeholder gone an operator can open the Feeds modal or the Tool Library mid-run and see two overlapping windows — the placeholder is what made that 3-deep stack unreachable. (2) Applying a candidate is NOT blocked on a stale baseline: the run scores a cloned session so an edit cannot corrupt it, but the outcome then names params the project no longer carries, and `FreshnessGate::banner` is the only mitigation. (3) The two cancels are asymmetric — the modal's own Cancel closes the window so `land_optimize_outcome` discards the partial outcome, while the row's Cancel leaves the window open so the partial outcome LANDS as `Ready`. **Not measured**: anything on screen. No GUI ran and no screenshot was taken. Four Optimize entry points stay ENABLED during a run (three "Optimize this op" buttons in `ui/sim_diagnostics.rs` and the rollup's "Review ▸") and each refuses with the new toast; no progress observer was added, so the row reports the label and the elapsed seconds alone. |
| WP26 | G-F036B-FLOOR: fix the AB5 floor arm's population in `adaptive_feed_modulation_pipeline_f036b.rs` — a per-index IR diff replaces the feed-differs-from-nominal proxy, plus a `PlungeRate` exemption; test-only, no feeds quantity moves (plan §31, operator ruling 2026-09-13) | DONE (`610f8a58`) | RED on `bca7656b`: `adaptive_feed_modulation_pipeline_f036b` read 8 passed / 2 failed. AB5 `modulated_path_never_emits_below_min_chipload` panicked at `:615` — "F-036b AB5: modulated IR has 21 of 126 modulated moves below the LUT band floor (worst = 0.0215 mm/tooth, band floor = 0.0304 mm/tooth, band.start = 0.0320, commanded feed = 770 mm/min)", left 21 right 0. GREEN with the commit: same binary, same flags, `9 passed; 1 failed`. The one red is `modulation_raises_cutting_chipload_toward_band` (`flag-ON median chipload 0.0221 should land in band [0.0320, 0.0550]`), red by design per `planning/ui_fix_2026-09-09/reports/J2.md`. The arm needed no compile fix and no format change. Suites: `feedopt_clamp_never_panics_wp21` `2 passed; 0 failed`; `plunge_guard_p3` `7 passed; 0 failed`. Gates: `cargo fmt --all -- --check` exit 0 (measured after the cherry-pick; the file needed no format change); `cargo clippy -p rs_cam_core --all-targets --features heavy-tests -- -D warnings` exit 0. `rs_cam_viz` clippy not run — no viz file moves. Memory low-water 10 GiB available against the 9 GiB floor. |
| WP27 | Viewport draws the SELECTED toolpath only by default; "show all" stays available (plan §32, operator ruling 2026-09-13 after the smoke pass: many rows make the viewport slow) | DONE (`940b8a05`; sentry `fc84547f`) | Sentry `crates/rs_cam_viz/tests/viewport_draws_selected_only_wp27.rs`, nine arms, all green: `a_fresh_viewport_draws_the_selected_toolpath_only`, `show_all_draws_every_toolpath_in_config_order`, `the_draw_set_follows_the_selection_and_the_pin_wins`, `an_ungenerated_or_hidden_toolpath_is_never_drawn`, `nothing_selected_draws_nothing`, `the_upload_and_the_pick_read_one_draw_rule`, `mcp_screenshot_toolpath_does_not_read_the_draw_set`, `the_upload_key_carries_the_scope_and_the_selection`, `the_simulation_workspace_still_draws_every_toolpath`. **RED KIND: COMPILE-red**, as the sentry commit said. All three missing symbols are named in one run — `error[E0432]: unresolved imports ...ToolpathDrawFilter, ...toolpaths_to_draw` at `:45:35`, then four `error[E0609]: no field show_all_toolpaths on type ViewportState` at `:119`, `:357`, `:362` and `:368`, and `error: could not compile rs_cam_viz (test "viewport_draws_selected_only_wp27") due to 5 previous errors`. The verifier took the sentry commit straight to the fix, so the brief's assertion-red split under a behaviour-neutral scaffold is NOT MEASURED. GREEN on the fix: `test result: ok. 9 passed; 0 failed`. **Suites, all 0 failed**: viz `--test viewport_draws_selected_only_wp27` 9; `--test overlays_registry` 27; `--test command_surface_completeness` 16; `--test command_registry_surfaces` 3; `--test ui_string_hygiene` 1; `--test workspace_menu_complete_g_wsmenu` 10; `--test mcp_wire_surface_pin` 2; `--test optimize_run_is_non_modal_wp24` 5; whole viz crate `cargo test -p rs_cam_viz -q` **696 passed over 48 binaries** (687 at WP24 plus the nine new arms), cargo exit 0; core `--test command_registry_completeness` 10; `cargo test -p rs_cam_mcp -q` 29. **The MCP wire snapshot did not move** — `mcp_wire_surface_pin` is green with no re-bless, because no prose moved. The row works on the wire regardless: `apply_overlays` reads the registry, not the prose. The writer's reading is CORRECT and the verifier checked it — `crates/rs_cam_viz/tests/snapshots/mcp_wire_surface.json` carries the `set_ui_view` overlays parameter's own description, which enumerates 29 overlay ids and does NOT name `all_toolpaths`. So the follow-up that adds the id to the two prose lists (`crates/rs_cam_viz/src/mcp_server.rs` and `crates/rs_cam_mcp/src/server.rs`) needs a re-bless under `RS_CAM_UPDATE_WIRE_SNAPSHOT=1`, which no lane in this item could run. **Gates**: `cargo fmt --all -- --check` exit 0 after TWO folds, each on the item's own file — the sentry's `for needle in [...]` array expanded onto five lines (folded into the SENTRY commit, the file that moved), and `gpu_upload.rs`'s `if draw_set.contains(&tp_id) && let Some(result) = result` expanded onto three (folded into the FIX commit). `git diff --stat` confirmed one file each time; `.mcp.json` is modified in the tree, is not the verifier's, and was never staged. `cargo clippy --workspace --all-targets --features rs_cam_core/heavy-tests -- -D warnings` exit 0; `cargo clippy -p rs_cam_viz --no-default-features --all-targets -- -D warnings` exit 0. Both print only the pre-existing `nom` / `quick-xml` future-incompat note. **Memory low-water 5.9 GiB available** (6079 MB at 19:00:37, during the workspace clippy compile), which dips just under the 6 GiB stop floor; no OOM kill followed, the run recovered to 7.8 GiB and completed, and the verifier let it finish rather than restart it into the same peak. Wall time 18 min (18:47 to 19:06). **Two consequences the operator must know**: nothing selected draws NO toolpath, so the viewport is empty after a project load until a row is clicked (`controller/io.rs` sets `Selection::None`); and a viewport click cannot select an undrawn toolpath, because the pick and the upload read one function — the operations list is the picker. **Not measured**: frame time, and anything on screen. No GUI ran and no screenshot was taken; the cost argument is a reading of the draw loop. No core file moves. Follow-ups ledgered by the brief: the option-2 `ToolpathDrawScope` enum unification, the "select the first toolpath on load" operator decision, and the two `set_ui_view` description lists. No whole-core suite and no heavy gate: operator ruling 2026-09-11; debug builds only, `target/release` untouched: §27 ruling 4.
| WP29 | Optimize reports progress: stage boundaries and candidate done/total on the progress row and the Optimize window (plan §33; the WP24 brief §3(f) deferral, operator asked 2026-09-13) | DONE (`83c6219b`; sentry `7834761f`) | **Five arms over three files.** Core sentry `crates/rs_cam_core/tests/optimize_reports_progress_wp29.rs`, one test `the_search_publishes_its_rungs_and_its_candidate_counts`, one search on Fixture G of the WP14b suite, five assertions (the run reaches the Refine rung; the AxisGrid rung forms candidates; AxisGrid enters every candidate it announced; Refine does the same; a settled run claims no rung). Two in-crate arms in `crates/rs_cam_viz/src/controller/tests.rs` (a fabricated run reports `stage 2/3` and `candidate 3/8`; a tier-map preview keeps the WP24 text). Two source-scan arms appended to `crates/rs_cam_viz/tests/optimize_run_is_non_modal_wp24.rs`. **RED KINDS, both measured at the sentry commit alone.** COMPILE-red on the core binary: `error[E0432]: unresolved imports rs_cam_core::tool_load::optimize::OptimizeProgress, ...::SearchPhase` at `:96:40`, `error[E0599]: no method named with_progress found for struct Box<OptimizeToolpathHandle>` at `:194:12`, `error: could not compile rs_cam_core (test "optimize_reports_progress_wp29") due to 2 previous errors`. COMPILE-red on `cargo test -p rs_cam_viz --lib optimize`: the same two unresolved imports at `controller/tests.rs:6174:44`, three `error[E0433]: could not find OptimizeStageMark in state` at `:6215`, `:6216`, `:6217`, `error[E0560]: struct state::OptimizeRun has no field named progress` at `:6190`, and `error[E0599]` on `progress_text` and on `stage_rows`. ASSERTION-red on the source-scan file, `test result: FAILED. 5 passed; 2 failed` — `the_workspace_bar_reads_the_shared_progress_text` panicked at `:188:5` ("ui/workspace_bar.rs still builds the row's sentence itself, so it reports no stage and no candidate count") and `the_optimize_window_lists_the_search_rungs` panicked at `:208:5` ("the Optimize window still shows a bare spinner"). **GREEN on the fix, all 0 failed**: `--test optimize_reports_progress_wp29` 1; `--test optimize_run_is_non_modal_wp24` 7; `-p rs_cam_viz --lib optimize` 33 (348 filtered out); `--test optimize_toolpath_is_a_job_wp14b` 5 **with no edit**, so the `execute_optimize_toolpath` signature pin at `:299-301` holds and the progress rides the handle; `--test optimize_runs_on_the_job_lane_wp14b` 2; `--test command_surface_completeness` 16; `--test command_registry_surfaces` 3; `--test ui_string_hygiene` 1; `--test mcp_wire_surface_pin` 2; whole viz crate `cargo test -p rs_cam_viz -q` **700 passed over 49 binaries** (696 at WP27 plus the four WP29 arms), exit 0; `cargo test -p rs_cam_mcp -q` 29. The MCP wire snapshot did not move and needed no re-bless: WP29 adds no tool and no parameter. **Gates**: `cargo fmt --all -- --check` exit 0 after ONE fold on the item's own file — `ctx.progress.begin_phase(SearchPhase::FeedRpm, stage_f_total)` in `tool_load/optimize/mod.rs:726` expanded onto two lines under the effective `fn_call_width` of 60, folded into the FIX commit; `git diff --stat` confirmed one item file; `.mcp.json` is modified in the tree, is not the verifier's, and was never staged. **Lint gate ADAPTED for memory** (about 8 GiB available at launch, not the usual 12): the workspace clippy ran as two per-crate runs in sequence — `cargo clippy -p rs_cam_core --all-targets --features heavy-tests -- -D warnings` exit 0 and `cargo clippy -p rs_cam_viz --all-targets -- -D warnings` exit 0 — plus WP25's `cargo clippy -p rs_cam_viz --no-default-features --all-targets -- -D warnings` exit 0. `rs_cam_cli` and `rs_cam_mcp` were therefore NOT linted in this item; no WP29 file lives in either crate. All three runs print only the pre-existing `nom` / `quick-xml` future-incompat note. **Memory low-water 6.4 GiB available** (6551 MB at 19:49:33, during the green suite), above the 6 GiB stop floor, so the run never paused. Wall time 12 min (19:43 to 19:55). **What the fix does**: `OptimizeProgress` is eight atomics and a `Copy` `SearchPhase` in the new `crates/rs_cam_core/src/tool_load/optimize/progress.rs`; it rides `EvaluationContext`, so all four candidate-forming helpers keep their signatures, and it rides `OptimizeToolpathHandle`, so `execute_optimize_toolpath` keeps its two parameters. The struct keeps one `(reached, total)` pair PER RUNG, because the three totals are discovered in sequence and a whole-run total is not honest. **The writer deliberately dropped the brief's `end_phase`**, and the commit body says why: a post-loop `reached = total` would report a cancelled rung as complete and would hide a tick placed inside `evaluate_candidate`. The verifier left that alone. **No time-left figure**, per §33: the whole-run total is unknown up front and a Refine candidate simulates at a finer cell than a grid candidate. **Not measured**: anything on screen. No GUI ran and no screenshot was taken, so the row's and the window's appearance is NOT verified; the measurement is the text builder and the rung list, which the in-crate arms read. Stage F is not asserted, because an `Unmodeled` chipload with no exceeding load gate runs neither Stage-F mode. Follow-up ledgered by the brief: the project rollup's per-toolpath progress still reaches no operator surface, and `G-OPTCANCELPARTIAL` stands. No whole-core suite and no heavy gate: operator ruling 2026-09-11; debug builds only, `target/release` untouched: §27 ruling 4. |
| WP25 | G-NOMCPTESTS: gate `controller/results_parity_tests.rs` on the `mcp` feature; non-MCP all-targets clippy joins the lint gate (plan §30 ruling 4) | DONE (`ba5b24af`) | **No sentry: the compile is the proof** (plan §30 ruling 4) — a feature configuration is a build property, and a unit test in one configuration cannot measure another. BEFORE, `cargo clippy -p rs_cam_viz --no-default-features --all-targets -- -D warnings` at `af69ee5f`, **exit 101**: `error[E0433]: failed to resolve: could not find mcp in app` at `results_parity_tests.rs:327:32` and `:408:32`; `error[E0599]: no method named build_mcp_diagnostics found for struct controller::AppController<B> in the current scope` at `:265:27`, `:339:27` and `:418:27`; `error[E0282]: type annotations needed` at `:268:20`, `:342:20` and `:426:20`; `error: could not compile rs_cam_viz (lib test) due to 8 previous errors`. AFTER, the same command: **exit 0** (the one `warning:` line is the pre-existing `nom` / `quick-xml` future-incompat note). The fix adds `#[cfg(feature = "mcp")]` to the three MCP tests and **to those three alone**. Ruling 4 permits "module or call sites", and the module declaration is the WRONG place here: the other two tests, `gui_drain_writes_findings_and_spans_into_session_results` and `gui_mode_toolpath_diagnostics_see_generation_stats`, are the STORE half of `G-RESULTS` — they read `session.results` and `diagnose_toolpath_with_trace`, they name no mcp symbol, and neither errored, so a module gate would have stopped them compiling without the feature for no reason. They now run in BOTH configurations: `cargo test -p rs_cam_viz --no-default-features --lib results_parity` reads **2 passed**, 0 failed. Nothing is weakened with the feature ON: `cargo test -p rs_cam_viz --lib results_parity` still reads **5 passed**, 0 failed, and all three MCP tests run. No production code moves and `Cargo.toml` is untouched (6 insertions in one file). Suites, all 0 failed: whole viz crate `cargo test -p rs_cam_viz -q` **679 over 47 binaries**, unchanged from WP23; viz `--lib results_parity` 5 (defaults) and 2 (no-default-features). `cargo fmt --all -- --check` clean, no fold needed. `cargo clippy --workspace --all-targets --features rs_cam_core/heavy-tests -- -D warnings` exit 0. The non-MCP all-targets clippy command is now recorded in the verifier lint gate. No whole-core suite and no heavy gate: operator ruling 2026-09-11; debug builds only, `target/release` untouched: §27 ruling 4. |

### Programme checkpoint — closing core dev loop (2026-09-13)

Plan §27 ruling 2 reserves ONE capped run of the whole core suite for the
programme close. It ran at `15aa896f`, after WP19, WP22, WP14b and WP15b
landed: `nice -n 10 env CARGO_BUILD_JOBS=2 cargo test -p rs_cam_core -q
--no-fail-fast -- --test-threads=2`, 10 minutes wall, 314 binaries. Log:
`scratchpad/core_dev_loop_close.log` (session-local).

**Totals: 3863 passed / 4 failed / 272 ignored.** The WP1 run of the same
loop read 3777 / 1 / 271.

All four failures are PRE-EXISTING. Each was reproduced at `2bddd689`, the
revision before WP15b, in a detached checkout of the main tree.

- `adaptive_feed_modulation_pipeline_f036b::modulation_raises_cutting_chipload_toward_band`
  — RED BY DESIGN (`planning/ui_fix_2026-09-09/reports/J2.md`):
  `flag-ON median chipload 0.0221 should land in band [0.0320, 0.0550]`.
- `adaptive_feed_modulation_pipeline_f036b::modulated_path_never_emits_below_min_chipload`
  — `F-036b AB5: modulated IR has 21 of 126 modulated moves below the LUT
  band floor (worst = 0.0215 mm/tooth, band floor = 0.0304 mm/tooth,
  band.start = 0.0320, commanded feed = 770 mm/min)`. The figures are
  IDENTICAL at `2bddd689`. This is the same finding as the arm above, seen
  from the other side, and it is NOT ledgered anywhere else. The WP1 run
  recorded one red in this binary, not two, so the second arm went red
  between WP1 and here and no package caught it.
- `perf_golden_sim_metrics::sim_metrics_match_golden` — G-PERFGOLDEN2D, 35
  fields, ledgered on the WP22 row.
- `replace_toolpath_config_gates_on_the_signature::the_row_declares_its_surfaces`
  — `the CLI writes a whole job file, so it reaches no live config`. The
  row's `cli` column reads `Reach::Reached` and the arm asserts `Skip`.
  WP15a flipped four `cli` columns to `Reached` and this arm was not
  restated with them. `git diff 2bddd689 HEAD -- crates/rs_cam_core/src/session/command.rs`
  is empty and the arm's own text is untouched by WP15b, so the stale
  claim is WP15a's to restate.

The verifier FIXED NOTHING in this step, by instruction. The two
unledgered reds — the second `f036b` arm and the stale
`ReplaceToolpathConfig` `cli` claim — are the operator's to rule on.

### Live smoke pass on the release build — 2026-09-13 evening

Operator's request after the programme closed: nothing had been seen on
screen since 2026-09-11. Driven over MCP against `target/release/rs_cam_gui`
built 17:30 at `38334997`, project `~/Downloads/wanaka200/wanaka200.toml`
(2 setups, 9 toolpaths, 661 k triangles). Screenshots in the session
scratchpad (`smoke_01..15`). Read-mostly; every write went to the scratchpad.

Verified on screen, in order:

- Load: 9 rows, two drills auto-regenerated, load warnings window.
- Generate Back Rough (24 527 moves) and the Setup 1 rest ladder (Rivers,
  Lakes, each waiting on simulated stock, as designed).
- Simulate at 0.5 mm: Simulation workspace, per-op rows, timeline, the
  advance-per-tooth strip on Back Rough, drill gates on the pin drill.
- Save to a scratch path: the simulation SURVIVES the save (WP17).
- Export: combined program written; byte-identical body to the split
  Setup 1 file (datum header aside).
- `set_toolpath_param` on Rivers: `stale_toolpaths [4, 5]`, the chain is
  stamped stale on screen, the MCP toast appears, the GUI selects the row.
- WP24 on screen: the workspace bar shows `Optimize Back Rough — N s` with
  a spinner and a Cancel in BOTH the Simulation and Toolpaths workspaces;
  the status bar reads `JOB running · Optimize toolpath #1 · N s`; a
  workspace switch during the run applied; a second Optimize request was
  refused with the warning toast `Optimize Back Rough is already running —
  one Optimize run at a time.`; the Optimize window is a plain window with
  its own Cancel. A project_curve run returns at once with `No improvement
  found` (no geometry knobs), which is correct.

Findings, ledgered below and NOT fixed (operator's word, "a ledger row, not
a fix"): G-MCPSIMMIRROR, G-EXPORTEMPTYSETUP, G-FRESHNESSDISAGREE,
G-DIRTYONLOAD. Pre-existing and out of scope: six rapid-through-stock
flags on Back Rough at the pin and hole columns (Z 33.0 → 11.8), which the
operator should read before cutting this program.

## Do these before the phases. They are defects, not refactors.

The rows below retain the original audit evidence. N1/N3's current execution
states above supersede their historical descriptions.

| ID | What | Evidence | Size |
|---|---|---|---|
| **N3 — DONE (`069a2314`)** | **Historical: STEP unit scale is DROPPED on the project loader.** `rs_cam_cli run --units inches part.step` cuts geometry **25.4x wrong**. Two lanes confirmed independently. `project_file.rs:617` computes the scale; the STL, DXF and SVG arms pass it; the STEP arm (`:665-678`) never reads it. Lane B read `truck-stepio` 0.3.0 and found its READER performs **no unit conversion at all**, so `ModelUnits` is the only unit conversion a STEP file gets in this product. Not a double-scale: the interactive door is correct and the project door is wrong. Reachable on three doors; the GUI alone cannot create such a record (`import_step_path` hardcodes 1.0, `rescale_model` refuses STEP). | `core/session/project_file.rs:665-678` vs `core/io.rs:31,108` | **ONE LINE**, plus a STEP row in `model_units_survive_reload_g_unitsreload.rs`, which today has ZERO `step` hits. G-UNITSRELOAD closed the third divergence in this loader pair and left the fourth. |
| **N1 — DONE (`d4e1154b`; sentry `2687b82b`)** | **Historical: CLI export may emit a DISABLED operation's toolpath.** `set_toolpath_enabled` calls `invalidate_output_dependents`, not `invalidate_result_chain`, so a disabled operation keeps its cached result on purpose. Core's export phase collect discards the `ToolpathConfig` and never reads `enabled`. The GUI and MCP routes filter; `rs_cam_cli project --emit-gcode` and `rs_cam_cli run` appear not to. **THIS IS A READ, NOT A REPRODUCTION** — reproduce before believing it. No test pins either behaviour. | `core/session/mutation.rs:339`, `core/gcode/mod.rs:328` | Small, once reproduced. Wrong-cut class if real. |
| **N2 — DONE (`736a2959`; sentry `8634acf1`)** | **G-DRILLTIME is closed on one path and live on another.** `apply_kinematics_cycle_time` integrates every toolpath including drills. The post-modulation retime (`session/compute.rs:3183-3257`) resets `project_total` to zero and folds over `trace.toolpath_summaries` — the engagement population, which a drill never joins. It never rewrites `trace.toolpath_runtimes`, and that is what `readiness::toolpath_cycle_time` reads FIRST for the readiness panel, pre-flight gate, export wizard and setup sheet. Needs a configured machine kinematics block. No sentry covers it: the one test exercising the retime has no drill in its fixture. **`CLAUDE.md`'s G-DRILLTIME entry is therefore now half-true and must be amended when this is fixed.** | `core/session/compute.rs:3183-3257` vs `core/compute/simulate.rs:1464-1533` | Medium. Two writers of one field. |
| **N4 — DONE (`44c68add`; sentry `6d1d2026`)** | **A parity guard that cannot bite.** `gui_and_mcp_diagnostic_ids_match` hand-rebuilds the session's inputs, and its "GUI arm" calls `ResolvedHeights::from_context` — the MCP-route call. **It compares MCP against a replica of MCP.** Recorded earlier as J8.4 "a mirror that became a parallel copy"; it is worse than that. | `viz/ui/properties/operations/mod.rs:2657` | Small. Rewrite or delete. |
| **N5 — DONE (`1e4373aa`; sentry `3b8cd298`)** | **`set_toolpath_param` accepts and silently discards a write on many operations.** The named `"stepover"` arm returns `Ok(())` on every operation whose config omits the setter — **11 operations**. `depth_per_pass` the same on **14**. The six named arms also bypass the DR-LIVE `ParamRange` gate entirely, which lives only in the generic `_` arm. `angular_step` / `point_spacing` have no registry range and are divisors. | `core/session/compute.rs:509`, `radial_finish.rs:96,108` | Part of Phase 4B, but the silent-success half is a defect now. |
| **N6** | `set_drill_selected_holes` is a second narrow mutation path the audit did not name, sitting beside a wide one. Closed by WP8 (`e079e192`). | `core/session/mutation.rs:944` vs `:911` | Small; folds into Phase 1A. |
| **N7 — DONE (`c6af7f4b`; sentry `c1191963`)** | **The modulation retime integrates on an unguarded kinematics fallback.** `apply_adaptive_feed_modulation` takes `self.machine.effective_kinematics()` with no `is_some()` guard, and that falls back to `generic_wood_router`. With modulation ON (the default) and `kinematics: None` the retime overwrites `trace.summary.total_runtime_s` and stamps `runtime_by_intent = Some(..)`, so `readiness.rs` labels the toolpath `MachineModel` on a machine that has no kinematics block. `machine.rs:141-146` says `None` must keep live-sim runtime byte-identical. `f036b.rs:281-284` still claims an `is_some()` guard that no longer exists. Found by the N2 scout 2026-09-10 (read, not run). Behaviour decision, not a fold bug: needs an operator ruling before a fix. | `core/session/compute.rs:3055`, `machine.rs:147-150`, `viz/ui/readiness.rs:507-509` | Small once ruled. |
| **N8** | **The core/MCP diagnostics route projects heights and drops every pin.** `ResolvedHeights::from_context` writes `top_z = stock_top_z`, `bottom_z = stock_bottom_z`, `feed_z = stock_top_z`, `retract_z = clearance_z = safe_z`, so on that route `geom.bottom_above_top_z`, `geom.feed_z_below_top_z` and `geom.clearance_z_below_retract_z` can never fire and `geom.depth_beyond_stock` misses a pinned Top Z (J8.1). The GUI's `diagnostics_heights` resolves the real `HeightsConfig`. Root cause is a missed call: `tc.heights` is in scope at the call site. Found by the N4 scout 2026-09-10 (read, not run). Fixed inside the N4 work package. | `core/diagnostics/adapters/from_static_checks.rs:78-90`, `core/session/compute.rs:4265` | Small; in N4. |
| **N9 — DONE (`52fdd7dd`)** | **The ribbon and the session route disagree on preconditions and model refs by design.** `collect_diagnostics` passes `preconditions: None` and `model_refs: None`; `diagnose_toolpath_with_trace` passes `Some(..)`. A Rest with no prior op exposes it. The N4 sentry excludes this family and says so. Found by the N4 scout 2026-09-10. | `viz/ui/properties/operations/mod.rs:2281,2285` vs `core/session/compute.rs:4291-4292` | Small; viz holds the session and can pass both. |
| **N12** | **Seven generation-input divergences between the GUI worker door and the session door** (Finding 1, made concrete). Both doors call `execute_operation_annotated_with_regions`, but everything before it is written twice. (1) `ToolpathConfig::face_selection` is read by the GUI only: `controller/events/compute.rs:354-372` derives polygons from the picked faces and `:536-542` anchors an auto `top_z` to the face; core's `resolve_generation_inputs` never reads the field. (2) A `BoundarySource::FaceSelection` boundary confines to the face on the GUI door (`worker/execute/mod.rs:770-775`) and to the stock rectangle on the session door (`session/compute.rs:2084-2115`). (3) Feed-optimisation stock: viz builds it (`worker/helpers.rs:41-52`), core passes `None` (`session/compute.rs:1712`); live on a fresh-stock 2D Pocket with the shipped default `feed_optimization: true`. (4) Dressup stock-top frame: core `emission_stock_bbox.max.z`, viz `req.heights.top_z`; diverge on a pinned Top Z. (5) Entry-probe index: core reuses the memoised index, viz builds a fresh one and gates on `entry_style`. (6) Four `HeightContext` builders; the two generation ones have no polygon fallback. (7) The single-polygon clip is open-coded in viz. Found by the Phase 0 generation-parity scout 2026-09-11 (read, not run). Phase 3 owns the fix; Phase 0 pins the shape. WP11b (`4b53576b`) lands items 1-9 and the two doors share one assembly; the row is NOT closed — a TENTH divergence stands (the GUI simulation state is not the session's, so `start` refuses every GUI rest operation). See the WP11b row. Closed by WP11b + fix (`276e5c13`): the AdoptSimulation row gives the GUI process one simulation state, so the tenth divergence above is closed too. | `viz/controller/events/compute.rs:181-760`, `viz/compute/worker/execute/mod.rs:43-237,760-908`, `core/session/compute.rs:1090-1993` | Phase 3. |
| **N13 — DONE (`4fe9972a`; sentry `b7503d8a`)** | **The feeds Apply funnel invalidates nothing.** `controller/events/mod.rs:936-949` (GUI Apply all, project rollup, MCP `apply_feeds`) writes `tc.operation` through `toolpath_configs_mut()` with no `drop_result`, no `invalidate_result_chain`, no `invalidate_toolpath_inputs`, and `session.simulation` is not cleared. `state/freshness.rs:79-97` derives freshness from `core_has_result` and ignores `stale_since`, so the op reads `Current` and export (`io/export.rs:281`) emits the PRE-apply geometry and feed words. The 500 ms auto-regen sweep masks it only on the 12 op specs with `default_auto_regen: true`. Same class as the pre-G-FRESHSTATE inspector defect. Found by the Phase 0 mutation-paths scout 2026-09-11 (read, not run). Wrong-feed export class: fix now. | `viz/controller/events/mod.rs:936-953`, `viz/state/freshness.rs:79-97` | Small. |
| **N14** | **Undo/redo and optimizer apply invalidate one index; the setter and the inspector invalidate the chain.** `apply_toolpath_param_snapshot` (`core/session/mutation.rs:1248`) ends at `drop_result` + `simulation = None`, so an undone or optimizer-applied edit leaves downstream `FromRemainingStock` results standing. `set_toolpath_param` (`:236-238`) and `invalidate_toolpath_inputs` (`:1385-1391`) walk the chain. Undo carries no `feeds_provenance` (`viz/state/history.rs:29-37`). `AUDIT.md:68`'s claim that `replace_toolpath_config` is narrow is STALE: it is wide since R0.1 §4.3 (`mutation.rs:1222`). Found 2026-09-11 (read, not run). Phase 1A with N6. Closed by WP8 (`e079e192`). | `core/session/mutation.rs:1248` vs `:236,1385` | Phase 1A. |
| **N15** | **A third staleness model: `compute_stale_set(ToolpathParamChanged)` returns exactly one index** (`core/session/compute.rs:53-58`), no chain walk, so the MCP reply's `stale_toolpaths` under-reports what core dropped, and on N13's path it is the only signal. `compute_stale_set_for_toolpath_param_returns_single_toolpath` (`compute.rs:5355`) PINS the narrow answer; Phase 1A must change that test, not only the code. Found 2026-09-11. Phase 1A. **WP1 (`90c8e90e`) renamed the sentry arm to `n15_apply_reports_the_set_the_setter_dropped`, which compares `apply(..)?.stale` against the dropped set, and deleted the vacuous unit test `compute_stale_set_for_toolpath_param_returns_single_toolpath`; the narrow `compute_stale_set` answer stays pinned under `n15_compute_stale_set_still_reports_one_index_pinned_divergence`.** **Closed by WP4 (`1c399a71`) for every row except `add_toolpath_via_gui` and `apply_feeds`: 29 MCP mutation rows now read `Effects.stale`, and the two remaining producers keep `compute_stale_set`, so the function and the pinned-divergence arm both stay.** | `core/session/compute.rs:53-58,5355` | Phase 1A. |
| **N10 — DONE (`3f8d97bb`; sentry `1634bb13`)** | **`radial_finish` divides by two unranged params.** `angular_step` and `point_spacing` are `ParamDef::required(.., "f64")` with no `ParamRange`; `0.0` saturates `num_spokes` to `usize::MAX` and overflows `Vec::with_capacity`; a negative value gives an empty toolpath with no error. Adding a range row is a numeric threshold, so it needs an operator ruling; the precedent shape is `peck_depth`'s `required_ranged(.., ParamRange::greater_than(0.0), ..)`. Found by the N5 scout 2026-09-10. Phase 4B. | `core/radial_finish.rs:96,108`, `catalog.rs:1933-1940` | Small once ruled. |
| **N11** | **The Apply/pill funnel discards a Stepover apply on a no-field op by the same mechanism as N5.** `suggest.rs:1084-1085` calls the defaulted `set_stepover` and does not read whether it wrote. Found by the N5 scout 2026-09-10. Phase 4B. | `core/feeds/suggest.rs:1084-1085` | Small. |

---

## Verified finding verdicts

| # | Finding | Audit rating | **Verified verdict** | Correction to the audit |
|---|---|---|---|---|
| 1 | Core and GUI assemble separate generation pipelines | High | **STILL TRUE** | Exactly TWO sites. **MCP is not a third** — it pushes the GUI's own `AppEvent::GenerateToolpath`. 14 comments in the viz worker describe themselves as mirroring `session/compute.rs`. |
| 2 | Invalidation is a convention, not an enforced contract | High | **PARTLY CLOSED** | Half the first bullet is DEAD: `replace_toolpath_config` now propagates AND has **zero callers**. `apply_toolpath_param_snapshot` is still narrow (6 production sites). Raw mutable accessors unchanged (**73 non-test sites, counted**). F2.5's `invalidate_tool` bypass is fixed viz-side; undo's toolpath-param arm still carries it. **A CONSTRAINT THE PLAN MUST CARRY:** `optimize/candidate.rs:428-430` documents a live DEPENDENCE on the narrowness. The transaction cannot be "always invalidate the chain". |
| 3 | Computed results have competing owners | High | **PARTLY CLOSED** | The export fallback the audit called the architectural problem is now guarded by `StaleResultPolicy`, default `Refuse` (G-STALEXPORT, `66d2c232`). Two stores remain but the split is now DELIBERATE (G-LATERESULT, `9544ca75`). |
| 4 | Export duplicates program assembly | High | **STILL TRUE** | **THREE sites, not two.** The CLI job-file pipeline is a third. All three stated differences hold. |
| 5 | Timing corrections update selected summaries | High | **PARTLY CLOSED** | G-AIRDENOM closed the per-toolpath half. Four summaries stay mixed-base, exactly as `CLAUDE.md` records. **FOUR partial provenance vocabularies already exist** (`FeedsProvenance`, `CycleTimeBasis`, `ToolpathKinematicRuntime`, `RebasedCuttingTimes`) — collapse them, do not add a fifth. |
| 6 | Model import duplicated below convergence | Medium | **STILL TRUE — and it hides N3** | Extension recognition duplicated **four** ways. `winding_report` is never measured on a project load. There is a **third** import door (`LoadedModel::from_file`) and a fourth legacy dispatch in `viz/io/project.rs`. F4.4 unified a DIFFERENT pair (the three in-place refresh doors). |
| 7 | Parameter support and validity have multiple authorities | High | **STILL TRUE and wider** | See N5. G-SCHEMAENUM closed advertised-subset-of-accepted only; the reverse direction and "the string is consulted at validation" are both open. |
| 8 | Drilling reconstructs its analytical model after generating motion | High | **STILL TRUE — but the audit's VERB is wrong** | It does NOT derive from emitted motion. `build_drill_op_for_config` derives from the CONFIG, in parallel with `generate_drill`. Holes and cycle expansion are already shared. **Five expressions written twice.** Size: 2 crates, 2 call sites, 3 functions. **Re-rate this: it is Small, not High.** |
| 9 | Shared run emission bypassed for annotations | Medium | **STILL TRUE and wider** | **SIX** hand-rolled emitters. The audit missed `adaptive/path.rs:1516-1598`, same omission and same motive. `steep_shallow.rs:473-505` duplicates with no annotation motive and is a separate item. Severity is masked, not absent: `tsp::rebuild_group` regenerates framing rapids and `optimize_rapid_order` defaults true. |
| 10 | Offset failure handling remains opt-in | Medium | **STILL TRUE** | **13 production sites** on the dropping door across 7 files; 8 already migrated. `adaptive/path.rs` alone holds 5. **Correction the plan must carry:** two of three failure classes are `debug_assert!`s in the dependency, so a RELEASE build cannot fully separate failure from collapse even after every caller migrates. The Policy row overstates what the door can deliver. |
| 11 | Vendor routing encodes incidents | Medium | **STILL TRUE** | `lut_query_for` is 30 lines: three special cases plus a pass-through. Its own doc names five finish ops with the same hole and says they are not routed. `LookupQuery` carries no provenance. The five named holes have **no sentry at all**. |
| 12 | New caches copy the same infrastructure | Medium | **STILL TRUE and UNDERCOUNTED** | **THREE** weak-identity memos, not two. `geom_cache.rs` is the same pattern and the OLDEST. ~250 non-blank lines of memo infrastructure across three files. The generic **must carry `peek`**, and `geom_cache`'s one-entry-three-slots shape does not fit a one-key-one-value generic. |

---

## Phase status, after verification

| Phase | Subject | Status | Note |
|---|---|---|---|
| 0 | Contracts and executable evidence | **ROUGHLY HALF PAID** | Of eight characterization requirements: 1 well covered, 3 substantially covered, 3 where adjacent tests exist but do not assert the stated contract, 1 barely started. See below. |
| 1A | One mutation contract | TODO | Finding 2. Carry the `optimize/candidate.rs` constraint. Fold in N6. |
| 1B | One artifact owner | TODO | Finding 3, de-scoped: the export fallback is already guarded. |
| 1C | Migrate consumers | TODO | |
| 2 | Centralize export planning | TODO | Finding 4, **three** sites. Settle N1 first. |
| 3 | Unify the full generation pipeline | TODO | Finding 1. The largest phase. Needs Phase 1. |
| 4A | Canonical model import | TODO | Finding 6. **Fix N3 first and separately** — it is one line and it is a live wrong-size defect. Four extension dispatches, four doors. |
| 4B | Canonical parameter contracts | TODO | Finding 7 / N5. |
| 5A | Resolve drilling once | TODO | **Re-rated to Small.** Finding 8's verb is wrong. |
| 5B | Shared annotated run emission | TODO | Finding 9, six emitters. |
| 5C | Structured offset outcomes | TODO | Finding 10, 13 sites. Carry the release-build caveat. |
| 6A | One timing view | TODO | Finding 5. Fix N2 first or fold it in. |
| 6B | Explicit vendor lookup use | TODO | Finding 11. |
| 7 | Cache mechanics | TODO | Finding 12, three files. |
| 8 | Whole-system acceptance | TODO | |

### Phase 0 — what is already paid

Roughly 30 sentries were added by the UI programme and the core track in the
two days before this tracker, on top of the F-024..F-040 regression net and
the `_litmatrix_*` feeds suite. The full heavy gate is green at **299
binaries, 3773 passed**, with `adaptive_feed_modulation_pipeline_f036b` red by
design.

The largest genuine gap is **core-versus-GUI export parity**: exactly one test
crosses the doors (`g_modexport`) for one property. Coolant, per-operation
RPM, tool changes and datums have none.
**Correction 2026-09-11 (read, not run):** `g_modexport` calls only the GUI
door (`export_gcode_from_session_with_policy`); no viz test calls
`export_gcode_checked`. Before Phase 0's parity test, NO test compared the two
doors on one session. The registry-driven-coverage ask is
mostly paid — nine binaries already drive off `for_each_op!`.

**Ten source-level string-pin sentries exist.** Three name their own weakness
in their headers; seven do not. A source-level pin is weaker than a test that
runs the path, and Phase 0's job is evidence a refactor cannot silently break.
Treat those seven as gaps, not coverage. N4 is the worst case of the class.

- 2026-09-11: core-vs-GUI export parity is now pinned by
  `crates/rs_cam_viz/tests/export_parity_core_vs_gui_p0.rs` (`1e497aa6`):
  RPM, tool changes and datums byte-identical; coolant divergence pinned
  pending P0-D1.
- 2026-09-11: the four param write paths are pinned by
  `crates/rs_cam_core/tests/mutation_paths_invalidate_alike_p0.rs`
  (`720c95e7`); N6, N14 and N15 divergences asserted with Phase 1A flip
  notes.
- 2026-09-11: the GUI worker door and the session door are pinned by
  `compute/worker/gen_parity_p0_tests.rs` (`41c3b6cd`): one geometry with
  feed-opt off; the feed-opt-stock divergence (N12 item 3) asserted with a
  Phase 3 flip note; N12 items 1, 2 and 4-7 unpinned.
- 2026-09-11: a disconnected Scallop finish is pinned by
  `crates/rs_cam_core/tests/disconnected_finish_retract_structure_p0.rs`
  (`3fa62094`): every inter-island transition retracts (hookup cap 3.0
  against a 17 mm gap), descents tag `EntryPlunge`, and no fed move leaves
  the islands.

---

## What earlier work already closed, per finding

**Finding 2.** G-FRESHSTATE made stock, tool and model edits invalidate on
both routes and put `tool_id`/`model_id` into `generation_inputs_signature`.
F2.12 gave the holder-clearance verdict the edit-counter stamp every sibling
row had. F2.13 made it declare how much of the job it examined. F4.7 made
`rescale_model` invalidate. `replace_toolpath_config` propagates and is dead.

**Finding 3.** G-STALEXPORT guarded the fallback; G-LATERESULT made the split
deliberate.

**Finding 5.** G-AIRDENOM rebased cutting times per sample.

**Finding 6.** F4.4 unified the three in-place refresh doors into
`LoadedModel::adopt_geometry` with an exhaustive destructure. **Different pair
from the one the audit names.**

**Finding 7.** G-SCHEMAENUM fixed six advertised enum values no config could
hold and added a generic round-trip sentry.

---

## Provenance, and one prior mistake to avoid

The pi command `/techdebt-orchestrate` pointed at
`planning/architectural_refactor_2026-06-06_v2.md` until 2026-09-10. **That
plan is COMPLETE** — every work item T0..T16 landed 2026-06-07, verified by
content and not by its own tracker. It is also a CORE refactor, not a GUI one.
An account pointed at it would have found nothing to do. The command now
points here.

`AUDIT.md` and `PLAN.md` were produced by gpt-5.6-astra on 2026-09-09, lived
only inside a pi session transcript, and were recovered on 2026-09-10.

## Related open items

- **G-FEEDOPTPLUNGE — FIXED by WP22 (`d880ca1a`, 2026-09-13; sentry `e9289426`).** The feedopt pass caps a geometric plunge at the operation's `plunge_rate` (plan §27 ruling 3). Measured red: 18 of 18 plunges at 2.00x on the sentry pocket; `perf_golden_sim_metrics` plunge leading-edge speed moved from 1000 to 400 mm/min on its 2D fixture. The original observation follows.
- **G-FEEDOPTPLUNGE (observed 2026-09-12 by the WP18 verifier, not acted on).** The feed
  optimisation dressup (`feedopt::optimize_feed_rates_inner`, `feedopt.rs:~164`) writes
  `nominal * factor` onto every cutting move and reads no move's own feed, clamped to
  `[nominal*0.5, max]`. A plunge descent that reads full engagement is therefore rewritten
  to the cutting nominal, up to about twice its plunge rate. The P3 geometric plunge guard
  lives in `adaptive_feed_modulate`, not in this pass. Pre-existing; needs an operator
  decision and a sentry before any change. Evidence: `feed_optimization_refusals_wp18.rs`
  arm (c) measured ON 72 vs OFF 114 off-commanded moves on the P0 pocket. WP21 (`4aeaeda3`)
  removed the panic; the plunge-rate overwrite itself stays open.

- **G-NOMCPTESTS (observed 2026-09-13 by the WP23 verifier, CLOSED by WP25 (`ba5b24af`)).**
  `cargo clippy -p rs_cam_viz --no-default-features --all-targets -- -D warnings`
  cannot pass: the in-crate module `controller/results_parity_tests.rs`
  (`4bfed4c4`, 2026-08-13) calls the mcp-gated `build_mcp_diagnostics` and
  `app::mcp::viz_project_evidence` with no feature gate (8 errors, identical
  at `bf77aac4`). The lib compiles clean without the feature since WP23
  (`07eddc1e`). WP25 gates the three MCP tests — not the module, because the
  other two are the store half and name no mcp symbol — and that command now
  exits 0 and joins the verifier lint gate.
- **G-F036B-FLOOR (observed 2026-09-13 by the closing core dev loop; FIXED by WP26 (`610f8a58`): the arm now diffs the pre- and post-modulation IR per index and skips `PlungeRate`-bound moves; binary reads 9 passed / 1 failed, the band arm red by design at 0.0221).**
  `adaptive_feed_modulation_pipeline_f036b::modulated_path_never_emits_below_min_chipload`
  is RED beside the red-by-design band arm: `21 of 126 modulated moves below the
  LUT band floor (worst = 0.0215, band floor = 0.0304, commanded feed = 770)`.
  Figures identical at `2bddd689`, before WP15b. WP1 recorded ONE red in this
  binary; no later package ran it, so the second red's first appearance is NOT
  MEASURED. Same family as the J2 finding; needs an operator reading before any
  change (the floor is a feeds quantity).
  **Scouted 2026-09-13 (brief `scratchpad/g_f036b_floor_brief.md`); operator
  ruled: fix the test arm → WP26.** The scout corrects two statements above.
  (1) The first appearance IS bracketed: AB5 was green at the N2 checkpoint
  and at the WP1 loop (`90c8e90e`); the one commit in the window that reaches
  this door is `4b53576b` (WP11b), which replaced the core generation door's
  literal `None` with `feed_opt_stock.as_mut()`, so the feed-optimisation
  dressup first ran on that door. (2) It is NOT the same finding as the band
  arm: J2 §4c names the band arm only, with a different cause (entry-segment
  multiplication) and a different fix. The mechanism: the arm's proxy at
  `...f036b.rs:598` counts a move as modulated when its feed differs from
  nominal, and since WP11b the dressup writes per-move feeds on the same
  door, so 21 dressup-written moves that the modulator passed through
  unchanged are counted. The solver floors every feed it writes at
  `band.min × rpm × flutes`; the only sub-floor writer is the P3 plunge guard
  at 0.0107 mm/tooth, and the worst reading is 0.0215, so no counted move is
  guard-capped. No feeds quantity moves; the floor clamp is untouched.
- **`set_toolpath_param` kept as the in-crate proof (orchestrator ruling
  2026-09-13).** After WP15b it is `pub(crate)` with no production caller and
  33 in-crate test callers, under `#[allow(dead_code)]`. It stays: it is the one
  place that shows the wrapper and the `apply` door are one route
  (`setters_have_rows_wp15a::every_wrapper_exemption_calls_the_door`).
- **G-PERFGOLDEN2D — CLOSED by an operator re-bless (2026-09-13).** Operator ruling: re-bless now. `UPDATE_PERF_GOLDENS=1` ran once, capped; the golden moved 41 lines (fields listed in the commit body); the clean re-run reads 5 passed. Which commit moved the fixture stays NOT MEASURED. The original observation follows.
- **G-PERFGOLDEN2D (observed 2026-09-13 by the WP22 verifier, not acted on).**
  `perf_golden_sim_metrics::sim_metrics_match_golden` is RED at base `61c16b75`
  with 36 fields off the golden (35 with WP22; every plunge field and
  `triage_action_count` move TOWARD the golden). The golden was last written at
  `d0aeee02`; last explicit re-bless `691304b4`. No planning note records this
  red. A re-bless (`UPDATE_PERF_GOLDENS=1`) is an operator decision: it writes
  measured numbers. NOT MEASURED: which commit moved the 2D fixture.

- **G-MCPSIMMIRROR (observed 2026-09-13 in the live smoke pass, not acted on).**
  The MCP `McpRequestKind::Core` door applies its command at
  `crates/rs_cam_viz/src/app/mcp.rs:358` (`session.apply(command)` then
  `describe_core`) and mirrors NEITHER `Effects::simulation_cleared` into
  `invalidate_simulation()` nor the WP19 sim-clear ruling (§28.1). Measured on
  wanaka200: `set_toolpath_param(4, feed_rate)` after a simulation. The session
  DROPPED its simulation (core reports `simulation_cleared` at
  `command.rs:2695`; the next `generate_toolpath(4)` refused with "no simulated
  remaining-stock snapshot is available"), while the viewport KEPT the last run
  with the old "Results may be stale (params changed) — re-run sim" banner,
  the `SIM` pill and the collision count. The session and the viewport no
  longer hold ONE simulation on this door (the WP11b / N12 item 10
  invariant). The same request through the feeds Apply funnel (`apply_feeds`)
  DID clear the viewport (status bar `SIM` pill and Simulation badge gone, rows
  fell back to planned times), so the gap is the Core door alone; the three
  WP19 apply doors (`apply_controller_command`, `apply_quietly`,
  `apply_panel_command`) are not on this route. `set_toolpath_enabled` shows
  the same shape (stale banner, sim kept). Fix shape: mirror
  `outcome.effects.simulation_cleared` at the one apply site, sentry in
  `crates/rs_cam_viz/tests/` driving the Core door with a session simulation
  present. NOT MEASURED: whether `set_tool_param`, `set_stock_config` and the
  other Core rows behave the same (same site, so presumably yes).
- **G-EXPORTEMPTYSETUP (observed 2026-09-13 in the live smoke pass, not acted
  on).** `export_gcode(split_setups: true)` with every Setup 2 toolpath
  DISABLED wrote `..._1_Setup_1.nc` and then refused the whole export:
  `Export failed (setup 'Setup 2'): No computed toolpaths in setup 'Setup 2'`.
  The reply says failed; a file is on disk. The single-file export of the same
  project succeeded and its body is byte-identical to the Setup 1 file. Either
  a setup with nothing enabled is skipped (the contract says a disabled
  toolpath is skipped) or the refusal comes before any file is written.
  Operator's call.
- **G-FRESHNESSDISAGREE (observed 2026-09-13 in the live smoke pass, not acted
  on).** With a simulation the inspector called `✓ live` (after a fresh
  `run_simulation` and a regenerate of the edited row), the Optimize window
  opened on Back Rough showed `⚠ Results stale (params changed) — re-run sim`
  (its `FreshnessGate::banner`). Two freshness readings on one screen
  disagreed. Likely cause: `stale_since` stamps on DISABLED rows (Lakes, 3D
  Finish 6) that a re-run does not clear; NOT MEASURED.
- **G-DIRTYONLOAD (observed 2026-09-13 in the live smoke pass, not acted on).**
  The status bar read `Modified` immediately after `load_project`, before any
  edit; the load auto-regenerated two drill rows. `GuiState::mark_edited`
  is the only writer of `dirty`; which caller runs on load is NOT MEASURED.
  Consequence: a close right after a load prompts for unsaved changes that do
  not exist, and `load_project` refuses to replace a project that was only
  loaded.
- **G-OPTCANCELPARTIAL (observed 2026-09-13 by the WP29 scout, not acted on).**
  The Optimize window says "hit Cancel to stop and keep partial results"
  (`crates/rs_cam_viz/src/ui/optimize_modal.rs:83-90`). Both cancel routes
  discard them: `spawn_job_lane` replaces the answer with
  `Err(ComputeError::Cancelled)` whenever either cancel flag is set
  (`crates/rs_cam_core/src/compute/worker.rs:1336-1341`), and
  `deliver_gui_job` maps that to `OptimizeRunStatus::Failed("Cancelled")`
  (`crates/rs_cam_viz/src/controller/events/compute.rs:1168`), so core's
  `finalize_partial` outcome never reaches the window. Either the sentence
  goes or the lane returns the partial outcome on cancel. Operator's call; the
  WP24 "two cancels are asymmetric" note is moot until this is decided.

`planning/ui_fix_2026-09-09/PLAN.md` §11 carries twelve follow-ons opened on
2026-09-10, several inside these phases: F2.14 (widen the holder check),
F4.10 / F4.11 / F4.12 (drill pick coverage), J8.1 (MCP drops a pinned Top Z),
J8.4 (now N4). Read that list before opening work packages here.
