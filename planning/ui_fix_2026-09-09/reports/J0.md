# J0 — consolidate the branches that never merged

Core-geometry account, 2026-09-10. Branch handed over: `core-consolidation`.
Worktree `/home/ricky/personal_repos/rs_cam_wt_alt`, cache
`/home/ricky/personal_repos/rs_cam_target_alt`, no shared lock used.

## Headline: the prompt's premise was wrong on three of the four branches

`PARALLEL_CORE_PROMPT.md` §J0 says four branches carry finished work and
none of them is in master. **Six of the eight commits are already in
master**, applied under different SHAs. `git cherry` reports them as
equivalent by patch-id:

```
$ git cherry master isoclip-rapid
- 0d14102e9e0c49a8ad214133ee95bbc1ecffc75f
- 9939d05e4d8684e12d6b9ad4911e62b797cb17bc
$ git cherry master ui-string-sentry
- 66ae90ad24b84eef63afb4e40264b90215855270
- df1232fdaf2efcc5cdd34da0ef446f59c7cdd0dd
$ git cherry master pipesmoke
- c25779092848c4e8de4d0b6b43db0bb520eeddb8
- 70f507137c416f3ba9718c4e746bf4fbbee9f791
$ git cherry master linking-pencil-dial
+ af658a7189cba8474926c4ec6291fd51c373372b
+ 627ef997c500135fa9700859a65ef2618985f77c
+ 2bc37470583090f078f6ca94dc2fc8f53bebc618
+ f0e04b6f6cff618a6dae2e6911707aab7f5cc16d
```

A `-` line is already upstream. The three branches are stale POINTERS, not
unlanded work. Their master twins:

| Branch commit | Master twin | Subject |
|---|---|---|
| `0d14102e` | `38f8d151` | G-ISOCLIPRAPID + G-ISOCLIPRAMPFALL |
| `9939d05e` | `fb6027fb` | G-TIERCONTINUOUS + G-OVERLAPFILL |
| `66ae90ad` | `8701a4f4` | string-hygiene sentry |
| `df1232fd` | `01ec1e41` | G-ISOCLIPENTRY |
| `c2577909` | (evidence PNGs, in master) | fourth-look screenshots |
| `70f50713` | `8854e729` | G-PIPESMOKE |

Rebasing them would have been a no-op that `git rebase` performs by
dropping every commit silently; cherry-picking them would have
double-applied or conflicted. Neither was done.

The corroborating detail, visible without running anything: `isoclip-rapid`
MODIFIES `isoclip_entry_ramp_g_isoclipentry.rs`, which is
`ui-string-sentry`'s own sentry file. G-ISOCLIPENTRY was therefore already
in the tree when `isoclip-rapid` was cut. The branch list in the prompt was
built from branch names and commit counts, not from patch identity.

**Nothing was rebased. No conflict was resolved, because none arose.**

## What actually landed

`linking-pencil-dial` is the one genuinely unlanded branch. Its merge-base
IS master (`3d88406b`) and it is 0 behind, so this was a cherry-pick onto a
fresh branch, not a rebase.

```
$ git checkout -B core-consolidation master
$ git cherry-pick af658a71 2bc37470 f0e04b6f
```

Three commits, no conflicts:

| New SHA | Subject |
|---|---|
| `e1f845dd` | feat(pencil): G-PENCILHOP — the clearance-hop cap is an operator dial |
| `10bbe41c` | measure(pencil): G-PENCILTIERS + G-FRESHLINK |
| `f84136fb` | fix(provenance): G-LINKTRACE |

### Deviation: `627ef997` was DROPPED deliberately

The fourth commit on `linking-pencil-dial`, `627ef997 chore(mcp): point
rs-cam at the prebuilt binary`, touches `.mcp.json` only. `.mcp.json` is on
this prompt's forbidden list. It is dropped, so `core-consolidation` touches
no config file:

```
$ git diff --name-only master..core-consolidation
crates/rs_cam_core/src/compute/catalog.rs
crates/rs_cam_core/src/compute/execute.rs
crates/rs_cam_core/src/compute/operation_configs.rs
crates/rs_cam_core/src/pencil.rs
crates/rs_cam_core/src/surface_link.rs
crates/rs_cam_core/src/transform_provenance.rs
crates/rs_cam_core/tests/pencil_hop_dial_g_pencilhop.rs
crates/rs_cam_core/tests/scallop_trace_survives_relink_g_linktrace.rs
planning/deep_doc_modulation_2026-09-08/PENCIL_chain_r10_rest.toml
planning/linking_2026-09-09/SPEC.md
planning/machine_kinematics_confidence_2026-09-07.md
```

That commit is a local MCP convenience (master's `.mcp.json` runs
`cargo run --release`, which blows the 30 s connect window on a cold
cache). It is NOT part of the geometry work and the operator can apply it
separately if they want it. It stays on `linking-pencil-dial`.

## Bonus: J0 closes half of J1

`narrate_regions_closed_c8` was red at master and is GREEN on
`core-consolidation` without a line of J1 work. See `J1.md`. The pin was
never stale; the link stage regressed the scallop's semantic trace and
`f84136fb` (G-LINKTRACE) restores it.

## Gates — and what is NOT run

**The FULL heavy-tests gate and the heavy-feature workspace clippy were NOT
run, on a standing operator instruction** ("please dont do the full heavy
test" / "it wlil kill my pc", 2026-09-10). The prompt asks for them once per
job; the operator's instruction overrides the prompt. **Both are OWED on
this branch** and the operator can grant them.

Run instead, in the private worktree and cache:

Run in the private worktree and cache, on `core-consolidation` at `f84136fb`
plus the J1 re-pin:

| Gate | Result | Time |
|---|---|---|
| `cargo fmt --all -- --check` | clean | — |
| `cargo clippy -p rs_cam_core --all-targets -- -D warnings` | zero warnings | 1:30 |
| `cargo test -p rs_cam_core -q` | **no failures** | 11:39 |

The core dev loop is fully green on this branch — including the two binaries
that were red at master, which is J1's subject.

## What J0 did NOT do

- Did not advance `master`. Did not open a pull request.
- Did not rebase, cherry-pick or otherwise touch `isoclip-rapid`,
  `ui-string-sentry` or `pipesmoke` — all already in master by patch-id.
  Those three branch pointers can be deleted by the operator; J0 did not
  delete them.
- Did not run the FULL heavy gate or the heavy-feature clippy (above).
- Did not verify that master's twin of each landed commit is byte-identical
  to the branch commit — `git cherry` compares patch-id, which ignores
  commit message and author but not content. A twin with an amended body is
  reported the same way as an identical one.
