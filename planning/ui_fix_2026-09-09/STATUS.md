# UI/UX fix programme — status ledger

Append-only. One line per task, dated, with state (done / blocked / partial),
commit hash, sentry name and any open question. Do not rewrite earlier lines.

| Date | Task | State | Commit | Sentry | Notes |
|---|---|---|---|---|---|
| 2026-09-09 | PLAN.md written | — | — | — | Source: planning/ui_review_2026-09-09 (R03, W03/support, IA). No tasks started. |
| 2026-09-09 21:20 | Night 1 start | — | fe18e588 | — | Orchestrator = session rs-cam-b9 (Fable). Integration branch `ui-fix-2026-09-09` from master a0d21b96. DEVIATION from PLAN §0.1 / prompt rule 1: code tasks run SERIALLY in the main checkout on task branches `ui-fix/<id>` (ff-merged after verify), not in per-task worktrees — disk has 21 GiB free and `target/debug` is 63 GiB with 286 core test binaries, so a second target path would exhaust the disk. Research agents (P0) run in parallel, read-only, no cargo. MCP server is down (user killed it); every MCP-VIEW acceptance is deferred and marked so. No GUI launched. |
| 2026-09-09 21:45 | R0.1 | done (verified) | — | — | research/R0.1.md, 269 lines. Orchestrator spot-check CODE: `properties/mod.rs::write_entry_config_to_session` (line ~3443) writes through `find_toolpath_config_by_id_mut` (session/mod.rs ~1563) and the caller block sets `rt.stale_since` + `mark_edited` only — `session.results` is never dropped on a GUI parameter edit. Claim CONFIRMED; it upgrades the freshness work: export reads the retained core result first, so a 3D manual-regen op edited in the GUI exports OLD geometry. Recommends Option A (derive `FreshnessState` from core cache + per-toolpath revision counter). Six open questions in §7 for the human. |
| 2026-09-09 21:55 | R0.5 | done (verified) | — | — | research/R0.5.md, 564 lines. Spot-checks CODE: (a) core `session/compute.rs::set_toolpath_param` (~278) DOES call `feeds_provenance.set(.., ValueProvenance::manual())` on feed_rate/plunge_rate — PLAN §2 R0.5 sentence corrected in place; (b) `components/value_row.rs` near-match compares the configured value against the RAW recommendation, so after F1.2 the pill must compare against the as-applied (clamped) value or it stays lit on a no-op — passed to the F1.2 worker. New code-read finding: modal "Apply explored values" also rewrites plunge (not live-tested). Eight open questions in §7. |
| 2026-09-10 01:11 | outage | — | — | — | Claude session limit hit at ~21:35, reset 01:10 (3.5 h lost). All seven research docs (R0.1–R0.7) were on disk before the cut. F1.1 worker was killed mid-clippy-fix with uncommitted edits on `ui-fix/F1.1` (app/mcp.rs, controller.rs, mcp_bridge.rs, new sentry). Resuming. |
| 2026-09-10 01:20 | R0.2 | done (verified) | — | — | research/R0.2.md, 478 lines. Spot-check CODE: `dressup.rs` ENTRY_CLEARANCE = 2.0 (line ~731) and the leg comment "ENTRY_CLEARANCE / tan(angle)" (~774) — the doc's formula (÷2 per leg, 19.08 mm at 3°, depth-independent) is consistent; PLAN §2 R0.2 corrected in place. Recommends option (a): fold the ramp along the following cut polyline, degrade to plunge at the entry column when the following cut is shorter than a min run, never refuse. Open question: 10 mm frame shift between the R03 NC and the SVG decides whether the sentry may load the SVG. |
| 2026-09-10 01:20 | R0.3 | done (verified) | — | — | research/R0.3.md, 449 lines. Spot-checks CODE: (a) `REG_SPIRAL_FINISH.feeds_family = Scallop` (catalog.rs ~2335) while its `tool_constraints` allow any tool, and `validate_tool_for_operation` (feeds/mod.rs ~825) keys on the family alone → Spiral Finish + flat tool IS refused at add (new membership defect, sixth); (b) MCP `generate_toolpath` pushes `AppEvent::GenerateToolpath` (app/mcp.rs ~5014), so it DOES run the static validator — the review said otherwise. Recommends always-create + core `compute/preconditions.rs` with eight message ids; binding rule closes F1.14 too. Blocking gap named: MCP cannot rebind a toolpath's tool/model (needs `set_toolpath_tool`/`set_toolpath_model`, add to P3). |
| 2026-09-10 01:20 | R0.4 | done (spot-checked) | — | — | research/R0.4.md, 440 lines, all seven sections. Spot-check CODE: `sim_diagnostics.rs` "Must address" cluster at ~549/586 exists as described. Key finding: two finding models exist and the GUI reads the weaker one (§2.1). Bookmark = typed focus (toolpath id + kind + anchor), four events, remap rule and failure text, named-subject table. Depends on R0.1 wording. |
| 2026-09-10 01:20 | R0.6 | done (verified) | — | — | research/R0.6.md, 449 lines. Spot-check CODE: `multitool.rs::remove_planned_toolpaths` retains on `planner_origin.is_some()` regardless of plan_id (~721) → hand-edited planner tiers are deleted on Apply. Recommends PlanChangeSummary above Apply, PLAN badge, explicit tool-draft Apply with parked drafts, library Save reads the committed tool + Replace/Keep both/Cancel, id-preserving `restore_toolpath` as the prerequisite for structural undo. |
| 2026-09-10 01:20 | R0.7 | done (verified) | — | — | research/R0.7.md, 326 lines. Spot-check CODE: `load_error` has no reader under crates/rs_cam_viz/src/ui/ (only the MCP wire at app/mcp.rs ~1901 and controller/io.rs) → "never rendered" holds. Small enough to be P1-sized per the doc; consumed by F4.3. |
| 2026-09-10 01:40 | F1.1 | done (verified, merged) | 852f710a | mcp_toasts_report_outcome_g_mcptoast.rs (7 tests) | Orchestrator re-ran fmt (4 s), clippy heavy-tests -D warnings (28 s), sentry (7/7). Read `McpOutcome` classification (ok:false / error key / Result) — all 13 sync mutation arms reply via `mcp_mutation_error` JSON, plain-text "Error:" replies exist only in read-only handlers. Not done: post-call selection/highlight still runs on a refused AddToolpath/SetParam (deliberate, no handler change); dispatch not driven end-to-end (RsCamApp needs eframe). Gate timings: fmt 6.6 s, clippy 9.6 s incremental (33 s cold), viz tests 24.6 s. |
| 2026-09-10 01:47 | F1.3 | done (verified, merged) | dfa57787 | feeds_card_labels_g_feedslabel.rs (6 tests, 5/6 failed pre-fix) | Orchestrator re-ran fmt, clippy heavy-tests (28 s), sentry (6/6) in worktree with shared target. New pure module `ui/properties/feeds_rows.rs`; card rows "Recommended advance/tooth / DOC / WOC" + "configured X" beside each; DOC/WOC marked "(calculator)". NOTE beyond a relabel: the advance row now prints rec. feed ÷ (rec. RPM × flutes) instead of the pre-derate target chipload (hover names the target) — more honest, but a semantic change to review. MCP-VIEW acceptance deferred (no GUI). Integration ref advanced by `update-ref` (main checkout busy with F1.2). |
| 2026-09-10 01:52 | infra | — | — | — | DEFECT in tonight's parallel set-up: cargo excludes the source path from a workspace crate's metadata hash, so worktrees sharing `target/` overwrite each other's rs_cam_viz/rs_cam_core artefacts; a gate in one tree can link the other tree's lib (observed: F1.8 sentry E0432 in orchestrator's run after the F1.2 tree built). All worker gate results before this line are SUSPECT. Fix: `/tmp/rs_cam_gate.sh <tree> cargo …` — global flock + touch of `crates/**/*.rs` on tree switch → forces a rebuild from the current tree. Every merged task is re-verified on the integration tree in the main checkout before the phase gate. |
| 2026-09-10 01:58 | F1.8 | done (verified, merged) | b0846bcd | rest_badge_one_predicate_g_restbadge.rs (9 tests, 3 failed pre-fix) | Re-verified by the orchestrator THROUGH the locked runner (rebuild from the wt_b tree): sentry 9/9, clippy heavy-tests clean 29 s, all 17 viz test binaries green. One predicate `state/rest_dependency.rs::rest_predecessors` (earlier, enabled, same setup, same model, previous tool); badge, `validate_toolpath` and `validate_toolpath_config` all call it. No core change. Not done: badge names no operation, no hover. |
| 2026-09-10 02:05 | F1.9 | done (verified, merged) | cbe20d06 (rebased from a80e9dd1) | export_refuses_ungenerated_op_g_exportskip.rs (6 tests, 4/5 failed pre-fix: a 13-line program was written with the op skipped) | Orchestrator rebased onto F1.8 and re-verified through the locked runner: sentry 6/6, clippy clean 28 s, 438 viz tests green, fmt ok. `io/export.rs::emitted_toolpaths` → Result; one text builder `ungenerated_toolpath_message` used by export, preflight (Fail card, export buttons disabled) and MCP `export_gcode`. FEATURE_CATALOG Export bullet added. Not done: `readiness::operations_check` still counts GUI-store results only. GUI-result fallback untouched (P2). |
| 2026-09-10 02:15 | F1.6 | done (verified, merged) | 124ca467 (rebased from b0e434e8) | depth_beyond_stock_cautions_g_depthstock.rs (9 tests; case (a) failed pre-fix with "expected one depth caution on the header, got []") | Orchestrator rebased onto F1.9 and re-verified through the runner: sentry 9/9, clippy clean 28 s, 447 viz tests green, fmt ok. Caution (id geom.depth_beyond_stock) on the header ribbon + amber row under the depth field in 11 draw fns; Generate untouched. Depth = op depth from resolved Top Z OR'd with pinned Bottom Z (deeper). Caveat recorded: 2.5D generators ignore a pinned Bottom Z, so a pin-only caution describes the Heights number, not motion (pre-existing). Not done: not on the Operations card row, not on MCP get_toolpath_diagnostics (rule is GUI-side; moving it into core heights_checks would cover both). |
| 2026-09-10 02:25 | F1.7 | done (verified, merged) | 58c1b55c | profile_through_cut_line_g_throughcut.rs (8 tests; pre-fix proof = no "Through cut" text existed) | Orchestrator re-verified through the runner: 8/8, clippy clean 29 s, 455 viz tests green, fmt ok. Builder `profile_through_cut_line`; row under Depth before the F1.6 caution (the two rules partition depth == thickness); Tabs disclosure default-open on through-cut && 0 tabs. `On` side NOT added: `ProfileSide` has Outside/Inside only and the generator has no on-line arm — the schema row still advertises `on` and `set_toolpath_param side=on` fails at serde (recorded, core change, candidate for P3/P4). Also noted: FEATURE_CATALOG says In Control is hidden from the Profile UI but the form draws it. MCP-VIEW deferred. |
| 2026-09-10 06:11 | outage 2 | — | — | — | Second session-limit cut at ~02:32, reset 06:10 (3.6 h lost). State at reset: F1.2 uncommitted in main checkout with its report already drafted (worker was in its gate sequence); F1.4 committed d9e8fa25 in wt_c but REJECTED for rework (removing the centroid path outright regresses documented SVG circle drilling; rework = circle-like polygons count as targets); F1.5 barely started in wt_b. Integration head 58c1b55c (F1.1, F1.3, F1.8, F1.9, F1.6, F1.7 merged). Resuming all three workers. |
| 2026-09-10 07:35 | F1.5 | done (verified, merged) | 7a668d90 | boundary_controls_always_visible_g_boundaryinherit.rs (3 tests) | Worker w-f15 ran out of credits after its gates passed and before committing; the ORCHESTRATOR staged its working tree, re-ran the gates through the runner (fmt clean, clippy clean 30 s, 458 viz tests, sentry 3/3) and committed on its behalf — noted in the commit body. Checkbox and the hide-gate removed; Source/Containment/Offset always visible; core field read and not written back (old files load). Not done: no provenance flag exists for the controller-assigned silhouette, so the "(auto)" suffix is never printed; sentry's constant-write exclusion would also permit a real UI write of `boundary_inherit: true` (weak spot, noted). |
| 2026-09-10 07:55 | F1.2 | done (verified, merged) | 07846057 (rebased from 04e98392) | pill_writes_clamped_value_g_pillclamp.rs (6 tests) + apply_contract_a3.rs extended (16) | Worker w-f12 ran out of credits mid-gate; the ORCHESTRATOR ran the gates, filled the report's gate section, committed on its behalf, then REBASED onto the six later merges and resolved 12 conflict hunks (properties/mod.rs + boundary_2d/drill/engrave param lists): F1.2 supersedes the raw `feeds_result` param with a `PillSuggestions` struct, so the resolution takes `pills` and KEEPS F1.6's `depth_caution` and F1.7's `through_cut`; one duplicate re-export line dropped. Re-gated after the rebase: fmt clean, clippy heavy-tests clean 28 s, 468 viz tests green, sentry 6/6. Core dev loop before the rebase: only the two known pre-existing red binaries. Pills now read `feeds::suggest::preview_field_applies` (dry run of the funnel); near-match compares the as-applied value; CLAUDE.md feeds caveat gained one sentence. |
| 2026-09-10 08:05 | F1.4 | done (verified, merged, after one REJECT) | 49493c13 (rebased from d9e8fa25) | drill_no_targets_refuses_g_drillcentroid.rs (4 tests; pre-fix the star generated 4 moves at (50.0, 51.1)) | First submission REJECTED by the orchestrator: removing the centroid path outright regressed documented SVG circle drilling (usvg flattens <circle> to paths, so the centroid path was the only way SVG circles ever drilled). Rework: `svg_input::circle_like_ring` classifies a closed ring with >= 8 vertices whose radii sit within max(2 %, 0.05 mm) of the mean as a drill target at its centre, so demo_pocket.svg's circle drills and demo_star.svg refuses with "No drill targets — pick points/circles or import a drawing with circles". One predicate `execute::drill_targets_refusal` serves generator, precondition adapter and GUI validator. Worker died out of credits before committing the rework; the ORCHESTRATOR committed it, fixed one clippy failure it left (a new unit-test module needed `expect_used` in its allow), rebased onto F1.2 and re-gated: fmt clean, clippy clean, sentry 4/4, 471 viz tests, core dev loop red only on the two known pre-existing binaries. Not done: SVG <circle> as a first-class target (still inferred from the flattened path). |
| 2026-09-10 07:58 | branch point | — | 3d88406b | — | CORRECTION to the 21:20 row: the integration branch's parent is master 3d88406b, not a0d21b96. Another developer amended master's tip commit (same G-LINKTRACE subject, rewritten message) around the time the branch was cut. `git merge-base --is-ancestor master ui-fix-2026-09-09` is TRUE, so there is no divergence: the branch is 11 commits ahead of current master and contains it. |
| 2026-09-10 08:35 | V6.1 phase gate | pass (2 pre-existing red) | 49493c13 | — | FULL gate on the merged head in a clean worktree: fmt clean; clippy --workspace --all-targets --features rs_cam_core/heavy-tests -D warnings zero warnings; `cargo test -p rs_cam_viz -q` 471 passed / 0 failed; `cargo test -p rs_cam_core --features heavy-tests --no-fail-fast` 285 binaries ok, 3718 passed, 288 ignored, 2 binaries red — `arcfit_intent_key_cost_f1` and `narrate_regions_closed_c8`, both re-run at master 3d88406b by the orchestrator and red there too. Neither is in this programme's blast radius; they need an owner. |
| 2026-09-10 08:45 | night 1 closed | — | 3651af57 | — | Baseline confirmed by the orchestrator: BOTH red binaries fail at master 3d88406b (`arcfit_intent_key_cost_f1`, `narrate_regions_closed_c8` 4 passed / 1 failed). Temporary worktrees rs_cam_wt_b / _c / _gate removed; task branches deleted (all merged into the integration branch by fast-forward). Main checkout left on `ui-fix-2026-09-09` at 3651af57, 12 commits ahead of master, working tree clean apart from three files that belong to other developers and were never touched: `.mcp.json`, `planning/multitool_2026-08-23/wanaka200_iso_scallop.toml`, `planning/ui_overlays_ux_2026-09-08.md` (the last one appeared mid-night; not ours). Nothing merged to master, no PR. NEXT: read the §7 open questions in research/R0.1.md and R0.3.md before night 2. |
| 2026-09-10 10:05 | Night 2 start | — | c2089d9d | — | Orchestrator = a fresh session, same operator. Integration head `c2089d9d`, master still `3d88406b` and contained — no rebase needed. Disk now 112 GiB free (was 24), `target/` 24 GiB, so per-lane worktrees are affordable again; the shared-cache hazard is unchanged, so every cargo command still goes through `/tmp/rs_cam_gate.sh` (byte-identical to the version-controlled `gate_runner.sh`). THREE lanes, one worktree each: `rs_cam_wt_p1` (`ui-fix/lane-p1`, P1 remainder), `rs_cam_wt_p2` (`ui-fix/lane-p2`, P2 serial chain), `rs_cam_wt_p3` (`ui-fix/lane-p3`, P3 MCP). DEVIATION from the night-2 prompt's "one task per worker, fast-forward merge in id order": a lane accumulates several task commits, so lanes 2 and 3 are REBASED onto the merged head, not fast-forwarded. Reason: the flock caps useful parallelism at three, and per-task worktrees would triple the rebase count for the same conflict set. MCP server still DOWN (`pgrep -af rs_cam_gui` empty), so every MCP-VIEW acceptance stays deferred. |
| 2026-09-10 10:10 | operator decisions | — | — | — | The four escalated design questions were ANSWERED by the operator today, in session, and they override the research recommendations where they differ. **R0.1 §7 Q1 — stock edits: EVERYTHING STALES**, dimensions, pins AND material alone, on both the GUI and the MCP route (operator chose the simple rule over R0.1's recommended dimensions-only split; cost accepted: every stock nudge regenerates every 2.5D op after the 500 ms debounce). **R0.1 §7 Q2 — regenerate on load: 2.5D ops only, respecting each op's own auto-regen dial**; 3D manual-regen ops load as `NoResult`. This removes the G-REGEN-RACE precondition and is scheduled as the NEW task F2.6, not folded into F2.1. **R0.3 §7 Q3 — bull nose on Scallop: WIDEN, allow it** (against R0.3's recommendation to keep the registry as the truth). Consequence the orchestrator attaches to F4.2: the scallop generator was written for a ball tip, so widening the registry is only honest if the generator uses the tool's CORNER radius; F4.2 must prove that from the code and, if it does not, record a blocker rather than shipping a permissive registry over a generator that cuts wrong. **R0.3 §7 Q6 — Spiral Finish joins the ball-tip list**, so it refuses a flat tool at the same surface as Scallop and Unified instead of generating and objecting only in the Feeds tab. R0.1 Q3 (kinematics: sim stale only) and Q4 (reordered Fresh ops stay Current) stand as recommended. R0.1 Q5, Q6 and R0.3 Q1, Q2, Q5 are NOT yet answered and stay with their research recommendations. |
| 2026-09-10 10:15 | wave 1 dispatched | — | — | — | Three workers running: w-f21 (F2.1, freshness state, lane p2), w-f110 (F1.10 a–d, lane p1), w-f37 (F3.7 + F3.8 MCP rebind, lane p3). Each carries the pre-fix-failing sentry requirement, the locked-runner invocation for its own tree, the forbidden list, and the write-files-before-gates rule that recovered three dead workers on night 1. |
| 2026-09-10 10:35 | F4.2 precondition | finding (blocks the naive fix) | — | — | The orchestrator ran the proof the operator's bull-nose ruling depends on, BEFORE dispatching F4.2, and the generator does NOT drive the corner radius. `MillingCutter::cusp_radius` (`tool/mod.rs`) special-cases `ToolGeometryHint::TaperedBall { tip_radius }` and falls through to `_ => self.radius()` for everything else; `BullNoseEndmill` (`tool/bullnose.rs`) overrides `corner_radius_mm` and `geometry_hint` (`Bull { corner_radius }`) but NOT `cusp_radius`. So on a Ø10 bull with a 2 mm corner the cusp-forming radius reads 5.0, not 2.0. `scallop.rs` takes `let cusp_r = cutter.cusp_radius_mm()` and drives every stepover and cusp equation from it, so a widened registry would space the passes off a radius 2.5× too large and leave scallops far taller than the requested scallop height — silently, with the operation reporting success. This is the exact split R0.3 §7 Q3 named: the FEEDS side already reads the corner radius (`feeds/cutter_constraints.rs::max_doc_scallop`, `Bull { corner_radius } => Some(corner_radius)`), the GEOMETRY side does not. Honest execution of the ruling therefore needs a `Bull` arm in `cusp_radius`, which is a core geometry change with 15 consumer files (tier_islands, multitool, rest_field, tier_map, pencil, finish_planner, unified_finish, measurement, finish_setup, operation_configs, tool_shape_key, scallop, reach, execute) and it changes bull-nose behaviour in all of them, not only in Scallop. Carried into F4.2's dispatch as a hard condition: prove the arm is correct and gate it with its own sentry, or record the blocker and leave the registry alone. |
| 2026-09-10 11:05 | J0 | done (gated, NOT merged) | e1f845dd..f84136fb on `core-consolidation` | (none — consolidation only) | CORE-PARALLEL account, own worktree `rs_cam_wt_alt` + own cache `rs_cam_target_alt`, no shared lock used. **The prompt's J0 premise was wrong on three of the four branches.** `git cherry master <branch>` reports SIX of the eight commits as already upstream by patch-id: `isoclip-rapid` (twins `38f8d151`, `fb6027fb`), `ui-string-sentry` (twins `8701a4f4`, `01ec1e41`), `pipesmoke` (twin `8854e729` + the evidence PNGs). They are stale POINTERS, not unlanded work — corroborated without running anything by the fact that `isoclip-rapid` MODIFIES `isoclip_entry_ramp_g_isoclipentry.rs`, which is `ui-string-sentry`'s own sentry. Nothing was rebased; no conflict arose. Only `linking-pencil-dial` is genuinely unlanded (merge-base IS master, 0 behind), so J0 is a cherry-pick of its three code commits onto `core-consolidation`. DEVIATION: its fourth commit `627ef997` was DROPPED — it touches `.mcp.json` only, which is on the prompt's forbidden list; it stays on `linking-pencil-dial` for the operator. Master NOT advanced, no PR. The three stale branch pointers can be deleted by the operator; J0 did not delete them. Not done: no check that each master twin is byte-identical to its branch commit (`git cherry` compares patch-id, so an amended body reads the same). Report: reports/J0.md. |
| 2026-09-10 11:05 | J1 | done (gated, NOT merged) | 240dce5f | the two red binaries themselves (no new sentry — this job changes no behaviour) | Both red binaries at master have DIFFERENT causes. `narrate_regions_closed_c8` was a REAL REGRESSION, already fixed by J0's `f84136fb` (G-LINKTRACE): the link stage returned `MoveProvenance::Permutation` where it used to return `Remap`, and the relinker creates foreign intrusion at every junction by construction, so `ScallopAnnotationChannel`'s `remap_range(i, i+1)` dropped every ring annotation — master reads `1 items, regions 0, rings 0`, this branch reads 5/5 green with no J1 edit. The pin was never stale. `arcfit_intent_key_cost_f1` was a STALE PIN, attributed by building the same test file against two libraries: `01ec1e41` moves=74 arcs=13 Retract=7 PASS, `38f8d151` moves=80 arcs=13 Retract=13 FAIL, master identical to `38f8d151` fingerprint included. G-ISOCLIPRAPID added a pure-vertical Retract lift before the lead-in's pre-position rapid; six of face_full's seven lead-ins gained one. **The F1 Q3 quantity did not move** — arcs 13 on every fixture, census (118, 20, 0, 0) unchanged, because a Rapid is neither a same-feed Linear join nor arc geometry. Pin re-measured 74→80 with the attribution in a comment; no assertion loosened, no test deleted. Gates: fmt clean, clippy -p rs_cam_core --all-targets -D warnings zero (1:30), `cargo test -p rs_cam_core -q` NO failures (11:39) — the core dev loop is fully green on this branch. **NOT RUN and OWED: the FULL heavy-tests gate and the heavy-feature workspace clippy**, on a standing operator instruction that they must not run on this machine ("it will kill my pc"). DECLINED and recorded rather than taken: pinning the per-intent histogram beside (moves, arcs) so the next drift names the intent — it would tighten the file but widen what an unrelated dressup change must re-baseline. Follow-up worth having: other goldens carrying a lead-in on an op with a retract plane moved by the same six-rapid class and may be un-rebaselined too. Report: reports/J1.md. |
| 2026-09-10 12:20 | F1.10 | done (verified, merged) | 46c07c54 (rebased from 99155474) | move_to_setup_honours_index_g_dropindex.rs (3), reorder_toolpath_inserts_g_dropindex.rs (5), generate_all_skips_disabled_g_genalldisabled.rs (2), fixpoint_resolution_notice_g_resnotice.rs (5) | All four letters were real defects; none skipped. (a) `move_toolpath_to_setup` takes `target_position: Option<usize>` and inserts; the panel passes `Some(drop_idx)`, MCP passes `None` and still appends — the literal `0` that used to sit at the MCP call site was INERT because core ignored the index, so passing it through would have silently moved the op to the front. (b) `reorder_toolpath` is remove+insert. The worker's ruling, which the orchestrator accepts: Move Up/Down always pass the ADJACENT index, where insert and swap are the same permutation, so those two menu items are byte-identical before and after; only drag carries an insertion gap, which only an insert can honour. `handle_reorder_toolpath` converts gap → target card (`gap > local_pos ? gap-1 : gap`). (c) `handle_generate_all`'s no-rest-chain branch had no `enabled` filter while the ladder branch beside it did, so one Generate All obeyed the enable toggle and the other did not, purely on whether the project held a rest chain; it now submits `scope.enabled` and the "No enabled toolpaths" guard moved above the branch. (d) new pure `generate_all::resolution_override_notice`, pushed at Warning before the two writes, which are unchanged — a truthfulness fix only. ORCHESTRATOR VERIFICATION: read the whole diff; rebased onto the integration head and proved the `crates` tree object is IDENTICAL before and after (`2eb3e714…`), so the code is byte-for-byte what the worker gated; fmt clean; clippy --workspace --all-targets --features rs_cam_core/heavy-tests clean in 1m07s; all four sentries re-run green (15 tests). INDEPENDENT pre-fix reproduction by the orchestrator, not taken on trust: reverting only the insert body back to `swap` fails 3 of 5 in the reorder sentry with `left: ["E","B","C","D","A"], right: ["B","C","D","E","A"]` — dragging the first op to the bottom also threw the last op to the top — and the 2 that still pass are exactly the adjacency cases, which corroborates the (b) ruling. The full `-p rs_cam_viz` suite was NOT re-run by the orchestrator (the worker ran it, exit 0, 13m24s) because the tree object is provably identical; recorded here rather than implied. CLAUDE.md's generate_all paragraph gained one caveat sentence: `simulation_resolution_mm` is not scoped to the call and the ladder's write persists after the run. FEATURE_CATALOG gained three bullets. NOT DONE, deliberately: the stale-snapshot regeneration and the export fallback (r05 §6 items 1–2) are untouched and the window is now WIDER, because a drag can move an op several places at once — that is P2 (F2.1–F2.4) and needs R0.1. One weak spot the orchestrator notes: `resolution_override_notice` compares floats with `f64::EPSILON` as an absolute tolerance, which is bit-equality at 0.3 mm; it errs toward notifying, never toward silence, so it is safe, but it is not a tolerance. Nothing checked live; MCP down, no GUI started. |
| 2026-09-10 13:10 | J2 | done (gated except the FULL gate; 1 binary left RED by design; NOT merged) | d0aeee02 | ramp_contained_in_region_g_rampcontain.rs (3 arms, ALL THREE failed pre-fix) | F4.1 / G-RAMPCONTAIN, R0.2 §4 option (a): a prism ramp now FOLDS along the operation's own following cut — out half the ramp length, back over the same ground, Z by cumulative XY distance, floored at the cut's own Z — so it is contained wherever the op's own cut is, with no region polygon or offset call at emit time. Degrades to a plunge below `max(1.0, tool_radius)` mm of following cut; never refuses. G-RAMPTERRAIN's Z clip untouched; the adaptive3d door passes `fold: None` and keeps the legacy legs. New XY checker `entry_audit::fed_moves_outside_region` + `Polygon2::distance_to_boundary`. **RED-FIRST:** 40 of 280 EntryRamp moves outside the pocket, worst disc overhang 2.934 mm; mechanism arm 14.0006 mm off the cut; sim arm 18 columns outside the outline cut to **−11.000 mm in a 12 mm board** (the ten depth passes all enter at the same XY, so the blind legs STACK) with `rapid_collisions` **0** throughout — no shipped checker could see it, because every one of those moves is fed. **The perf golden was pinning the gouge**: its zigzag machines air (the pocket already cut 3 mm deeper) yet reported 541.63 mm³ of removal; now 0.00, Profile +423.37, project −118.27 — `541.63 − 423.37 = 118.26`, so 118.27 mm³ was stock the shipped program destroyed for nothing. Goldens re-pinned WITH attribution: arcfit arc_raster (40,8)→(48,16), finish_role_wide (32,8)→(32,16), finish_role_tight (32,8)→(76,44); transform_provenance arc_raster fingerprint + link landing sites (shift accumulates +4/+6/+8 in entry order, which is the provenance remap being RIGHT); perf golden regenerated. **ARCS MOVED and that IS the F1 Q3 quantity** — named, not buried: a curved following cut makes a curved entry that `fit_arcs` collapses; `intent_breaks_unknown_strict` unchanged everywhere, and the two STRAIGHT-following-cut fixtures (three_pass, face_full) are byte-identical, which isolates the cause. **LEFT RED ON PURPOSE:** `adaptive_feed_modulation_pipeline_f036b::modulation_raises_cutting_chipload_toward_band` — it medians EVERY F word and the fold doubled the entry population (F192 42/138 → 90/216), dragging the median off the cutting moves the assertion names. Measured both libraries; no cutting move changed feed; restricting the median to cutting feeds gives F1980 / chipload 0.0550 IN BAND on BOTH, so the recommended instrument fix reproduces the pre-fix verdict and is not tuned to this branch. It is the F-036b programme's sentry, so re-pointing it is not this job's call — nothing relaxed, band untouched. CLAUDE.md gained sentences in the existing G-RAMPTERRAIN bullet ("Prism ops keep blind legs BY DESIGN" became false about XY) — flagged for the operator, on this branch only. Gates: fmt clean, clippy -p rs_cam_core --all-targets -D warnings zero, 11 targeted binaries green. **NOT RUN and OWED: FULL heavy-tests gate + heavy-feature clippy** (standing operator instruction). NOT DONE: the report-only finding chain (ToolpathStats::entry_outside_region, diagnostics adapter, id) and the triage safety row — R0.2 marks the generation→triage join UNVERIFIED and it was not traced; the `rs_cam_viz` worker call site is the UI programme's to wire; VCarve/Chamfer uncovered by the checker's region table; Adaptive's helix not run through it. Report: reports/J2.md. |
| 2026-09-10 14:05 | F2.1 | done (verified, merged) | 5765a179 (rebased from 44b35e7d) | 13 tests in the G-FRESHSTATE section of `controller/tests.rs`, walking the R0.1 §2.3 mutation matrix; pre-fix 8 failed / 5 passed, quoted verbatim in the report | R0.1 Option A implemented. `state/freshness.rs` holds `FreshnessState` (Current / EditedSince / Regenerating / WaitingOnUpstream / Error / NoResult / Disabled), derived on every read from the core result cache plus the lane status; nothing stored, nothing persisted. `ProjectSession::drop_result` is the single funnel for "a result is removed because an input changed" and bumps a new `toolpath_revision` that F2.4 will read; `insert_result` deliberately does not move it. THE S0 DEFECT IS CLOSED: `properties/mod.rs::write_entry_config_to_session` compares a generation-inputs signature and calls the new public `invalidate_toolpath_inputs`, so a GUI inspector edit now drops the core result — before this the panel wrote `ToolpathConfig` fields directly, no core setter ran, and `get_result` kept the PREVIOUS parameter set's geometry, which export emitted. OPERATOR RULINGS IMPLEMENTED: Q1 every stock setter calls `drop_all_results` on both the GUI and MCP routes; Q3 kinematics stales the simulation only and the MCP route now calls `invalidate_machine` synchronously; Q4 swapped `Fresh` ops stay `Current`. A DEFECT FOUND BY THE WORK, worse than the one it was sent for: the setup panel wrote `face_up` / `z_rotation` straight into `SetupData` with no core setter, so every toolpath in a flipped setup kept a result generated in the OLD frame — flip a setup, export, and the program is geometry for the previous orientation with no surface saying so. Now routed through new core `set_setup_face` / `set_setup_rotation`. DEVIATION, accepted: a new `AppEvent::HeightPlanesChanged`, because the heights write-back borrowed `StockChanged` for a GPU re-upload and the operator's Q1 ruling would otherwise have staled the whole project on any heights nudge. `stale_since` was NOT renamed and the controller pump was not built (worker's report §5). ORCHESTRATOR VERIFICATION: read the whole diff and the new module; audited the funnel claim independently and found ONE `results.remove` outside it, at `session/compute.rs:1438` — that site is the pre-existing G-STICKYEMPTY guard which drops the result because a generation is STARTING, not because an input changed, so it correctly does not bump the revision and the funnel holds; rebased onto the head carrying F1.10 with NO conflict and hand-checked the one function both lanes edited (`move_toolpath_to_setup` keeps F1.10's `target_position` insert AND F2.1's `drop_result`, in the right order); fmt clean; clippy heavy-tests clean 49s; 336 lib tests and every viz binary green; F1.10's four sentries re-run green on the merged tree; `-p rs_cam_core --no-fail-fast` red on exactly `arcfit_intent_key_cost_f1` and `narrate_regions_closed_c8`, the two known master reds, and nothing else. NOT DONE, and this matters: F2.1 does NOT close the export defect. `io/export.rs` still falls back to the GUI's retained result when the core result is gone, and because F2.1 makes far more routes drop the core result that fallback is now reached MORE often, not less — an edited op still exports its old geometry. F2.3 closes it. ORCHESTRATOR DEVIATION from PLAN §4 ordering: F2.3 is dispatched BEFORE F2.2, because the plan's order leaves the data-loss path open while improving how it is displayed. Nothing checked live; MCP down, no GUI started. |
| 2026-09-10 14:30 | J3 | done (gated except the FULL gate; NOT merged) | cd7eb7c7 (+ e8466d86 CLAUDE.md readability) | bull_nose_cusp_radius_g_bullcusp.rs (4 arms, 2 failed pre-fix) | **The 10:35 F4.2 precondition row is CONFIRMED and DISCHARGED, but the one-line arm it described was NOT enough.** Verified the claim first: `cusp_radius` matched only `TaperedBall` and fell through to `_ => self.radius()`; `BullNoseEndmill` overrides `corner_radius_mm` and `geometry_hint` but not `cusp_radius`. **RED-FIRST, measured:** a Ø10 bull with a 2 mm corner read cusp **5.000**, and `equal_cusp_stepover_mm` gave **1.4107 mm where the requested 0.05 mm scallop height allows 0.8888 mm — 1.59× too wide, with the operation reporting success.** Two things the prompt did not say and that matter: `ToolDefinition` forwards both spellings to the inner cutter and `cusp_radius_mm` is a plain alias, so the default trait method is a SINGLE change site; and `ToolGeometryHint::Bull` is produced by exactly one type, so nothing else reaches the arm. The `corner_radius > 0.0` guard is load-bearing — a zero-corner bull is constructible and is a flat end mill, and a cusp radius of 0 divides by zero in `equal_cusp_stepover_mm`. **THEN the suite refused the naive arm, at the site the audit had already flagged:** `rest_region_tip_radius_dilation_f1::every_non_tapered_shape_keeps_the_radius_it_had`, whose own message says "F1 would change this shape's dilation and moat, WHICH IT MUST NOT". It is right — a bull nose cannot enter a valley its corner radius suggests, because the full-diameter cylinder sits only one corner radius above the tip, so it binds on its FULL radius past the corner; dilating a rest mask by the corner would report territory the fine tool cannot reach, and those polygons become `BoundarySource::DerivedRestRegions`, i.e. what a fine tool is ALLOWED to cut. `pencil::resolve_reference_cutter` rests on the same invariant ("so nothing but the tapered path moves"). **RESOLUTION: landed H2.** The taxonomy in `tool/mod.rs` already reserved `VALLEY | two-wall / profile fit in a valley | no API today (H2)`; `MillingCutter::valley_radius_mm()` now fills it — tip radius for a tapered ball, `radius()` otherwise, i.e. BYTE-IDENTICAL to pre-arm `cusp_radius`, so the four FIT call sites moved onto it (rest_field erosion radius, mask→polygon dilation, trust-region moat; pencil reference resolution) change behaviour for no tool that exists. `ToolDefinition` forwards it explicitly for PR-2's stated reason. The F1 arm now asserts its identity claim against `valley_radius_mm` — the query that actually feeds the dilation and moat — so the claim is PRESERVED not weakened, plus two ADDED assertions pinning that the queries part company on a bull (cusp 1.0, valley 3.0) so a revert trips it. Per-consumer audit in the report, incl. three findings worth knowing: `pencil::tip_contact_radius` already preferred the corner radius, so the repo had ruled this way at its own site; `reach::working_half_width_mm` is a NO-OP for a bull by arithmetic (`width_at_height(0) == R − R2 > R2`), verified against bullnose.rs not assumed; `tool_shape_key` and `sim_prefix::hash_tool` change VALUE so bull-nose cached work misses once (not a correctness hazard — both already hash `corner_radius_mm()`, so a moved key misses rather than being reused wrongly). Gates: fmt clean, clippy -p rs_cam_core --all-targets -D warnings zero, full `cargo test -p rs_cam_core -q` green EXCEPT the one binary J2 left red by design. **NOT RUN and OWED: FULL heavy-tests gate + heavy-feature clippy** (standing operator instruction). NOT DONE: does NOT widen the Scallop registry (that is F4.2, the UI programme's); grid-sizing consumers (`finish_setup`, `measurement`, `reach_map`) now plan a bull-nose finish on a finer grid — 6.25× the cells for the `/4` modes — which is more correct and slower, and is NOT MEASURED (no timing run). Report: reports/J3.md. |
| 2026-09-10 15:10 | F1.13 | done (verified, merged) | 99b4eaee (rebased from 3bb69824) | workspace_menu_complete_g_wsmenu.rs (10 tests; pre-fix 5 passed / 5 failed, one failure per repair, quoted verbatim) | The defect under the plan text was FOUR hand-written copies of one enumeration: menu_bar's three buttons, workspace_bar's four tabs, its four-arm hint match, and — sharpest — `workspace_keys_round_trip` in app/mcp.rs, the test whose job is to prove every variant round-trips, iterating its own re-typed list. A completeness test that maintains a private copy of the thing it checks cannot catch a missing entry, which is how Readiness sat on the tab bar from W3.8 to today and never appeared in the Workspace menu. `Workspace::ALL` + `order()` + `label()` + `hint()` is now the one list on the `ui/overlays/registry.rs` pattern; menu, bar and the MCP test read it; order and labels unchanged, so the bar is pixel-identical. WORKER RULING, accepted: completeness is enforced by a PAIR — `order()` is an exhaustive match so a new variant does not COMPILE until placed, plus a sentry that the position is a real, unique, fully-claimed slot — rather than `assert_eq!(ALL.len(), 4)`, which would have been the same hand-maintained number the fix exists to end. Also: "Select an item in the project tree" → "Select an operation, tool, setup or model" (no panel of that name exists); the combined "Generate and export G-code" step split into 5. Generate toolpaths / 6. Simulate and review / 7. Export G-code, because the new step could otherwise only sit before generation or inside export; camera fit on ALL THREE `open_job_from_path` call sites (RS_CAM_JOB startup, File > Open, MCP `load_project`) using the existing `fit_camera_to_first_model()` that ResetView already uses — no second fit invented — with the startup route fitting after construction because the camera does not exist while RS_CAM_JOB loads inside `RsCamApp::new`. MCP gets the fit deliberately: an agent's next call after a load is usually a screenshot and the viewport held the previous project's framing; it changes a view, not project data, and the reply text is byte-identical. FEATURE_CATALOG and CLAUDE.md deliberately NOT updated (no capability or operator-visible rule moved), reasoning in the report. NOT DONE: every surface check is a source read, because each draws through `egui::Ui` and `RsCamApp` needs an `eframe::CreationContext` — the same stand-in `mcp_toasts_report_outcome_g_mcptoast.rs` uses. Visual confirmation of the wording and the framing is OWED to V6.2. |
| 2026-09-10 15:12 | F1.15 | done (verified, merged, after one CORRECTION) | c3e8b79f (rebased from 489c2f2c, amended from a9023d11) | inspector_header_wraps_g_reachwrap.rs (7 tests; pre-fix 2 passed / 5 failed, one per changed site plus the helper, re-quoted after the rename) | The worker cropped and enlarged the W02 capture rather than trusting the review's prose: both lines run to the WINDOW edge and stop mid-word with no second line — the reach reading cut at "max gap 2.82" with `grid_note` never drawn, the tool-load caution cut at "The whole derat". WHY IT MATTERS, and the reason this task is not cosmetic: in both sentences the qualification sits at the END, so clipping removes the qualification and leaves the claim — a truncated caveat reads as an unqualified result. The clause lost from the caution says the derated band sits below the chip-formation floor, that burnishing is still expected, and what to do instead; `grid_note` and `over_statement_note` are the same thing for the reach number. FIX: the reach readings sat inside `ui.horizontal`, which yields `TextWrapMode::Extend`, and the bare labels inherited it; they now sit in the vertical flow as full-width wrapped lines, plus a `wrapped_small_label` helper (wrap only, no width, no truncation), explicit `.wrap()` on `render_diagnostic_row`'s message, and the validation_errors loop. ORCHESTRATOR CORRECTION, and the reason the commit was amended before merge: the first submission claimed the labels were "unwrappable BY CONSTRUCTION at any width — `.wrap()` could not have helped". That is false. The orchestrator checked egui-0.34.3 directly: `Label::wrap()` sets `wrap_mode = Some(TextWrapMode::Wrap)` (`widgets/label.rs:63`) and the label resolves `self.wrap_mode.unwrap_or_else(|| ui.wrap_mode())` (`label.rs:191`), so the label's own setting WINS over the container's. `.wrap()` would have wrapped — at `ui.available_width()`, which on that row is only the checkbox's remainder, a narrow column. The prescribed fix was INSUFFICIENT, NOT INERT, and the reason to move to the vertical flow is the wrapping WIDTH. The worker verified both citations itself, corrected the claim in all four places it appeared (report with a dated "corrected" note naming the earlier wording, sentry module doc, two code comments, commit body), renamed the test that had encoded the wrong claim (`..._are_not_on_the_unwrappable_checkbox_row` → `the_reach_readings_get_the_panel_width_not_the_row_remainder`), and re-ran the pre-fix demonstration because the rename made its quoted output stale. CENSUS: six header lines carry sentences; two already wrapped and were untouched; the other four are the ones changed; the worker states it looked and that is the complete list. NOT RESOLVED, stated rather than papered over: the tool-load caution was ALREADY inside `horizontal_wrapped` (mode Wrap) and clipped anyway, past the panel's own inner margin, so the panel is laying out content wider than the panel. The worker's mechanism — the Extend row poisoning the panel's reported content width — is labelled a HYPOTHESIS, not a proven cause; ledgered F1.23 and the next place to look if V6.2 still sees clipping. F1.22 opened for `draw_feeds_card`'s identical shape on the Feeds tab. The sentry is a source read and its own doc says it does NOT prove "no text clipped at the right edge"; no GUI was started, no rendered evidence exists, none is claimed, and the visual check is OWED to V6.2. No text was changed anywhere. ORCHESTRATOR VERIFICATION of both rows: read both diffs; verified the egui citations independently in the vendored source; rebased the two commits over F2.1 with NO conflict (the lanes edited different regions of `app/mcp.rs` and `properties/mod.rs`) and re-gated on the rebased tree — fmt clean, clippy heavy-tests clean 30.9s, all 24 viz binaries green with zero failures. Core not re-run: F1.13 and F1.15 change no core file, so the core tree is the one already gated at F2.1's merge. P1 IS NOW CLOSED except F1.12, which stays HELD because it edits `controller/io.rs` and lane p2 is working there. |
| 2026-09-10 16:20 | F2.3 | done (verified, merged) | 66d2c232 (rebased from ccd9a0de) | export_refuses_stale_result_g_stalexport.rs (7 tests, pre-fix 4 failed / 3 passed, quoted verbatim) | THE EXPORT HOLE IS CLOSED. `emitted_result_toolpath` reads `session.results` first, as before, and reaches the viz store ONLY under `StaleResultPolicy::AcceptPreviousGeometry`; the default refuses and names the operation. The fallback was KEPT rather than deleted, and the worker states why: deleting it would make the accept flag unimplementable, because once an edit drops the core slot the previous geometry exists only in the viz store. The viewport is untouched — only export's PERMISSION changed. G-MODEXPORT is intact: `session.results` is still read first and is still the only store the modulation post-pass writes, so the acceptance emits the pre-modulation worker IR exactly as the old silent fallback did. ONE FAMILY, ONE BUILDER: F1.9's `ungenerated_toolpath_message` became `blocking_toolpath_message`, now keyed on `FreshnessState` instead of `ComputeStatus` — which could not tell "never generated" from "edited since", both reading Done/absent, and so had no sentence for the second. F1.9's three sentences are byte-identical; two are added (edited-since, still-generating). Export, pre-flight and MCP `export_gcode` all print it, so no two surfaces can disagree. THE FLAG: an enum in process so there is no `true` to fat-finger, `accept_previous_geometry` on the wire, and the checkbox reads "Cut the PREVIOUS geometry — the file will not match the parameters on screen", in its own block, never folded into the tool-load overrides. It waives ONE situation only: a missing, failed or still-generating result refuses under it too, and the MCP route builds its policy from its own parameter so automation cannot inherit a ticked box. NEW REFUSAL worth knowing: exporting while the compute lane is generating an operation used to emit the program being replaced; it now says "is still generating". R0.1 §7 Q5 ANSWERED BY ASSUMPTION — per-export, never persisted, per R0.1's recommendation; flagged as an assumption in the report and here, and still open for the operator. ORCHESTRATOR VERIFICATION, concentrated on the test changes because that is where a fix like this can quietly weaken its own net: the worker changed one existing sentry's expectation and corrected six fixtures, so the orchestrator diffed EVERY test file in the commit for removed assertions — exactly TWO removed lines, both `.expect(...)` preserved through a signature change that threads the new policy parameter, against 40 added assertions. No assertion was weakened; the claim holds. The G-MODEXPORT sentry got STRONGER: `viz_export_falls_back_to_the_worker_result_...` became `viz_export_refuses_the_worker_result_...` and now pins BOTH arms — the default refusal must name the operation and say "edited after it was generated", AND the acceptance must still emit `COMMANDED_FEED`, which is the original assertion kept inside the acceptance arm. The six fixture corrections seeded only the viz store, which no longer means "generated"; one of them, `per_setup_export_puts_identity_setup_in_the_stock_relative_frame`, was F2.1's stock rule working correctly (it called `set_stock_config` after seeding). Rebased over F1.13 and F1.15 with NO conflict; re-gated on the rebased tree — fmt clean, clippy heavy-tests clean 29.3s, 25 viz binaries / 515 tests green with zero failures, `-p rs_cam_mcp` 29 green. Core NOT re-run and not required: `git diff --name-only HEAD~1 HEAD -- crates/rs_cam_core/` is empty. Nothing checked live; MCP down, no GUI started. |
| 2026-09-10 17:30 | F2.2 | done (verified, merged) | 6b39feaf (rebased from c10693a8) | freshness_surfaces_g_freshrender.rs; F1.17 folded in per PLAN §11 | TEN surfaces now read `state::freshness` — card chip, the play button, the stats row, the setup header n/m, the rest `dep` badge, the inspector header, both workspace chips, `readiness::operations_check` and the viewport. No second source of truth, no invented state; F2.1's seven arms covered every surface. TWO ROOT CAUSES separated by the worker rather than merged: `ComputeStatus::Done` is genuinely TRUE (the generation did finish; the enum simply cannot say "and then the inputs moved"), and `gui.toolpath_rt[..].result` is deliberately retained so the viewport can draw — so any surface COUNTING it counts an unreproducible op as computed. F1.17 folded in: `operations_check` counts `is_current()`, "computed" became "current" because an edited op WAS computed, and the row now names them ("1/2 current — 1 edited since generation"). TWO ADJACENT FIXES in files the worker was already in: the setup header counted `ComputeStatus::Done` and so read n/n beside a STALE card — one panel contradicting itself — and the rest `dep` badge leaned on `stale_since`, which F2.1 demoted to a debounce clock, so rows that drop a result without setting a regen request left it green. WORKER JUDGEMENT on prominence, accepted and worth preserving: the word stays STALE because the repo already spends that word on this idea and a second word for one idea is how three stores came to disagree in the first place; amber not red because nothing is WRONG and spending red here flattens "may hurt the machine" against "picture is out of date"; and the prominence is deliberately NOT in the chip — four amber letters do not undo three confident figures two rows below, so the stats row is prefixed "old:" and demoted and the play button reappears on exactly the card whose message is "regenerate me". Three consistent signals rather than one loud one. R0.1 §7 Q6 ANSWERED BY ASSUMPTION (R0.1 §4.4's own recommendation: opacity, same palette; dashed grey not tried), using the EXISTING 0.45 dim uniform rather than introducing a second constant nobody ruled on. ORCHESTRATOR VERIFICATION: read the diff; checked the one alarming item myself — the worker modified F1.8's ALREADY-MERGED sentry, and the change is PURELY ADDITIVE (a core result inserted to model "generated", a core result dropped to model "not generated", to match F2.1's definition); no assertion was altered and all nine still pass. Rebased over F2.3 with no conflict; fmt clean, clippy heavy-tests clean 30.7s, 26 viz binaries / 525 tests green, zero failures. Core not re-run: no core file changed. NOT DONE: MCP-VIEW and the `list_toolpaths.stale` cross-check were NOT run, no screenshot exists and the row's acceptance is NOT claimed — owed to V6.2. TWO FOLLOW-ONS OPENED, renumbered by the orchestrator to F2.7 and F2.8 because the worker's proposed "F2.6" collides with the load-regen task the operator's Q2 ruling created this morning: F2.7 the MCP wire publishes no `freshness` key (it still exposes the legacy `stale` boolean off the demoted clock), F2.8 the palette-exclusivity problem — a stale path under an engagement palette is a stale palette too. ONE ITEM THE ORCHESTRATOR IS SENDING BACK, not blocking the merge: the worker's guard `no_freshness_text_promises_that_export_is_gated` was written against a head WITHOUT F2.3 and its failure message asserts "F2.3 has not landed the gate and a stale operation still exports". F2.3 landed first. The guard passes today because it scans only `toolpath_panel.rs` and `readiness_panel.rs`, but its message is now FALSE and it would block a future card chip that correctly says a stale op will not export. Reconciliation dispatched as F2.9. |
| 2026-09-10 18:40 | F3.7 + F3.8 | done (verified, merged) | c5f7b63e (rebased from ceb97653) | toolpath_rebind_g_mcprebind.rs (9, core) + mcp_rebind_surface_g_mcprebind.rs (6 viz, 5 failing pre-fix with "the embedded server does not register `set_toolpath_tool`") | THE DEAD END IS CLOSED. Pre-fix evidence captured verbatim on the unmodified tree: the embedded router registered neither name, and `set_toolpath_param(0, "tool_id", 1)` returns `Err(InvalidParam("unknown parameter 'tool_id' for Pocket operation. Valid parameters: stepover, depth, …"))` — no operation config declares `tool_id` or `model_id`, so the key was refused for EVERY op type. A blocked operation was therefore visible over MCP and unrepairable over MCP, which is also why R0.3's refusal contract (F4.2) could not be driven from a test. FINDING the worker reported rather than worked around: the GUI combos emit NO `AppEvent` at all — `properties/mod.rs` writes `entry.tool_id` / `entry.model_id` and `write_entry_config_to_session` copies them into the session — so there was no widget event to share and it says so instead of inventing one. Both tools route through new `ProjectSession::set_toolpath_tool` / `set_toolpath_model` in `session/mutation.rs`, ending in `invalidate_result_chain` (a different cutter removes different material, so it is a stock-chain change, not the narrower `remove_result`). `set_toolpath_param` KEEPS its behaviour and keeps refusing both keys — one route, one meaning; Rest's `prev_tool_id` is a real op param and a different thing — with its description now pointing at the two new tools. Both take a project-assigned ID, not a positional index (differs deliberately from `add_toolpath`'s `tool_index`); the reply echoes the resolved tool/model and the refusal lists the valid ids. `set_toolpath_tool` DELIBERATELY allows a shape the operation rejects, because that is what keeps a blocked op repairable; the generator still refuses. ORCHESTRATOR VERIFICATION: read the diff; confirmed the new core setters land on F2.1's funnel rather than beside it (`invalidate_result_chain` → `drop_result`, which bumps `toolpath_revision`). FIRST REAL REBASE CONFLICT of the programme, resolved by the orchestrator: `CLAUDE.md`, where lane p2's G-FRESHSTATE caveats and lane p3's G-MCPREBIND caveat were appended to the same MCP paragraph — mechanical, BOTH sides kept in order, markers verified gone. Re-gated after the resolution: fmt clean, clippy heavy-tests clean 38.2s, 27 viz binaries / 531 tests green, `-p rs_cam_mcp` 29 green, `-p rs_cam_core --no-fail-fast` red on exactly `arcfit_intent_key_cost_f1` and `narrate_regions_closed_c8` and nothing else. ONE WORKER CLAIM REJECTED AS STALE, not carried into the ledger as a finding: its point 4 said the GUI rebind route invalidates nothing — no result drop, no `mark_edited`, no undo entry. That was TRUE on its base `c2089d9d` and is FALSE on the merged head: F2.1's `generation_inputs_signature` (`properties/mod.rs`) includes `tc.tool_id` and `tc.model_id`, so a GUI rebind now drops the core result through `invalidate_toolpath_inputs`. The orchestrator checked the current head rather than accepting the report. GENERAL LESSON for the remaining lanes, recorded here because it will recur: with three lanes in flight a worker's OBSERVATIONS describe the tree it branched from, exactly as its gate results do, and both must be re-checked against the merged head. Still true from its report: no undo entry is created for a rebind on either route. Nothing exercised live; MCP server down. |
| 2026-09-10 19:45 | F2.9 | done (verified, merged) | 70f7b677 (rebased from fcdb25ee) | freshness_surfaces_g_freshrender.rs, guard renamed `no_freshness_surface_writes_its_own_export_blocking_sentence` (6 tests) | ORCHESTRATOR-INITIATED. The guard the orchestrator asked for in F2.2 was written against a head without F2.3, and its failure message asserted "F2.3 has not landed the gate and a stale operation still exports". F2.3 landed first, so TWO things had become untrue about it WHILE IT STILL PASSED — the worst way for a test to be wrong: its message asserted a falsehood to whoever tripped it, and it forbade a now-TRUE sentence ("this will not export until you regenerate"), which would have sent that author hunting a hole that had already been filled. The worker agreed with the orchestrator's reading rather than overturning it: the risk INVERTED. Over-promising is impossible now the gate exists; the live risk is a second surface explaining WHY an operation blocks in its own words and drifting from F2.3's `blocking_toolpath_message`. New rule: a freshness surface MAY say an operation will not export, but the sentence comes from the shared builder. Four surfaces scanned, up from two; a file that calls the builder is skipped, because it is using the one text by construction; only string literals with a blocking CLAIM (cannot / won't / block / refus / prevent / …) are caught, comments exempt, and the bare word "export" stays legal — the "Export G-code…" button and the Getting-started step both survive, which is the test of whether the rule is drawn in the right place. Added `the_shared_blocking_text_is_available_to_a_freshness_surface`, pinning the builder public and keyed on `FreshnessState`, because a rule saying "call this instead" is only fair if a surface can. The new failure message names the builder to call instead of asserting a state of the world that can go stale — which is exactly what made the old one rot. WORKER JUDGEMENT the orchestrator endorses: told that an own sentry was not required for a subject change, it injected the exact violation anyway and confirmed the guard bites, on the principle that a guard which has never failed is a guard nobody has checked; the injected line is not in the commit and the output is quoted in the report. No production code changed: one test file plus the report. |
| 2026-09-10 19:50 | F2.4 | done (verified, merged) | 9544ca75 (rebased from 0846eba9) | extends `a_param_edit_mid_generate_*` in `controller/tests.rs` (same file, same fixture, beside the auto arm) plus an acceptance row; pre-fix 1 passed / 1 failed with the comparison reverted to `(Some(_), Some(_)) => true`, output verbatim | NO CORE FILE CHANGED — the orchestrator budgeted for one and it was not needed, because F2.1 had already made `toolpath_revision` public and given `insert_result` the deliberate non-bumping behaviour. F2.4 is the FIRST CONSUMER of the counter F2.1 built and said was for this task. Three edits, all in rs_cam_viz: `ToolpathRuntime::submitted_revision`, a stamp immediately before the handover in `submit_toolpath_compute`, and a comparison on arrival in `drain_compute_results`. THE KEY DESIGN POINT, verified in the code by the orchestrator: on a mismatch the core slot is left EMPTY — which is what derives `EditedSince` and puts STALE on every surface F2.2 wired — while `rt.result` still receives the geometry, so the viewport draws it and a minutes-long 3D generation is not thrown away. Stored, not discarded, and the worker argued the case rather than merely complying: discarding costs the operator the whole compute and leaves a STALE card over an EMPTY viewport, with nothing to distinguish "your edit cancelled a generation you were watching" from "it never ran". The stamp lives on the GUI runtime rather than the `ComputeRequest` because the request crosses a thread boundary and is an INPUT to generation, while what the GUI believed it submitted is bookkeeping; equivalent here because at most one job per toolpath is in flight. Fallback arm `_ => true` (never submitted through this controller, or the toolpath is gone) is the conservative choice and is documented — absent evidence of a mismatch the result is treated as current, so no existing path changes behaviour. WHY THE ROW NAMES THE MANUAL ARM: the auto arm was already safe by an unrelated route (resubmit → supersede → G-REGEN-RACE drops the abandoned result before it arrives); nothing supersedes on a 3D manual-regen op. The sentry asserts `!superseded_toolpaths.contains(..)` so it cannot silently decay into a second copy of the auto arm, and the auto arm's own sentry still passes untouched, which is the evidence the guard does not disturb the supersede path. WORTH KNOWING: `rt.status` stays `Done` because the generation genuinely did finish, and WHILE THE JOB RUNS the state is `Regenerating`, not `EditedSince`, because `freshness` puts the lane status first — so the operator is told work is in progress; the sentry pins that intermediate state too, because it is easy to "fix" into a wrong answer. ORCHESTRATOR VERIFICATION of both rows: read both diffs and the rewritten guard; confirmed the late-result path leaves the core slot empty and fills the GUI slot; rebased both over lane p3's F3.7/F3.8 merge with no conflict; fmt clean, clippy heavy-tests clean 40.2s, 27 viz binaries / 534 tests green, zero failures. Core not re-run and not required: `git diff --name-only HEAD~2 HEAD -- crates/rs_cam_core/` is empty. NEW FOLLOW-ON F2.10, added to PLAN §4: the SIMULATION arm of the same race (R0.1 §5's `…_g_latesim`) — same shape, different counter (`is_stale` / `edit_counter`), not in this row. |
| 2026-09-10 21:05 | ORCHESTRATOR ERROR | corrected by w-f37 in 16da77fb | 3350595c introduced it | — | THE ORCHESTRATOR MERGED A FALSE SENTENCE INTO `CLAUDE.md` AND MUST OWN IT. Resolving lane p3's rebase conflict at 3350595c, the orchestrator kept both sides of a `CLAUDE.md` paragraph after inspecting the incoming side TRUNCATED AT 260 CHARACTERS. The untrue clause sat at the END of it: "The GUI's Tool: / Input: combos write the same two fields but invalidate nothing today, so the MCP route is the stricter of the two." That claim was true at the worker's base `c2089d9d` and false at the head being merged, because F2.1's `generation_inputs_signature` includes `tc.tool_id` and `tc.model_id`. The orchestrator had ALREADY identified and rejected this exact claim in the same worker's chat report, and recorded the rejection in the 18:40 ledger row — and then failed to carry it into the FILE. So the wrong statement was live in `CLAUDE.md`, the document every agent in this repo reads as authoritative, from 3350595c until 16da77fb. LESSON, recorded because it generalises: a conflict resolution is a content decision, not a mechanical one. "Keep both sides" is only safe when both sides have been READ IN FULL; a truncated inspection is not a reading. The correct check was the one the orchestrator applies to every worker claim — verify against the merged head — and it was skipped precisely because the text arrived as a merge conflict rather than as a claim. CAUGHT AND FIXED BY THE WORKER, unprompted: w-f37 re-read both `generation_inputs_signature` and its own sentence at 3350595c rather than taking either on trust, and corrected the bullet in F3.5's commit. The undo half of its original observation stands and is retained: neither route creates an undo entry for a rebind. |
| 2026-09-10 21:10 | F3.1 + F3.5 | done (verified, merged) | (see head) (rebased from 16da77fb) | add_toolpath_via_gui_g_guiadd.rs (9: 4 schema + 5 driving the GUI path on the flat-first seed) and get_notifications_g_toastread.rs (8, including the F1.1 pairing end to end); pre-fix `MISSING: ["add_toolpath_via_gui", "get_notifications"]` out of 76 registered tools | F3.1 dispatches the REAL widget event: `ui/toolpath_panel.rs::add_op_menu_item` pushes `AppEvent::AddToolpath(op)` and the tool routes through the same `handle_add_toolpath` entry point `controller/tests.rs` already uses. NO binding logic reimplemented — not the `tools().first()` pick, the Suggest call, the `models().first()` fallback, nor the B.7 boundary auto-enable — which is the whole point, since F4.2's acceptance depends on this path being the GUI's own. `setup_index` is honoured the way a click is (select the setup, then raise the event, because `handle_add_toolpath` reads the selection and takes no setup argument), and an out-of-range index is refused BEFORE dispatch so a typo never leaves a toast on the operator's screen. The reply must read the toast stack because `handle_add_toolpath` returns `()` and the refusal text exists nowhere else — so it reads it through F3.5's accessor, and the two tools verify each other rather than each asserting its own story. The new row is found by id-set difference, not `len()-1`, because `add_toolpath` inserts into the setup's ordering. ONE DELIBERATE EXCEPTION TO F1.1, and the reasoning is right: the arm pushes no toast of its own, because the GUI path already pushed exactly the one F1.1 requires and a second would break F1.1's own "exactly one Warning" assertion; zero bare `push_notification(` added, so F1.1's census sentry is untouched. The §5 parity case is present: with a compatible ball nose in the project and second in the list, Scallop is STILL refused, and the assertion message says F4.2 will flip it — so a future trip reads as the contract arriving, not as a regression. Behaviour is unchanged by design; this makes the path observable and F4.2 changes it. F3.5 adds `AppController::notifications(&self) -> &[Notification]` — nothing outside the crate could see the stack before. READ-ONLY, with `&self` so the compiler enforces it: draining would clear toasts the operator had not read, from a call they did not make; "what did THIS call push" is answered by snapshotting the length and reading the tail, which only works because reads do not mutate. Age is `created_at.elapsed()`, in-process, empty after a restart, with `ttl_seconds` (4/6/8 by severity) and `visible` derived so a reader need not know the table; `include_expired` defaults to TRUE because expired entries are collected on the frame loop and an assertion running a second late should not lose the evidence. Both surfaces share one `notification_json` builder so F3.1's array and `get_notifications` cannot drift. ORCHESTRATOR VERIFICATION: read the diff; confirmed the false `CLAUDE.md` clause is gone at this commit and replaced with the accurate statement; rebased with no conflict; fmt clean, clippy heavy-tests clean 34.6s, 29 viz binaries / 551 tests green, `-p rs_cam_mcp` 29 green. Core not re-run and not required: no core path in the diff. Nothing exercised live; MCP server still down. |
| 2026-09-10 22:15 | F2.5 | done (verified, merged) | (head) (rebased from b277fc1f) | 4 cases in `controller/tests.rs`; pre-fix 1 passed / 3 failed, output verbatim, the tool case reading `left: Current` over geometry cut with a tool no longer in the project | P2 IS CLOSED but for F2.6. The worker reduced the row's three clauses — written before F2.1 existed and phrased in the old three-store vocabulary — to ONE rule: after any undo or redo, the derived `FreshnessState` of every affected toolpath, the dirty flag and the simulation's staleness are what they would be if the operator had made that same edit BY HAND. TWO REAL DEFECTS across five arms, with a per-arm table in the report. (1) `ToolChange` bypassed `invalidate_tool` in BOTH directions, so dependents kept a CORE result cut with the OTHER tool's geometry, read `Current` on every surface, and would have passed F2.3's export gate — that is the serious one. (2) NO arm called `mark_edited`, so an undone project was not offered for saving, the close interception did not warn, and the GUI presented a simulation computed from the just-discarded configuration as current evidence. The row's third clause needed no separate fix: "re-stales the simulation" is exactly what `mark_edited` does on that arm, with the hand-edit treatment (stale, not cleared), which is why `apply_toolpath_snapshot` deliberately does not call `invalidate_simulation`. `mark_edited` is called ONCE after each match, not five times inside it, so a sixth `UndoAction` cannot be added without it — the defect being closed is that five arms each had to remember and none did. THE DECISION, made explicitly as the orchestrator required rather than picked silently: AN UNDONE EDIT DOES NOT RESTORE `Current`. Three reasons, the third decisive. (a) Nothing records which configuration the retained `rt.result` answers, and since F2.4 it may itself be a late result for a superseded config, so promoting it could make current a result that never matched any configuration the operator asked for. (b) The snapshot carries three of the nine fields in `generation_inputs_signature`; stock, tool and model may have moved since. (c) Rolling `toolpath_revision` BACKWARDS would break F2.4's guard, because a late result stamped with the rolled-back value would then be accepted. Cost stated plainly rather than hidden: undo-then-redo needs a regeneration although nothing net changed. The decision is pinned by its own test with the reasoning in the failure message, so reversing it must be somebody's decision rather than a side effect. REDO had every one of the same defects and is fixed in the SAME commit, not ledgered — the worker's reason, which the orchestrator endorses: splitting would ship a release where Ctrl+Z is honest and Ctrl+Shift+Z is not. Both directions now share `apply_tool_snapshot` and `apply_toolpath_snapshot`. ONE SUBTLETY worth preserving: the simulation assertion is "cleared OR stale, never present-and-fresh", because three arms clear and two stale, matching their hand equivalents — asserting `is_stale` alone was WRONG and failed on the clearing arms while they behaved correctly, since `is_stale` requires `last_run.is_some()` and clearing sets it to `None`. ORCHESTRATOR VERIFICATION: read the diff; confirmed `apply_tool_snapshot` calls `invalidate_tool` and propagates to dependents exactly as F2.1's `commit_tool_draft` does, and that both the undo and redo matches route through the shared helpers with `mark_edited` after each; rebased with no conflict; fmt clean, clippy heavy-tests clean 36.4s (after the worker factored one `type ArmBuilder` for `type_complexity`), 29 viz binaries / 555 tests green. Core not touched. F2.11 opened for the flaky test below. |
| 2026-09-10 22:20 | F2.11 | opened (gate-trust) | — | — | REPORTED BY THE WORKER UNPROMPTED, about a failure it did not cause and could have said nothing about. `compute::worker::tests::cancelled_toolpath_returns_partial_debug_trace` failed on its first full-suite run while the machine was still loaded from clippy, then passed 4/4 on re-run; it touches no module F2.5 changed. The ORCHESTRATOR probed it independently: five consecutive isolated runs, all green (0.04s each). So it is not reproducible on demand and is most likely a tight timeout under contention rather than a logic race — but that is a hypothesis, not a finding, and the orchestrator has NOT established a cause. WHY THIS IS RECORDED AS A PROGRAMME ITEM rather than a curiosity: this entire verification method rests on a red gate meaning a real regression. A test that fails under load and passes on retry is exactly the thing that trains an operator, or an agent, to re-run until green — and with three lanes contending for one build cache on this machine, load is the normal condition, not the exception. RULE until F2.11 is closed: a lone failure in this test on a loaded machine must be re-run before it is believed, and the re-run must be RECORDED in this ledger, never silently taken as the answer. |
