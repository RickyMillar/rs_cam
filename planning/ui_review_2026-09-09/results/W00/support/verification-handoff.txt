I have all precise line references. Now I'll write the final report (<=1800 words). Let me be disciplined and evidence-anchored.

Note on confidence: I'll mark CODE observations as CODE/TEST evidence, live checks as the concrete thing to verify, and avoid inferring safety guarantees or novice success.

Let me write the final response.# R06+R07 Supporting Investigation — Simulation → Readiness → Export

**Scope:** CODE-only review (read/grep/find). Checkout ef91cb03; live binary 8a4df241-dirty is **not** this checkout, so CODE evidence ≠ live parity. Inspected: `ui/sim_op_list.rs`, `ui/sim_diagnostics.rs`, `ui/readiness_panel.rs`, `ui/readiness.rs`, `ui/preflight.rs`, `ui/export_wizard.rs`, `ui/menu_bar.rs`, `ui/components/freshness.rs`, `state/simulation.rs` (is_stale), `app/input.rs` + `app/export.rs` (export dispatch), `controller/io.rs`, core `tool_load/verdict.rs`, and `tests/wizard_e2e.rs` (test scope inspected, not only filename). Evidence labels: CODE/TEST. No HUMAN/DESKTOP/MCP interaction performed — discoverability outcomes are **untested**.

---

## 1. Path/state matrix — what a human is told

Staleness driver: `SimulationState::is_stale` = `last_run.is_some_and(|m| current_edit_counter > m.last_sim_edit_counter)` (simulation.rs:1003). Requires results to exist; an unsimulated job is *not* "stale", it is "not run" — distinct states the UI keeps separate.

| Surface | Generated-unsimulated (toolpaths computed, no sim) | Current-sim (fresh results) | Edited-stale (results exist, edit_counter advanced) |
|---|---|---|---|
| **sim_op_list (left panel)** | "No toolpaths computed" card w/ "Go to Toolpaths" OR "Ready to simulate" card + "Run Simulation" (if any enabled config) (sim_op_list.rs ~80-120). No staleness banner. | Op list; per-tp worst-of status flags + kinematics pill (planned/emitted provenance); "Re-run Simulation" button. | "⚠ Results may be stale" banner + "Re-run Simulation", **then** the (stale) op list with stale status flags below it (sim_op_list.rs ~140-170). |
| **sim_diagnostics (Inspector)** | Body: "Run simulation to see the cut overview here." (results.is_none()). Status header **not drawn** (drawn only when results.is_some()). | Status header: collision/air/OK banner + "✓ live" chip; Project/Toolpath/Span sections (cycle basis, within/exceeding pills, NOT MEASURED strip, must-address vs informational, top hotspots). | Status header shows "⚠ Results stale" chip (FreshnessGate::banner, sim_diagnostics.rs:178). **Body sections are NOT wrapped in a FreshnessGate** — stale hotspots/load badges/pills render at full opacity. |
| **readiness_panel** | Headline ≥ "REVIEW" (sim_status=Warning). Sim row "Not run"; Rapid "Run simulation first"; Holder "Not checked"; Tool load Warning (empty report). Cycle: CuttingOnly basis + inline caveat. | FreshnessGate open (full opacity). Sim "Up to date" ✓; Rapid count/"None detected"; Holder "Clear"/count; Tool-load pills; cycle basis-tagged. | FreshnessGate **dims all detail rows** via `set_opacity(0.5)` (freshness.rs:53) but **interaction preserved**. Headline ≥ "REVIEW" (staleness forces sim_status=Warning, so banner can never read "READY" when stale). Bottom "Run simulation" button is **absent** (only renders when `!has_results`). |
| **preflight modal** | Same checks as readiness; "Not run"/"Run simulation first"/"Not checked". Export G-code button; gate blocks on unmodeled/exceeded unless overrides. | Up-to-date checks; Export G-code enabled (unless load gate blocks). | "Stale — parameters changed"; rows shown; override checkboxes if unmodeled/exceeded. |
| **export_wizard** | Resumes at `last_step_visited`. Step 6 preview re-emits gcode each frame; validator findings shown; Save blocked on errors unless override. | Same; Save enabled. | Same (wizard does not gate on sim freshness — staleness is not consulted in step_save). |
| **Direct export menu** | "All toolpaths" → opens preflight. "Combined"/"Per-setup" → `export_*_from_session` + save dialog **directly**. | Same. | Same — **no preflight, no collision confirmation, no validator gate** shown for Combined/Per-setup (input.rs:240,276). |

---

## 2. Candidate risks (≤4)

### Risk A — Direct Combined/Per-setup export bypasses preflight collision confirmation and the validator gate
**File:line:** menu_bar.rs:104,111 (launch); input.rs:240 `ExportCombinedGcode`, input.rs:276 `ExportSetupGcode`; contrast export.rs:222 `gate()` (called **only** in `handle_wizard_save`); preflight.rs:~195-234 (collision "I understand the risks" confirmation lives only in the preflight modal).
**Reproducible state:** scratch job, ≥2 setups, simulate so a holder/rapid collision is present, then File ▸ "Direct export (skip wizard)" ▸ "Combined (M0 pauses)".
**CODE observation:** `ExportCombinedGcode`/`ExportSetupGcode` call `export_*_from_session` then write via `rfd::FileDialog::save_file()` with no preflight modal, no `self.gate()` validator call, and no collision-confirmation checkbox. The tool-load gate may still refuse *inside* the export helper (the `_checked` path per wizard_e2e `build_session` setting `accept_unmodeled=true`), but if it refuses the user only receives an error notification — there is no UI presenting override checkboxes or the collision summary. The wizard path (`handle_wizard_save` → `self.gate()`, export.rs:222) and the preflight path both surface these before writing.
**Concrete live check:** with an unresolved holder collision, export via File ▸ Direct export ▸ Combined — does it write a file with zero collision warning/confirmation? Compare Readiness ▸ "Export G-code…" which shows the preflight collision confirmation. **Confidence: probable** (the `export_combined/setup_gcode_from_session` helper bodies were not read; whether they enforce the tool-load gate internally needs verification).

### Risk B — Inspector detailed findings render stale data at full opacity; only a top chip signals staleness
**File:line:** sim_diagnostics.rs:178 (`FreshnessGate::banner` — chip only, in `draw_status_header`); `draw_project_section`/`draw_toolpath_section`/`draw_span_section` are **not** wrapped in `FreshnessGate::show`. Contrast readiness_panel.rs (FreshnessGate::new(stale).show wraps all detail rows) and freshness.rs:48-58 (`show` dims+banner; `banner` only banner).
**Reproducible state:** simulate a job with ≥1 hotspot/collision, then edit one feed or depth parameter (advancing edit_counter), then scroll the Inspector to a focused hotspot/issue card or the Top-hotspots list.
**CODE observation:** `draw_focused_hotspot_card`/`draw_focused_issue_card` and the project findings render `wasted_runtime_s`, `peak_chipload_mm_per_tooth`, collision counts and load badges from the stale trace with no inline stale marker; the only cue is the "⚠ Results stale" chip at the top of the panel. A user who opens a focused hotspot card (or the Top-hotspots collapsible) sees stale numbers as if current. The readiness panel avoids this by wrapping its rows; the Inspector does not.
**Concrete live check:** after a param edit, open a focused hotspot card — is any inline indication present that the hotspot data predates the edit? **Confidence: confirmed (CODE).**

### Risk C — Preflight-confirmed export does not run the gcode_validator; the validator-error gate is wizard-only
**File:line:** export.rs:304 `export_gcode_with_summary` (preflight-confirmed path: `controller.export_gcode()` then save dialog — no `self.gate()` call in the read span, export.rs:300-360); export.rs:222 `gate()` (validate + block-on-error) is invoked **only** from `handle_wizard_save`; export_wizard.rs step_save (`save_blocked = errors > 0 && !allow_validator_errors`).
**Reproducible state:** a job whose emitted gcode produces a validator `Severity::Error` that is not already a hard export refusal (e.g., a post-limit/structural finding rather than the inch block, which is refused upstream). Export via Readiness ▸ "Export G-code…" ▸ preflight ▸ "Export G-code".
**CODE observation:** `export_gcode_with_summary` proceeds straight to the save-file dialog without calling `validate()`. The wizard's Step 6 shows findings and Step 7 blocks Save on errors. Thus two export entry points differ on the validator-error check: wizard enforces, preflight-confirmed does not (in the read span). The preflight modal *does* show the tool-load gate and collision confirmation, so this is specifically the gcode_validator-error layer that diverges.
**Concrete live check:** craft a job emitting a validator Error; export via preflight — does it save without surfacing the error, where the wizard would block? **Confidence: probable** — 103 lines of `export_gcode_with_summary` were unread; the read span (300-360) shows no gate call, but a gate call in the unread tail cannot be excluded. Verify the full function.

### Risk D — A fully-vacuous load report reads "Pass" on readiness; vacuity is handled only at the row-flag level, not in the headline/summary
**File:line:** readiness.rs:121 `tool_load_check` (consults only `any_exceeded`/`any_unmodeled`/`per_toolpath.is_empty()` — **no `is_vacuous`**); verdict.rs:594-595 ("a vacuous `Within` still reads `Within`"); verdict.rs:453-510 `summary()` buckets a toolpath as `within` when `!any_exceeded && modeled_count > 0` — a vacuous Within is not Unmodeled, so `modeled_count > 0`, so it counts as `within`; vacuity handling exists only at sim_op_list.rs:1065 and sim_diagnostics.rs:1036 (`if status.is_vacuous()` → ∅ glyph).
**Reproducible state:** a toolpath whose load criteria all return `Within` on an empty measurement population (the X-VAC case: "a gate handed an empty population returns `Within`", sim_diagnostics.rs:~1036 comment). Such a project → `summary.within = total`, `exceeds=0`, `fully_unmodeled=0` → `tool_load_check` returns `Pass`.
**CODE observation:** readiness_panel's Tool-load row shows `CountPill::verdict("within", summary.within)` green with no vacuity marker, and if all other checks Pass the headline banner reads "✓ READY TO CUT". The Inspector row flags show ∅, but the dashboard headline and the Findings pills do not distinguish vacuous-within from measured-within. This is exactly the "empty gate population can be vacuous" warning: green that rests on nothing measured.
**Concrete live check:** produce a toolpath with empty cutting-contact population; on the Readiness panel, does the Tool-load row / headline distinguish "within on nothing" from "within measured"? **Confidence: confirmed (CODE) for the threshold logic; the reproducible fixture trigger needs live confirmation.** Note: `Within` on a genuinely measured empty cut is *not* a safety guarantee — it is an abstention dressed as a pass.

---

## 3. Strengths to preserve

- **State distinction is deliberate and consistent.** "No toolpaths computed" / "Ready to simulate" / "⚠ stale" / "✓ live" are separate UI states (sim_op_list.rs); `is_stale` requires results to exist (simulation.rs:1003), so unsimulated is never mislabeled stale. The brief's absent/unmeasurable/zero/stale/drill-native distinction is largely honored in CODE.
- **Vacuity is named, not hidden, at the row level.** X-VAC handling renders ∅ (sim_op_list.rs:1065, sim_diagnostics.rs:1036) and the tooltip prepends the vacuity clause (sim_diagnostics `verdict_tooltip`), explicitly so "an op whose gates measured nothing looked cleaner than one whose gates measured an approximation." Drill `NotApplicableForOp` is bucketed separately from `fully_unmodeled` (verdict.rs:467-477) and skipped from row flags (`is_drill_not_applicable`).
- **FreshnessGate preserves interaction.** `set_opacity(0.5)` dims visually but does not disable widgets (freshness.rs:53), so in-gate "Run sim"/"Re-check" jump buttons stay clickable when stale — no staleness trap.
- **Staleness forces ≥ Warning on the readiness headline.** `simulation_check` returns Warning when stale, so the verdict banner can never read "READY TO CUT" on stale evidence (readiness.rs `simulation_check`).
- **Cycle time always travels with its basis** (CycleTimeBasis: MachineModel/SimulatedNoAccel/CuttingOnly) and prints an inline caveat + remedy on every surface including the wizard Save step and setup sheet (readiness.rs). A dash is used for no-estimate, never 0:00.
- **Two distinct override flags** (accept_unmodeled vs accept_exceeded) in preflight (preflight.rs `draw_tool_load_overrides`) — "we don't know" vs "we know it's bad" are not collapsed.
- **Wizard steps are all dispatched** (export_wizard.rs:62-69, 0..6), contradicting the stale module comment ("only Step 1 is wired"); the validator gate + dry-run clamp + inch refusal + datum-frame correction are all exercised by wizard_e2e tests.

---

## 4. MCP parity caveats

- **A backend `export_gcode` call is not a GUI wizard interaction.** wizard_e2e.rs is explicitly a *backend data-path* harness: it builds a `ProjectSession`, mutates `WizardState` "exactly the way the UI's AppEvent handlers do", and calls the same `io::export` helpers. It does **not** drive egui, click steppers, open the preflight modal, render the collision-confirmation checkbox, or present the validator-findings scroll. The file header states the MCP layer "today just has `export_gcode`, which doesn't model the wizard's configurable layout/template." So MCP-STATE/backend parity of *emission* does not establish GUI *route* parity, *override UX*, or *discoverability*.
- **wizard_e2e never runs a simulation** (build_session sets `accept_unmodeled = true` because "every criterion reads Unmodeled(SimulationRequired)"). It therefore does not exercise the stale/current-sim distinction, the NOT MEASURED strip, the per-row ∅ rendering, or the readiness headline — those CODE paths have no test coverage observed here.
- **The named test scope is narrower than its filename suggests.** `wizard_inch_units_override_blocks_export` confirms the inch refusal; `wizard_dry_run_clamps_cutting_moves_to_safe_z` confirms Z-clamping; `per_setup_export_puts_identity_setup_in_the_stock_relative_frame` confirms the datum shift. None assert that the *preflight-confirmed* path or the *direct Combined/Per-setup* path enforce the validator gate or surface collisions — so Risk A/C are not covered by this test file. Treat any MCP "export succeeds" claim as establishing emission only, not gate interaction.
- **generated ≠ simulated ≠ measured Within ≠ physical safety.** Passing the modeled tool-load gate (even non-vacuously) does not certify the real cut; the wizard's validator pass does not either. No threshold changes are proposed.
