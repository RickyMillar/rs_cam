# 2026-09-17 baseline — re-baseline after the structure programme

**Predecessor:** `2026-06-04.csv` (Phase 5 Step 5.4, per-species wood Kc).

**Trigger:** the Q1 remainder of tech-debt programme 2 landed in
`bb1ccc7b`: the CLI smoke runner now passes `SuggestContext.model_bbox`,
so the runtime-sanity stepover back-off can fire in the smoke cases. The
baseline was 3.5 months old, so a diff against it could not isolate that
change.

## Paired measurement of the Q1 change

The suite ran twice on the same day at resolution 0.5 mm: once with the CLI
built at `bb1ccc7b^` (the commit before the change) and once at `f649bef6`.
The two CSVs are byte-identical. **The populated Suggest context changes no
smoke case.** No case reaches the stepover back-off on this matrix.

## Diff against `2026-06-04.csv` (months of intended change, not one commit)

The June baseline predates the `resolution_mm` column and the unified
load model, the per-axis kinematics, the plunge guard, the entry-move
work and the rapid-safety phase. The runner reports 1 regression and many
value moves. None of them is from today. Recorded here so the operator can
read them; re-run `rs_cam_cli smoke --diff --baseline
planning/toolpath_acceptance/baselines/2026-06-04.csv --output
planning/toolpath_acceptance/baselines/2026-09-17.csv` to reproduce.

| case | change |
|---|---|
| AS012 | status `generation_failed` → `ok`; drill gates now written (`within`, `within`, `within`; chip welding observed 0.667) |
| AS013 | chipload `exceeds_low` → `within`; observed 0.018 → 0.074 mm/tooth; deflection 0.118 → 0.009 mm; power 0.017 → 0.103 kW; peak axial DOC 14.8 → 3.7 mm |
| AS014 | chipload `exceeds_low` → `exceeds_high`; deflection `exceeds` → `within`; **power `within` → `exceeds`** (0.028 → 1.007 kW) — the one verdict the runner calls a regression |
| AS015 | chipload `exceeds_low` → `exceeds_high`; power 0.002 → 0.151 kW; peak axial DOC 3.5 → 6.8 mm |
| AS017 | deflection `exceeds` → `within`; rapid collisions 100 → 0; deflection 1.29 → 0.05 mm |

**For the operator:** AS014's power verdict moved from `within` to
`exceeds`. The power model changed on purpose between June and now
(`feeds::force`, literature-absolute recalibration, T-8 `a7c17be7`), so the
new verdict is most likely the model's, not a defect; it is not proven
either way here. The regression net now measures from this file.
